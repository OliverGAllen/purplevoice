---
status: partial
phase: 02.7-security-posture
source: [02.7-VERIFICATION.md]
started: 2026-04-30T00:00:00Z
updated: 2026-04-30T00:00:00Z
---

> **Item 2 resolved 2026-04-30** via Option (a): SECURITY.md §SBOM now contains a "Scope disclaimer: repo-only Syft scan" subsection that honestly enumerates the 1-package SBOM, cites Pitfall 2 rationale, points auditors to upstream Homebrew SBOMs for transitive enumeration, mentions the per-binary SHA256 pinning in setup.sh, and reserves a future opt-in `verify_sbom_full.sh` for institutional adopters who need the consolidated view. Framing lint + functional + security suites all GREEN; Pattern 2 invariant intact.

## Current Test

[awaiting human testing — none of these items block phase close per verifier; recommended pre-release sweep before tagging v1]

## Tests

### 1. Sudo-authenticated `verify_egress.sh` empirical run on Oliver's macOS Sequoia 15.7.5 build 24G624
expected: Either (a) PASS (full 3-layer evidence) — pf is functional on this build, or (b) PASS (weakened — pf broken on this macOS build per Pitfall 1) — positive control caught the regression and layers 1-2 carry the claim.
why_human: Plan 02 SUMMARY: "Pitfall 1 positive control evaluation deferred — sudo unavailable in this execution context. Empirical evaluation of pf enforcement on Oliver's macOS Sequoia 15.7.5 (24G624) is pending an interactive sudo run." The verifier ran without sudo and confirmed the weakened-PASS path; the strongest layer's behaviour on Oliver's specific OS build has not been observed.
how: `sudo bash tests/security/verify_egress.sh` — read the output for layer-3 PASS or PF_BROKEN flag.
result: [pending]

### 2. SBOM scope-gap editorial decision
expected: Either (a) accept the documented gap (Syft scoped to repo only per Pitfall 2; transitive/Hammerspoon/brew binaries not enumerated as separate package entries — only carried by reference via system-context annotations + supporting prose), or (b) extend Step 8 to do a multi-scope scan (`/opt/homebrew/Cellar/{sox,whisper-cpp}` + `/Applications/Hammerspoon.app`) before v1 release.
why_human: SECURITY.md §SBOM claims "Direct dependencies: sox, transcription binary, ggml-small.en.bin, ggml-silero-v6.2.0.bin, Hammerspoon.app, ... Transitive: Auto-discovered by Syft." The actual SBOM has 1 package (PurpleVoice itself). RESEARCH Pitfall 2 anticipated this scope tension; Plan 02 chose repo-only scope to avoid leaking unrelated brew formulae into the SBOM. A government IT auditor reading the prose claim and then opening SBOM.spdx.json may notice the asymmetry. Three mitigations are documented in VERIFICATION.md (recommended Option 1: one-paragraph SECURITY.md disclaimer noting the repo-only scope + reference to brew/Homebrew upstream SBOMs for transitives).
result: RESOLVED 2026-04-30 — Option (a): Scope disclaimer subsection added to SECURITY.md §SBOM (1 paragraph, ~210 words). Honestly enumerates the 1-package SBOM, cites Pitfall 2 rationale (repo-only scope to avoid leaking unrelated brew formulae from $HOME), points to upstream Homebrew SBOMs (formulae.brew.sh/api/formula/{sox,whisper-cpp}.json + cask/hammerspoon.json) for transitive enumeration, mentions per-binary SHA256 pinning in setup.sh (MODEL_SHA256, SILERO_SHA256, brew bottle hashes), reserves opt-in tests/security/verify_sbom_full.sh as future extension. Framing lint GREEN; functional suite 8/0; security suite 5/0; Pattern 2 invariant intact.

### 3. Hostile-read honesty pass over the 6 framed framework sections + 3 deferral sections
expected: No sentence can be screenshot to mislead an institutional buyer into thinking PurpleVoice IS [framework] compliant/certified/authorised. Disclaimer-first discipline holds; "compatible with" / "consistent with" / "supports" framing is consistent.
why_human: Lint catches mechanical drift (`\bcompliant\b` / `\bcertified\b` / `\bguarantees\b` outside qualified contexts). Subtle phrasing drift (e.g., "PurpleVoice supports HIPAA technical safeguards" read fast) is human-only. Spot-check of disclaimer-firsts (verifier confirmed all 6 sections open with explicit Status disclaimer; HIPAA / SOC 2 / ISO 27001 lead with "PurpleVoice is a tool, not [an organisation/Covered Entity/ISMS-operator]") passed automated review. Final hostile-read pass before tagging the v1 release recommended.
result: [pending]

### 4. Optional: Run `PURPLEVOICE_OFFLINE=1 bash setup.sh` on a genuinely network-disconnected Mac with sideloaded artefacts
expected: setup.sh completes with `OFFLINE: ... present` lines for all 4 sideloaded artefacts; subsequent recording attempt produces a transcript without any network access.
why_human: `verify_air_gap.sh` simulates the absent-model path by atomic mv; it does not prove the documented procedure works in a real air-gapped environment. VALIDATION.md Manual-Only Verifications already lists this as "optional pre-release; deferred to first institutional adopter feedback if separate offline machine unavailable".
result: [pending]

## Summary

total: 4
passed: 1
issues: 0
pending: 3
skipped: 0
blocked: 0

## Gaps

(none — all items are pre-release sweeps + 1 editorial decision, none block phase close)
