---
phase: 02.7-security-posture
plan: 03a
type: execute
wave: 2
depends_on: ["02.7-01", "02.7-02"]
files_modified:
  - SECURITY.md
autonomous: true
requirements: [SEC-01]
gap_closure: false

must_haves:
  truths:
    - "SECURITY.md §NIST SP 800-53 Rev 5 / Low-baseline Mapping has hybrid prose+table with >=15 per-control rows from AC, AU, IA, SC, SI, PT families"
    - "Status vocabulary across NIST table: only Met / Partial / Not Pursued / N/A (Pitfall 15)"
    - "Zero Rev-4-only NIST control IDs (Pitfall 13)"
    - "tests/test_security_md_framing.sh still GREEN (lint baseline maintained)"
    - "Pattern 2 invariant intact"
  artifacts:
    - path: SECURITY.md
      provides: "NIST SP 800-53 Rev 5 / Low-baseline deep per-control mapping (Plan 03b adds 6 framed sections)"
      contains: "## NIST SP 800-53 Rev 5 / Low-baseline Mapping"
  key_links:
    - from: SECURITY.md §NIST 800-53 mapping
      to: tests/security/verify_egress.sh
      via: "SC-7 Boundary Protection cross-reference"
      pattern: "verify_egress\\.sh"
---

<objective>
Author the NIST SP 800-53 Rev 5 / Low-baseline section in SECURITY.md with per-control depth (D-14). This is the deepest framework mapping (>=15 per-control rows from AC, AU, IA, SC, SI, PT, CM, MP, SA, SR families); Plan 03b authors the 6 lighter-touch framed framework sections.

Purpose: This is the gap analysis a US federal IT auditor reads first. The hybrid prose+table format (D-15) supports skim and depth simultaneously. The "compatible with" framing (D-17) is the load-bearing trust constraint — over-claims here damage the entire SECURITY.md narrative.

**Plan split rationale (per checker M-5):** The original Plan 03 produced ~50 table rows across 3 tasks, each requiring careful "Met/Partial/Not Pursued/N/A" framing decisions. Pitfall 11 review burden is highest for the 6 framed sections (HIPAA / SOC 2 / ISO 27001 are organisational standards most often confused with product certifications). Splitting NIST (this plan) from the 6 framed sections (Plan 03b) reduces content density per task and isolates the highest-Pitfall-11-risk content into its own plan.

**Sequencing:** Plan 03a runs in Wave 2 (after Wave 1's Plans 01 + 02). Plan 03b runs in Wave 3 (after this plan completes), because both touch SECURITY.md (merge-conflict avoidance — see checker M-5 cleaner-than-parallel choice). Plan 04 moves to Wave 4.

Output: SECURITY.md §"NIST SP 800-53 Rev 5 / Low-baseline Mapping" filled with concrete content. Per-control table has >=15 rows (AC-3, AC-6, AC-14, AU-2, AU-12, IA-2, IA-5, SC-7, SC-8, SC-12, SC-28, SI-2, SI-7, SI-10, PT-3, PT-4 — covering YES/LIMITED families per RESEARCH Priority 4).

Plan dependency: requires both Plan 02.7-01 (SECURITY.md threat model + framing lint baseline) and Plan 02.7-02 (verify_*.sh implementations referenced as substantiation).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.7-security-posture/02.7-CONTEXT.md
@.planning/phases/02.7-security-posture/02.7-RESEARCH.md
@.planning/phases/02.7-security-posture/02.7-VALIDATION.md
@.planning/phases/02.7-security-posture/02.7-01-SUMMARY.md
@.planning/phases/02.7-security-posture/02.7-02-SUMMARY.md
@SECURITY.md
@tests/test_security_md_framing.sh

<interfaces>
<!-- Verbatim content templates from RESEARCH.md Priority 4 + Pattern 7 + Pitfalls 4/13/15. Copy carefully. -->

D-14: NIST SP 800-53 Rev 5 / Low-baseline primary; 6 other frameworks framed only (Plan 03b).
D-15: Hybrid (prose + summary table). NIST table most detailed.
D-17: "compatible with" framing. The plan-checker / test_security_md_framing.sh enforces this. Banned outside qualified contexts: compliant, certified, guarantees.

Pattern 7: NIST 800-53 control row template (status vocabulary: Met / Partial / Not Pursued / N/A — never Compliant or Certified per Pitfall 15).

Required NIST 800-53 Rev 5 control rows (research Priority 4 — preview list of >=15):
- AC-3 Access Enforcement — Met (TCC enforces Microphone + Accessibility per bundle-id; hotkey requires user-scoped hardware access)
- AC-6 Least Privilege — Met (sox + whisper-cli run as user UID; no SUID bits; no privileged operations)
- AC-14 Permitted Actions w/o Identification — Met (no anonymous usage; user must hold hotkey on their own keyboard)
- AU-2 Event Logging — Not Pursued (by design; privacy > accountability; documented in §"Repudiation" of threat model)
- AU-12 Audit Record Generation — Not Pursued (same)
- IA-2 User Identification & Authentication — Met (via OS) (macOS user session = identification; PurpleVoice inherits)
- IA-5 Authenticator Management — N/A (no authenticators issued by PurpleVoice)
- SC-7 Boundary Protection — Met (zero outbound network egress at runtime; verified by `tests/security/verify_egress.sh`)
- SC-8 Transmission Confidentiality & Integrity — N/A (no transmission to protect — tool is local-only)
- SC-12 Cryptographic Key Establishment & Management — N/A (no keys; no cryptographic operations on user data)
- SC-28 Protection of Information at Rest — Partial (WAV ephemeral via EXIT trap; transcript not persisted; relies on macOS FileVault for at-rest protection of `~/.config/purplevoice/`)
- SI-2 Flaw Remediation — Partial (brew-tracked deps update via `brew upgrade`; PurpleVoice itself is git-tracked and update-visible; no vulnerability disclosure programme yet — see §Vulnerability Disclosure)
- SI-7 Software, Firmware, and Information Integrity — Met (`setup.sh` Step 5 SHA256-verifies the Whisper model; mismatch aborts)
- SI-10 Information Input Validation — Met (vocab.txt cap ~150 words; denylist exact-match per Phase 2 hardening)
- PT-3 PII Processing Purposes — Met (voice content processed solely for transcription; ephemeral; no telemetry — substantiated by SECURITY.md + verify_egress.sh)
- PT-4 Consent — Met (user-initiated hotkey; explicit TCC grants required)

Honesty discipline (Priority 4):
- Never claim Moderate or High baseline (those invoke continuous monitoring, IR family, AU-11 retention — organisational controls).
- Mark "Not Pursued" with rationale, NOT "Failed" or "Out of Scope" (auditors read "Not Pursued" as honest engineering choice).
- Pitfall 13: Use ONLY Rev 5 IDs. Banned: AC-3a-c subdivisions, SC-12(2) etc. that don't exist in Rev 5.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3a-1: Author SECURITY.md §NIST SP 800-53 Rev 5 / Low-baseline Mapping (deep per-control)</name>
  <files>SECURITY.md</files>
  <read_first>SECURITY.md, .planning/phases/02.7-security-posture/02.7-RESEARCH.md (specifically §"Priority 4: NIST SP 800-53 Rev 5 / Low-baseline" lines 612-672 + §"Pattern 7: NIST 800-53 control row template" + §"Pitfall 13: NIST 800-53 control IDs drift between Rev 4 and Rev 5" + §"Pitfall 15: Plan-checker doesn't catch missing 'compatible with' qualifier in tables"), tests/test_security_md_framing.sh (the lint that will enforce the discipline)</read_first>
  <action>
    Locate the line `<!-- TODO: Plan 02.7-03a fills this section (per-control table for AC, AU, IA, SC, SI, PT + LIMITED families; Met/Partial/Not Pursued/N/A vocabulary per Pitfall 15; Rev 5 IDs only per Pitfall 13). -->` in the §NIST SP 800-53 Rev 5 / Low-baseline Mapping section. Replace with the populated content below.

    The section structure: brief framing prose → applicability matrix (which families apply) → per-control table (>=15 rows) → out-of-scope rationale.

    ```markdown
    ### Framing

    PurpleVoice's NIST 800-53 mapping targets the **Rev 5 Low-baseline** (per SP 800-53B). Moderate and High baselines invoke continuous monitoring (SC-5(2)), incident response procedures (IR family), and audit-record retention (AU-11) — all organisational controls that don't apply to a single-binary local tool. Claiming Low-baseline alignment is honest engineering; claiming Moderate would over-reach.

    All control IDs reference NIST SP 800-53 Rev 5 (csrc.nist.gov/pubs/sp/800/53/r5/upd1/final). No Rev 4-only enhancements (e.g., AC-3a-c subdivisions or SC-12(2)) are used here.

    Status vocabulary:
    - **Met** — mitigation in place + verifiable (cross-referenced to a verification script or codebase reference).
    - **Partial** — mitigation exists with documented residual risk.
    - **Not Pursued** — engineering choice, with rationale (privacy > accountability for a personal tool).
    - **N/A** — does not apply, with rationale.

    ### Applicability Matrix (Family-level)

    NIST 800-53 Rev 5 has 20 control families. For a single-binary local-only personal tool, the following families apply; all others are **N/A by tool-class** (organisational concerns):

    | Family | Code | Applies? | Rationale |
    |---|---|---|---|
    | Access Control | AC | Yes | TCC permission model, hotkey scoping, who-can-invoke-PurpleVoice |
    | Audit and Accountability | AU | Limited | No logging by design (privacy > accountability); documented explicitly |
    | Awareness and Training | AT | N/A | Organisational; single-developer tool |
    | Assessment, Authorization, and Monitoring | CA | N/A | Organisational process |
    | Configuration Management | CM | Limited | Model file SHA256, idempotent setup.sh, config dotfiles — partial application |
    | Contingency Planning | CP | N/A | Organisational; PurpleVoice is stateless |
    | Identification and Authentication | IA | Yes | TCC binds permissions to bundle-id; explicit grant model |
    | Incident Response | IR | N/A | Organisational |
    | Maintenance | MA | N/A | Organisational |
    | Media Protection | MP | Limited | Ephemeral WAV cleanup is the closest analogue; partial |
    | Physical and Environmental Protection | PE | N/A | OS-level / facility-level |
    | Planning | PL | N/A | Organisational |
    | Program Management | PM | N/A | Organisational |
    | Personnel Security | PS | N/A | Organisational |
    | PII Processing and Transparency | PT | Yes | Tool processes voice (PII per most interpretations); transparency = this document |
    | Risk Assessment | RA | N/A | Organisational |
    | System and Services Acquisition | SA | Limited | SBOM (D-11) is the SA-equivalent for a tool |
    | System and Communications Protection | SC | Yes | Zero-egress claim; clipboard transient marker; process-tree silence — central to PurpleVoice |
    | System and Information Integrity | SI | Yes | Model SHA256, denylist filter, hallucination suppression |
    | Supply Chain Risk Management | SR | Limited | SBOM + brew + HuggingFace mirror = supply-chain surface |

    ### Per-Control Mapping (Yes / Limited families)

    | Control ID | Title (abbreviated) | Status | Rationale + Substantiation |
    |---|---|---|---|
    | **AC-3** | Access Enforcement | Met | TCC enforces Microphone + Accessibility on the Hammerspoon bundle-id (`org.hammerspoon.Hammerspoon`); hotkey requires user-scoped hardware access. Verified by macOS System Settings → Privacy & Security panel state. |
    | **AC-6** | Least Privilege | Met | `sox` + `whisper-cli` run as the user UID; no SUID bits; no privileged operations. Verified by `ls -l /opt/homebrew/bin/sox /opt/homebrew/bin/whisper-cli`. |
    | **AC-14** | Permitted Actions w/o Identification | Met | No anonymous usage path; user must hold a physical hotkey on their own keyboard; recording is bound to the macOS user session. |
    | **AU-2** | Event Logging | Not Pursued | By design. Privacy > accountability for a personal tool. Cross-reference STRIDE §Repudiation row in the [Threat Model](#threat-model). v1.x QOL-04 (10 MB rolling history log) is the opt-in mitigation if a user opts in. |
    | **AU-12** | Audit Record Generation | Not Pursued | Same rationale as AU-2. |
    | **IA-2** | User Identification & Authentication | Met (via OS) | macOS user session = identification; PurpleVoice inherits. No PurpleVoice-issued credentials. |
    | **IA-5** | Authenticator Management | N/A | PurpleVoice does not issue authenticators. |
    | **SC-7** | Boundary Protection | Met | Zero outbound network egress at runtime. Verified by `tests/security/verify_egress.sh` (3-layer evidence: lsof + nettop + pf+tcpdump). See [Egress Verification](#egress-verification) for methodology + Sequoia 15.7.5 caveat. |
    | **SC-8** | Transmission Confidentiality & Integrity | N/A | No transmission to protect — PurpleVoice is local-only at runtime. |
    | **SC-12** | Cryptographic Key Establishment & Management | N/A | No keys; PurpleVoice performs no cryptographic operations on user data. The only cryptographic primitive is SHA-256 for model file integrity (install-time) — see SI-7. |
    | **SC-28** | Protection of Information at Rest | Partial | WAV files are ephemeral (`EXIT` trap cleanup including SIGINT per ROB-04); no transcript persistence beyond opt-in v1.x history; user's `~/.config/purplevoice/` is at-rest-protected by macOS FileVault if the user has enabled it (organisation's responsibility). |
    | **SI-2** | Flaw Remediation | Partial | Brew-tracked dependencies update via `brew upgrade`; PurpleVoice itself is git-tracked and update-visible. No vulnerability disclosure programme yet — see [Vulnerability Disclosure](#vulnerability-disclosure). |
    | **SI-7** | Software, Firmware, and Information Integrity | Met | `setup.sh` Step 5 SHA256-verifies the Whisper model against the pinned constant `MODEL_SHA256`; mismatch aborts the install with non-zero exit. Silero VAD model is size-sanity-checked. |
    | **SI-10** | Information Input Validation | Met | `vocab.txt` cap (~150 words for `--prompt`); denylist exact-match filter (Phase 2 TRA-06); duration gate (Phase 2 TRA-05) drops <0.4s clips. |
    | **PT-3** | PII Processing Purposes | Met | Voice content processed solely for transcription; ephemeral; no telemetry — substantiated by this document + `tests/security/verify_egress.sh`. |
    | **PT-4** | Consent | Met | User-initiated hotkey; explicit TCC grants required (Microphone + Accessibility); no implicit consent path. |
    | **CM-7** | Least Functionality | Met | One-shot CLI per utterance; no persistent daemon; no extra features beyond the documented loop. |
    | **MP-6** | Media Sanitization | Partial | Ephemeral WAV cleanup via `EXIT` trap is the closest analogue. Full cryptographic erasure is out of scope (relies on macOS APFS journal handling). |
    | **SA-15** | Development Process, Standards, and Tools | Limited | SBOM (`SBOM.spdx.json`, SPDX 2.3, regenerated by `setup.sh` Step 8 with Syft) is the SA-equivalent. See [Software Bill of Materials](#software-bill-of-materials-sbom). |
    | **SR-3** | Supply Chain Controls and Processes | Limited | Model file SHA256-pinned; brew bottle SHA256s verified by Homebrew; HuggingFace mirror is the upstream source for the Whisper + Silero models; supply chain trust inherited from these upstreams. |

    ### Out of Scope (Family-level, with rationale)

    The following families are out-of-scope for a single-developer personal tool. Each is N/A by tool-class:

    - **AT** (Awareness and Training) — Organisational training programme; not a tool concern.
    - **CA** (Assessment, Authorization, and Monitoring) — Organisational process (continuous monitoring, ATO).
    - **CP** (Contingency Planning) — Organisational; PurpleVoice is stateless and per-utterance.
    - **IR** (Incident Response) — Organisational; PurpleVoice has no organisational incident-response programme.
    - **MA** (Maintenance) — Organisational; not relevant to a personal tool.
    - **PE** (Physical and Environmental Protection) — OS-level / facility-level.
    - **PL** (Planning) — Organisational planning concern.
    - **PM** (Program Management) — Organisational.
    - **PS** (Personnel Security) — Organisational.
    - **RA** (Risk Assessment) — Organisational.

    Marking these "Out of Scope" with rationale (rather than "Failed" or "Not Applicable") is the auditor-honest framing — these families contain controls that simply don't have a tool-level analogue.
    ```

    **Constraints:**
    - DO NOT use any control IDs not in NIST SP 800-53 Rev 5 (Pitfall 13). Specifically banned: `AC-3a`, `AC-3b`, `AC-3c` (Rev 4 sub-letter form), `SC-12(2)` (a Rev 4 enhancement number that doesn't exist in Rev 5 in this form).
    - DO NOT use status values other than "Met", "Partial", "Not Pursued", "N/A" (Pitfall 15) inside any table cell.
    - DO NOT use the words `compliant`, `certified`, `guarantees` anywhere (D-17).
    - The cross-references `#egress-verification`, `#software-bill-of-materials-sbom`, `#threat-model`, `#vulnerability-disclosure` use kebab-case anchor links matching the H2 section names.
    - The per-control table MUST have at least 15 rows of actual controls (AC, AU, IA, SC, SI, PT, CM, MP, SA, SR per the list above — minimum 15).
    - Update the SECURITY.md footer "*Phase 2.7 in progress. Last updated:*" to today's date.
    - DO NOT touch the 6 other framework sections (FIPS / FedRAMP / CC / HIPAA / SOC 2 / ISO 27001) — those are Plan 03b's territory; their TODO placeholders must remain intact.
  </action>
  <verify>
    <automated>
test -f SECURITY.md && \
grep -q "## NIST SP 800-53 Rev 5 / Low-baseline Mapping" SECURITY.md && \
grep -q "### Per-Control Mapping" SECURITY.md && \
grep -q "### Applicability Matrix" SECURITY.md && \
grep -q "AC-3.*Met" SECURITY.md && \
grep -q "AC-6.*Met" SECURITY.md && \
grep -q "AU-2.*Not Pursued" SECURITY.md && \
grep -q "SC-7.*Met" SECURITY.md && \
grep -q "SC-28.*Partial" SECURITY.md && \
grep -q "SI-7.*Met" SECURITY.md && \
grep -q "PT-3.*Met" SECURITY.md && \
grep -q "verify_egress.sh" SECURITY.md && \
[ "$(grep -cE '^\| \*\*(AC|AU|IA|SC|SI|PT|CM|MP|SA|SR)-' SECURITY.md)" -ge 15 ] && \
grep -q "Plan 02.7-03b fills" SECURITY.md && \
! grep -nE '\bcompliant\b' SECURITY.md && \
! grep -nE '\bcertified\b' SECURITY.md && \
! grep -nE '\bguarantees\b' SECURITY.md && \
! grep -E "AC-3[abc]\b" SECURITY.md && \
! grep -q "SC-12\(2\)" SECURITY.md && \
! grep -E "\| (Compliant|Certified) \|" SECURITY.md && \
bash tests/test_security_md_framing.sh && \
[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ]
    </automated>
  </verify>
  <acceptance_criteria>
    - SECURITY.md §NIST contains H3 subsections `### Framing`, `### Applicability Matrix`, `### Per-Control Mapping`, `### Out of Scope`.
    - Per-control table has >=15 rows whose ID matches `^| **(AC|AU|IA|SC|SI|PT|CM|MP|SA|SR)-` pattern.
    - Required individual control rows present (verified by grep): AC-3 Met, AC-6 Met, AU-2 Not Pursued, SC-7 Met, SC-28 Partial, SI-7 Met, PT-3 Met.
    - `verify_egress.sh` cross-reference present (SC-7 row links to the egress verification mechanism).
    - Zero `\bcompliant\b`, `\bcertified\b`, `\bguarantees\b` hits.
    - Zero Rev 4-only IDs: no `AC-3[abc]\b` (sub-letter form), no `SC-12(2)`.
    - Zero banned status cells: no `| Compliant |` or `| Certified |` table cells.
    - **Plan 03b TODO placeholders intact**: `Plan 02.7-03b fills` still appears in SECURITY.md (the 6 framed sections remain TODO-marked for Plan 03b to fill in Wave 3).
    - `bash tests/test_security_md_framing.sh` exits 0 (lint baseline preserved).
    - Pattern 2 invariant: `grep -c WHISPER_BIN purplevoice-record == 2`.
  </acceptance_criteria>
  <done>
    NIST 800-53 Rev 5 Low-baseline section authored with 20-row Applicability Matrix + >=15-row Per-Control Mapping + Out-of-Scope rationale; status vocabulary disciplined (Met / Partial / Not Pursued / N/A only); zero Rev 4-only IDs; framing lint GREEN. The 6 framed framework sections (FIPS / FedRAMP / CC / HIPAA / SOC 2 / ISO 27001) remain TODO-marked for Plan 03b in Wave 3.
  </done>
</task>

</tasks>

<verification>
**Plan 02.7-03a acceptance:**

1. SECURITY.md NIST table has >=15 per-control rows from AC, AU, IA, SC, SI, PT, CM, MP, SA, SR families.
2. Zero `\bcompliant\b`, `\bcertified\b`, `\bguarantees\b` hits across all of SECURITY.md.
3. Zero Rev 4-only IDs (no AC-3a/b/c, no SC-12(2)).
4. Zero `| Compliant |` or `| Certified |` table cells (Pitfall 15).
5. `bash tests/test_security_md_framing.sh` exits 0.
6. SECURITY.md still contains the 6 `Plan 02.7-03b fills` TODO placeholders for the 6 framed sections (Plan 03b's territory).
7. Pattern 2 invariant: `grep -c WHISPER_BIN purplevoice-record == 2`.
8. `bash tests/run_all.sh` GREEN (functional suite + framing lint).
9. Brand consistency intact: `bash tests/test_brand_consistency.sh` GREEN.
</verification>

<success_criteria>
- NIST section authored at deepest depth (>=15 control rows).
- "Compatible with" discipline maintained absolutely.
- Zero file changes outside SECURITY.md.
- Wave 3 (Plan 03b) ready to fill 6 framed framework sections.
</success_criteria>

<output>
After completion, create `.planning/phases/02.7-security-posture/02.7-03a-SUMMARY.md` documenting:
- 1 file modified (SECURITY.md) with line-count delta (before vs after)
- NIST per-control row count (>=15)
- Lint status (test_security_md_framing.sh GREEN)
- D-17 framing audit summary (zero violations)
- Pitfall 13 audit summary (zero Rev 4 IDs)
- Pitfall 15 audit summary (zero banned status cells)
- Pattern 2 invariant confirmation
- Brand consistency confirmation
- Wave 3 readiness signal: 6 `Plan 02.7-03b fills` placeholders remain in SECURITY.md for Plan 03b to fill
</output>
</content>
</invoke>