---
phase: 02.5-branding
status: passed
date: 2026-04-29
verifier: orchestrator (inline; gsd-verifier agent unavailable due to credit-limit constraint)
---

# Phase 02.5 Verification — Branding

## Phase Goal (from ROADMAP)

Apply the public-facing product name `PurpleVoice` consistently across all user-visible surfaces (replacing the working name "voice-cc"). Stage the brand assets (icon + lavender menubar) so Phase 3.5 HUD and Phase 3 Distribution inherit a stable identity.

## Verdict

**PASSED** — 23 of 23 predicates verified.

All 18 VALIDATION.md predicates plus 5 derivative checks pass on the post-execution codebase. The phase achieved its goal: PurpleVoice is the canonical brand across all user-visible surfaces, the broadened privacy-first positioning (6 audience segments) lands in PROJECT.md, the visual identity is in place (lavender `#B388EB` menubar + 256×256 PNG icon with white lips on lavender), and the regression-catch test (`tests/test_brand_consistency.sh`) is wired into the suite to prevent future drift.

## Predicate Results

```
✓ BRD-01: PurpleVoice in PROJECT.md
✓ BRD-01: privacy paragraph present
✓ BRD-01: broadened audience (institutional terms)
✓ BRD-01: Phase 2.7 SECURITY.md forward-reference
✓ BRD-01: tagline in PROJECT.md
✓ BRD-01: no over-claims (FIPS-validated / formally certified / certified compliant)
✓ BRD-02: zero voice-cc strings in non-exempt sources (test_brand_consistency.sh)
✓ BRD-02: 5+ PurpleVoice notification titles in init.lua
✓ BRD-02: 'PurpleVoice loaded' alert
✓ BRD-02: purplevoice-record exists and is executable
✓ BRD-02: purplevoice-lua/init.lua exists
✓ BRD-02: .hammerspoon-init-snippet.lua shows require("purplevoice")
✓ BRD-02: setup.sh has 3 migrate_xdg_dir calls
✓ BRD-02: setup.sh banner contains tagline
✓ BRD-02: README.md header contains tagline
✓ BRD-02: Pattern 2 boundary preserved (grep -c WHISPER_BIN purplevoice-record == 2; no whisper-cli in init.lua)
✓ BRD-02: All 7 unit tests GREEN (run_all.sh exits 0; the 7th is the new test_brand_consistency.sh)
✓ BRD-02: hs.notify orphan-tag cleanup present (old voiceccOpen* tags + hs.notify.unregister calls)
✓ BRD-02: BRD-01..03 traceability rows in REQUIREMENTS.md (exactly 3)
✓ BRD-03: assets/icon-256.png exists
✓ BRD-03: icon is 256×256 (sips reports both pixelWidth and pixelHeight = 256)
✓ BRD-03: assets/icon.svg source exists for reproducibility
✓ BRD-03: menubar uses lavender #B388EB

Results: 23 passed, 0 failed
```

## Requirement Coverage

| Requirement | Plan(s) | Closure |
|-------------|---------|---------|
| BRD-01 | 02.5-04 | PROJECT.md "Name & Positioning" section + REQUIREMENTS.md formalisation. Six audience segments enumerated (privacy-conscious individuals + government/defence + healthcare/legal + finance/compliance + journalists + air-gapped). Phase 2.7 SECURITY.md forward-referenced as substantiation document. Auditable/verifiable wording, no over-claims. |
| BRD-02 | 02.5-01, 02.5-02, 02.5-04 | All user-visible strings rebranded: 5 hs.notify titles + module load alert + 2 stderr error messages + setup.sh banner + tagline in 4 placement sites (README, setup.sh, HS load alert, future Phase 3.5 HUD via M.BRAND constant). Working name `voice-cc` survives ONLY in approved exemptions (.planning/, .git/, repo path, setup.sh migration FROM-args, CLAUDE.md auto-managed block deferred to Phase 3). Regression-caught by `tests/test_brand_consistency.sh`. |
| BRD-03 | 02.5-03, 02.5-04 | Lavender `#B388EB` menubar via BRAND constant + `hs.styledtext` (idle U+25CB outline / recording U+25CF filled glyph differentiation). 256×256 PNG icon at `assets/icon-256.png` with lavender background + white lips silhouette, derived deterministically from `assets/icon.svg` via `sips`. Icon referenced from README.md header. |

All 3 phase requirements closed.

## Phase 2.5 Success Criteria (from ROADMAP)

1. **Name + rationale in PROJECT.md** — ✅ PROJECT.md "Name & Positioning" section
2. **All user-visible strings rebranded** — ✅ verified by `tests/test_brand_consistency.sh`
3. **Phase 3 inherits the brand without rework** — ✅ M.BRAND constants exported for downstream phases; Phase 2.7 SECURITY.md and Phase 3 Distribution both forward-referenced
4. **Optional: minimal icon** — ✅ pursued in this phase (was not punted)

## Pattern 2 Invariant (Cross-Phase)

`grep -c WHISPER_BIN purplevoice-record == 2` ✅
`! grep -q "whisper-cli" purplevoice-lua/init.lua` ✅

The Phase 5 v1.1 warm-process upgrade remains a one-function-body swap.

## Live Migration Verification

The Wave 2 setup.sh migration ran live on the host machine during Plan 02.5-02 execution:
- ~466 MB Whisper model + Silero VAD weights moved atomically via APFS rename (`mv`, not `rsync`)
- Both stale symlinks (`~/.local/bin/voice-cc-record`, `~/.hammerspoon/voice-cc`) removed
- Both new symlinks (`~/.local/bin/purplevoice-record`, `~/.hammerspoon/purplevoice`) resolve correctly
- Second `bash setup.sh` run: zero `Migrating`/`Removed stale` log lines (idempotent only-new branch confirmed)

## Deferred Items (Phase 3 follow-up, logged in STATE.md Open TODOs)

- **`.planning/research/STACK.md` rebrand + CLAUDE.md auto-managed block regeneration** — the GSD-managed `<!-- GSD:stack-start -->` block in CLAUDE.md still contains `voice-cc` references. Editing it directly would silently revert on next regeneration; the source-of-truth STACK.md needs updating instead. Logged in STATE.md Open TODOs for Phase 3 (where Distribution will rebuild user-facing docs anyway).

## Test Suite Final State

```
[test] test_brand_consistency.sh                PASS  (NEW — Phase 2.5)
[test] test_denylist.sh                         PASS
[test] test_duration_gate.sh                    PASS
[test] test_sigint_cleanup.sh                   PASS
[test] test_tcc_grep.sh                         PASS
[test] test_vad_silence.sh                      PASS
[test] test_wav_cleanup.sh                      PASS
Results: 7 passed, 0 failed
```

## Sign-Off

Phase 2.5 (Branding) is verifiably complete. Ready for `/gsd:discuss-phase 2.7` (Security Posture & Government Readiness) per the new execution order: 1 → 2 → 2.5 → **2.7** → 3.5 → 4 → 3 → 5 (conditional).
