---
phase: 02.5-branding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - voice-cc-record
  - voice-cc-lua/init.lua
  - .hammerspoon-init-snippet.lua
  - tests/test_duration_gate.sh
  - tests/test_denylist.sh
  - tests/test_wav_cleanup.sh
  - tests/test_sigint_cleanup.sh
  - tests/test_tcc_grep.sh
  - tests/test_vad_silence.sh
  - tests/lib/sample_audio.sh
  - tests/run_all.sh
  - tests/manual/test_clipboard_restore.md
  - tests/manual/test_transient_marker.md
  - tests/manual/test_menubar.md
  - tests/manual/test_audio_cues.md
  - tests/manual/test_tcc_notification.md
  - tests/manual/test_reentrancy.md
  - tests/manual/test_accessibility_prompt.md
  - config/denylist.txt
autonomous: true
requirements:
  - BRD-02
must_haves:
  truths:
    - "Bash glue lives at purplevoice-record (executable, exists)"
    - "Lua module lives at purplevoice-lua/init.lua and loads cleanly under require('purplevoice')"
    - "All 5 hs.notify titles use the PurpleVoice: prefix"
    - "Module load alert says 'PurpleVoice loaded — local dictation, cmd+shift+e'"
    - "User-paste snippet shows require('purplevoice')"
    - "Old hs.notify tags voiceccOpenMicSettings / voiceccOpenAccessibilitySettings are unregistered at module top so stale notifications don't crash console"
    - "Pattern 2 boundary preserved on the renamed file (grep -c WHISPER_BIN purplevoice-record == 2)"
    - "All 6 existing bash unit tests still GREEN after rebrand (paths and env-var names updated together)"
  artifacts:
    - path: "purplevoice-record"
      provides: "Phase 2 hardened bash glue, renamed from voice-cc-record"
      contains: "transcribe()"
    - path: "purplevoice-lua/init.lua"
      provides: "Phase 2 hardened Hammerspoon module, renamed from voice-cc-lua/"
      contains: "PurpleVoice loaded"
    - path: ".hammerspoon-init-snippet.lua"
      provides: "Updated user-paste snippet referencing the new module name"
      contains: 'require("hs.ipc")'
  key_links:
    - from: "purplevoice-record"
      to: "$HOME/.local/share/purplevoice/models/ggml-small.en.bin"
      via: "MODEL env var default with PURPLEVOICE_MODEL override"
      pattern: 'PURPLEVOICE_MODEL'
    - from: "purplevoice-lua/init.lua"
      to: "purplevoice-record"
      via: "SCRIPT_PATH constant points at $HOME/.local/bin/purplevoice-record"
      pattern: '/.local/bin/purplevoice-record'
    - from: "purplevoice-lua/init.lua"
      to: "old hs.notify tag namespace"
      via: "pcall(hs.notify.unregister, ...) cleanup at module top"
      pattern: "voiceccOpenMicSettings"
---

<objective>
Propagate the PurpleVoice brand through every code surface that ships strings to the user without changing the runtime XDG paths, the migration block, the README, or any visual identity (BRD-03 lives in Plan 03; XDG migration lives in Plan 02; README + REQUIREMENTS + PROJECT.md edits live in Plan 04).

Purpose: rename the bash glue file, the Lua module directory, the env vars, the notification titles, the load alert, the orphan-tag cleanup callbacks, the user-paste snippet, the test path strings, and the test env-var hook — atomically and consistently — so Plans 02/03/04 can build on a fully-renamed code surface. Per D-01/D-03/D-04/D-06/D-08 in CONTEXT.md.

Output: `purplevoice-record` (was `voice-cc-record`), `purplevoice-lua/init.lua` (was `voice-cc-lua/init.lua`), `.hammerspoon-init-snippet.lua` updated to reference the new module name, all bash tests + manual walkthroughs updated to reference the new file/path/env-var names, no `voice-cc` strings surviving in any of these files. Pattern 2 invariant preserved (grep -c WHISPER_BIN purplevoice-record == 2). All 6 bash unit tests still GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-branding/02.5-CONTEXT.md
@.planning/phases/02.5-branding/02.5-RESEARCH.md
@.planning/phases/02.5-branding/02.5-VALIDATION.md
@voice-cc-record
@voice-cc-lua/init.lua
@.hammerspoon-init-snippet.lua
@tests/run_all.sh

<interfaces>
<!-- Surfaces and contracts the executor must preserve verbatim through the rebrand. -->
<!-- These are derived from the existing files — DO NOT alter them, only rename strings. -->

Pattern 2 boundary (voice-cc-record → purplevoice-record):
- WHISPER_BIN appears on EXACTLY 2 lines: line 29 (assignment with folded `[ -x ]` check) and line 79 (invocation inside transcribe()).
- After rebrand: `grep -c WHISPER_BIN purplevoice-record` MUST equal `2`.
- The variable name WHISPER_BIN itself is NOT rebranded — it is whisper-related, not brand-related.

Exit codes (control protocol with init.lua — DO NOT touch):
- 0 = success, paste transcript
- 2 = duration gate (silent abort)
- 3 = empty / denylist (silent abort)
- 10 = TCC mic denied
- 11 = binary or model missing
- 12 = generic sox/whisper-cli failure

Five notification tags + titles (init.lua → renamed):
- Tag voiceccOpenMicSettings        → purplevoiceOpenMicSettings  (title "PurpleVoice: microphone blocked")
- Tag voiceccOpenAccessibilitySettings → purplevoiceOpenAccessibilitySettings (title "PurpleVoice: accessibility required")
- Untagged title "voice-cc: install incomplete"     → "PurpleVoice: install incomplete"
- Untagged title "voice-cc: transcription failed"   → "PurpleVoice: transcription failed"
- Untagged title "voice-cc: unexpected exit N"      → "PurpleVoice: unexpected exit N"

Env-var renames (per RESEARCH.md "State of the Art" + decision Q1 → hard rename, no fallback):
- VOICE_CC_MODEL          → PURPLEVOICE_MODEL
- VOICE_CC_SILERO_MODEL   → PURPLEVOICE_SILERO_MODEL
- VOICE_CC_VAD_THRESHOLD  → PURPLEVOICE_VAD_THRESHOLD
- VOICE_CC_NO_SOUNDS      → PURPLEVOICE_NO_SOUNDS
- VOICE_CC_TEST_SKIP_SOX  → PURPLEVOICE_TEST_SKIP_SOX
- VOICE_CC_INTEGRATION    → PURPLEVOICE_INTEGRATION

Per RESEARCH.md "Pattern 2: hs.notify orphaned-tag cleanup": the new init.lua MUST call
  `pcall(hs.notify.unregister, "voiceccOpenMicSettings")`
  `pcall(hs.notify.unregister, "voiceccOpenAccessibilitySettings")`
at the very top, BEFORE the new register calls. The OLD tag strings MUST appear (in the unregister calls) — verification grep depends on it.

Tagline (canonical, used by alert in this plan and elsewhere):
"Local voice dictation. Nothing leaves your Mac."
For the inline alert form factor (D-12), use:
"PurpleVoice loaded — local dictation, cmd+shift+e"
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rename file + directory and rewrite the bash glue strings</name>
  <files>purplevoice-record (was voice-cc-record), tests/test_duration_gate.sh, tests/test_denylist.sh, tests/test_wav_cleanup.sh, tests/test_sigint_cleanup.sh, tests/test_tcc_grep.sh, tests/test_vad_silence.sh, tests/lib/sample_audio.sh, tests/run_all.sh, config/denylist.txt</files>
  <read_first>
    - voice-cc-record (the file you are about to rename + edit; you must see exit codes, Pattern 2 boundary, EXIT trap, sox amplitude detection, set -euo pipefail strict mode — ALL must survive verbatim except for the brand strings and env-var names)
    - tests/run_all.sh (the suite runner — must continue to find tests by glob)
    - tests/test_duration_gate.sh (uses VOICE_CC_INTEGRATION + VOICE_CC_TEST_SKIP_SOX hooks)
    - tests/test_denylist.sh (references ~/.config/voice-cc/denylist.txt path)
    - tests/test_wav_cleanup.sh (references VOICE_CC_TEST_SKIP_SOX, /tmp/voice-cc/, ./voice-cc-record)
    - tests/test_sigint_cleanup.sh (references VOICE_CC_TEST_SKIP_SOX, /tmp/voice-cc/, ./voice-cc-record)
    - tests/test_tcc_grep.sh (references mktemp -t voice-cc-test-stderr-XXXXXX)
    - tests/test_vad_silence.sh (references ~/.local/share/voice-cc/models/* paths and mktemp prefix)
    - tests/lib/sample_audio.sh (comments reference voice-cc-record by name)
    - .planning/phases/02.5-branding/02.5-CONTEXT.md (load-bearing decisions D-01, D-04, D-05; runtime paths in D-05 will land in Plan 02; THIS plan only edits string surfaces, not the migration block)
    - .planning/phases/02.5-branding/02.5-RESEARCH.md "Pattern 1: Per-File Targeted Rebrand" + "Pitfall 3: Pattern 2 boundary lost during edits"
    - .planning/phases/02.5-branding/02.5-VALIDATION.md (predicate map for BRD-02)
    - config/denylist.txt (1 voice-cc reference in the comment header)
  </read_first>
  <action>
Step 1 — file rename (do this FIRST so subsequent edits land on the new file):
```bash
git mv voice-cc-record purplevoice-record
chmod +x purplevoice-record
```

Step 2 — rewrite `purplevoice-record` strings IN PLACE. Apply these EXACT replacements:

  Line 2 (header comment):
    OLD: `# voice-cc-record — Phase 2 hardened bash glue`
    NEW: `# purplevoice-record — Phase 2 hardened bash glue (PurpleVoice)`

  Lines 16 (header comment about test hook):
    OLD: `#   - VOICE_CC_TEST_SKIP_SOX env var hook for tests/test_*.sh`
    NEW: `#   - PURPLEVOICE_TEST_SKIP_SOX env var hook for tests/test_*.sh`

  Line 29 (Pattern 2 line — keep the structure exactly: assignment + folded `[ -x ]` check + exit 11 + stderr message all on the same line):
    OLD: `WHISPER_BIN="${WHISPER_BIN:-/opt/homebrew/bin/whisper-cli}"; [ -x "$WHISPER_BIN" ] || { echo "voice-cc: whisper-cli missing — run setup.sh" >&2; exit 11; }`
    NEW: `WHISPER_BIN="${WHISPER_BIN:-/opt/homebrew/bin/whisper-cli}"; [ -x "$WHISPER_BIN" ] || { echo "PurpleVoice: whisper-cli missing — run setup.sh" >&2; exit 11; }`

  Line 32:
    OLD: `MODEL="${VOICE_CC_MODEL:-$HOME/.local/share/voice-cc/models/ggml-small.en.bin}"`
    NEW: `MODEL="${PURPLEVOICE_MODEL:-$HOME/.local/share/voice-cc/models/ggml-small.en.bin}"`
    (NOTE: keep the path as `voice-cc/models/...` — Plan 02 owns the XDG path migration. Only the env-var name changes here.)

  Line 33:
    OLD: `SILERO_MODEL="${VOICE_CC_SILERO_MODEL:-$HOME/.local/share/voice-cc/models/ggml-silero-v6.2.0.bin}"`
    NEW: `SILERO_MODEL="${PURPLEVOICE_SILERO_MODEL:-$HOME/.local/share/voice-cc/models/ggml-silero-v6.2.0.bin}"`

  Line 36:
    OLD: `VAD_THRESHOLD="${VOICE_CC_VAD_THRESHOLD:-0.50}"`
    NEW: `VAD_THRESHOLD="${PURPLEVOICE_VAD_THRESHOLD:-0.50}"`

  Line 57 (stderr message inside model-missing check):
    OLD: `  echo "voice-cc: Whisper model missing at $MODEL — run setup.sh" >&2`
    NEW: `  echo "PurpleVoice: Whisper model missing at $MODEL — run setup.sh" >&2`

  Line 61 (stderr message inside Silero-missing check):
    OLD: `  echo "voice-cc: Silero VAD weights missing at $SILERO_MODEL — run setup.sh" >&2`
    NEW: `  echo "PurpleVoice: Silero VAD weights missing at $SILERO_MODEL — run setup.sh" >&2`

  Line 93 (test-hook comment):
    OLD: `# VOICE_CC_TEST_SKIP_SOX=1 → skip capture (tests pre-stage the WAV).`
    NEW: `# PURPLEVOICE_TEST_SKIP_SOX=1 → skip capture (tests pre-stage the WAV).`

  Line 94 (the env-var read itself):
    OLD: `if [ "${VOICE_CC_TEST_SKIP_SOX:-0}" != "1" ]; then`
    NEW: `if [ "${PURPLEVOICE_TEST_SKIP_SOX:-0}" != "1" ]; then`

  DO NOT change WAV paths (`/tmp/voice-cc`, `~/.config/voice-cc/...`, `~/.local/share/voice-cc/models/...`). Plan 02 owns those — they MUST stay verbatim in this plan so Plan 02's migration block has the literal old strings to migrate FROM.

  DO NOT touch any other line. Strict mode (`set -euo pipefail`), the EXIT trap, the SOX_SIGNALED flag, the Sequoia silent-stream amplitude detection, the duration gate awk float-compare, the denylist canonicalisation, the empty-drop, and the printf at line 176 must all be byte-identical.

Step 3 — directory rename:
```bash
git mv voice-cc-lua purplevoice-lua
```
(The init.lua content edits land in Task 2 below.)

Step 4 — rebrand the test files. Apply these EXACT replacements:

  tests/test_duration_gate.sh:
    line 9:  `# must be used by Plan 02-01 inside voice-cc-record.` → `# must be used by Plan 02-01 inside purplevoice-record.`
    line 11: `# Integration assertion (voice-cc-record itself exits 2 when fed a < 0.4s WAV)` → `# Integration assertion (purplevoice-record itself exits 2 when fed a < 0.4s WAV)`
    line 12: `# is gated behind VOICE_CC_INTEGRATION=1 because it requires Plan 02-01's` → `# is gated behind PURPLEVOICE_INTEGRATION=1 because it requires Plan 02-01's`
    line 13: `# VOICE_CC_TEST_SKIP_SOX hook (not implemented yet — RED until then).` → `# PURPLEVOICE_TEST_SKIP_SOX hook (not implemented yet — RED until then).`
    line 20: `WAV=$(mktemp -t voice-cc-test-XXXXXX.wav)` → `WAV=$(mktemp -t purplevoice-test-XXXXXX.wav)`
    line 36: `# Plan 02-01 must add VOICE_CC_TEST_SKIP_SOX support to voice-cc-record so` → `# Plan 02-01 must add PURPLEVOICE_TEST_SKIP_SOX support to purplevoice-record so`
    line 39: `if [ "${VOICE_CC_INTEGRATION:-0}" = "1" ]; then` → `if [ "${PURPLEVOICE_INTEGRATION:-0}" = "1" ]; then`
    line 41: `  short_tap_wav /tmp/voice-cc/recording.wav` → KEEP path as `/tmp/voice-cc/recording.wav` (Plan 02 owns runtime paths; the test is exercising the bash glue's actual path)
    line 42: `  if VOICE_CC_TEST_SKIP_SOX=1 ./voice-cc-record >/dev/null 2>&1; then` → `  if PURPLEVOICE_TEST_SKIP_SOX=1 ./purplevoice-record >/dev/null 2>&1; then`
    line 48: `    echo "PASS: voice-cc-record exited 2 ...` → `    echo "PASS: purplevoice-record exited 2 ...`
    line 50: `    echo "FAIL: voice-cc-record exited $EXIT_CODE ...` → `    echo "FAIL: purplevoice-record exited $EXIT_CODE ...`
    line 40: `  mkdir -p /tmp/voice-cc` → keep as `/tmp/voice-cc` (runtime path — Plan 02 owns it).

  tests/test_denylist.sh:
    line 10: `# The integration assertion (voice-cc-record exits 3 when fed a denylist` → `# The integration assertion (purplevoice-record exits 3 when fed a denylist`
    line 12: `# behind VOICE_CC_INTEGRATION=1. ...` → `# behind PURPLEVOICE_INTEGRATION=1. ...`
    line 15: `DENYLIST="$HOME/.config/voice-cc/denylist.txt"` → KEEP as `~/.config/voice-cc/denylist.txt` (Plan 02 owns the path migration).

  tests/test_wav_cleanup.sh:
    line 4:  `# Asserts that after a voice-cc-record invocation ...` → `# Asserts that after a purplevoice-record invocation ...`
    line 5:  `# /tmp/voice-cc/ directory ...` → KEEP `/tmp/voice-cc/` (runtime path — Plan 02 owns).
    line 8:  `# RED until Plan 02-01 adds the VOICE_CC_TEST_SKIP_SOX hook AND the EXIT` → `# RED until Plan 02-01 adds the PURPLEVOICE_TEST_SKIP_SOX hook AND the EXIT`
    line 15: `if ! grep -q 'VOICE_CC_TEST_SKIP_SOX' voice-cc-record 2>/dev/null; then` → `if ! grep -q 'PURPLEVOICE_TEST_SKIP_SOX' purplevoice-record 2>/dev/null; then`
    line 16: `  echo "FAIL: voice-cc-record does not support VOICE_CC_TEST_SKIP_SOX hook (Plan 02-01 not yet implemented — expected RED)"` → `  echo "FAIL: purplevoice-record does not support PURPLEVOICE_TEST_SKIP_SOX hook (Plan 02-01 not yet implemented — expected RED)"`
    line 21: `mkdir -p /tmp/voice-cc` → KEEP path
    line 22: `rm -f /tmp/voice-cc/* 2>/dev/null` → KEEP path
    line 27: `silence_wav /tmp/voice-cc/recording.wav 0.1` → KEEP path
    line 29: `# Invoke voice-cc-record ...` → `# Invoke purplevoice-record ...`
    line 30: `VOICE_CC_TEST_SKIP_SOX=1 ./voice-cc-record >/dev/null 2>&1 || true` → `PURPLEVOICE_TEST_SKIP_SOX=1 ./purplevoice-record >/dev/null 2>&1 || true`
    line 32: `# Now assert /tmp/voice-cc/ is empty ...` → KEEP path
    line 34: `LEFTOVERS=$(find /tmp/voice-cc -maxdepth 1 ...` → KEEP path
    line 36: `  echo "PASS: /tmp/voice-cc/ contains no ...` → KEEP path
    line 39: `  echo "FAIL: /tmp/voice-cc/ has leftovers ...` → KEEP path

  tests/test_sigint_cleanup.sh:
    line 4:  `# Verifies the EXIT trap Plan 02-01 must add cleans up /tmp/voice-cc/ even` → KEEP path
    line 8:  `# backgrounding voice-cc-record fails — the duration gate fires immediately` → `# backgrounding purplevoice-record fails — the duration gate fires immediately`
    line 17: `if ! grep -q 'VOICE_CC_TEST_SKIP_SOX' voice-cc-record 2>/dev/null; then` → `if ! grep -q 'PURPLEVOICE_TEST_SKIP_SOX' purplevoice-record 2>/dev/null; then`
    line 18: `  echo "FAIL: voice-cc-record does not support VOICE_CC_TEST_SKIP_SOX hook ...` → `  echo "FAIL: purplevoice-record does not support PURPLEVOICE_TEST_SKIP_SOX hook ...`
    line 23: `mkdir -p /tmp/voice-cc` → KEEP path
    line 24: `rm -f /tmp/voice-cc/* 2>/dev/null` → KEEP path
    line 27: `# gate in voice-cc-record. Without this, the gate fires immediately and the` → `# gate in purplevoice-record. Without this, the gate fires immediately and the`
    line 29: `medium_wav /tmp/voice-cc/recording.wav   # 1.0s tone @ 440Hz` → KEEP path
    line 31: `# Background voice-cc-record (it will pass the gate and proceed into` → `# Background purplevoice-record (it will pass the gate and proceed into`
    line 33: `VOICE_CC_TEST_SKIP_SOX=1 ./voice-cc-record >/dev/null 2>&1 &` → `PURPLEVOICE_TEST_SKIP_SOX=1 ./purplevoice-record >/dev/null 2>&1 &`
    line 40: `LEFTOVERS=$(find /tmp/voice-cc -maxdepth 1 ...` → KEEP path
    line 42: `  echo "PASS: /tmp/voice-cc/ empty after SIGINT (EXIT trap fired correctly)"` → KEEP path
    line 45: `  echo "FAIL: /tmp/voice-cc/ has leftovers after SIGINT: $LEFTOVERS"` → KEEP path

  tests/test_tcc_grep.sh:
    line 13: `FAKE_STDERR=$(mktemp -t voice-cc-test-stderr-XXXXXX)` → `FAKE_STDERR=$(mktemp -t purplevoice-test-stderr-XXXXXX)`

  tests/test_vad_silence.sh:
    line 16: `MODEL="${MODEL:-$HOME/.local/share/voice-cc/models/ggml-small.en.bin}"` → KEEP path (Plan 02 migrates)
    line 17: `SILERO="${SILERO_MODEL:-$HOME/.local/share/voice-cc/models/ggml-silero-v6.2.0.bin}"` → KEEP path
    line 28: `WAV=$(mktemp -t voice-cc-test-vad-XXXXXX.wav)` → `WAV=$(mktemp -t purplevoice-test-vad-XXXXXX.wav)`

  tests/run_all.sh:
    line 21: `if bash "$f" >/tmp/voice-cc-test.log 2>&1; then` → `if bash "$f" >/tmp/purplevoice-test.log 2>&1; then`

  tests/lib/sample_audio.sh:
    line 6:  `# wants, identical to what voice-cc-record's sox capture produces).` → `# wants, identical to what purplevoice-record's sox capture produces).`
    line 39: `# in voice-cc-record. Used by tests that need to drive the script PAST the` → `# in purplevoice-record. Used by tests that need to drive the script PAST the`

  config/denylist.txt:
    line 1: `# ~/.config/voice-cc/denylist.txt — Whisper hallucination denylist` → KEEP path (Plan 02 migrates)
    line 7: `# To pin a custom version, chmod -w ~/.config/voice-cc/denylist.txt after install.` → KEEP path

  Why some `voice-cc` strings are intentionally preserved here:
   - Runtime paths (`/tmp/voice-cc/`, `~/.config/voice-cc/...`, `~/.local/share/voice-cc/...`) belong to Plan 02 (XDG migration). Migrating them here would split the migration logic across two plans and break the migrate-block-as-source-of-truth invariant.
   - The Pattern 2 boundary in `purplevoice-record` REQUIRES the WHISPER_BIN line to stay one line. Verify after edit: `grep -c WHISPER_BIN purplevoice-record` returns 2.

Step 5 — rebrand the manual walkthrough docs. For each file in tests/manual/, replace literal `voice-cc` with the appropriate brand string per these rules:
  - `voice-cc-record` → `purplevoice-record`
  - `voice-cc-lua/init.lua` → `purplevoice-lua/init.lua`
  - `voice-cc loaded` → `PurpleVoice loaded`
  - `voice-cc: microphone blocked` → `PurpleVoice: microphone blocked`
  - `voice-cc: accessibility required` → `PurpleVoice: accessibility required`
  - `voice-cc module loaded` → `PurpleVoice module loaded`
  - `voice-cc loop` (deployed/running) → `PurpleVoice loop`
  - `voice-cc indicator` → `PurpleVoice indicator`
  - `voice-cc paste` → `PurpleVoice paste`
  - `voice-cc currently works` → `PurpleVoice currently works`
  - `VOICE_CC_NO_SOUNDS` env-var refs → `PURPLEVOICE_NO_SOUNDS`
  - In `tests/manual/test_audio_cues.md` line 27 the false-positive condition mentions `os.getenv("VOICE_CC_NO_SOUNDS")` — change this to `os.getenv("PURPLEVOICE_NO_SOUNDS")` in the prose.
  - KEEP `/tmp/voice-cc/` runtime paths (cross-reference Plan 02). KEEP any reference to `voice-cc` that names a Phase 1/2 plan or SUMMARY (none present).

Each manual walkthrough file is short (~30-50 lines). Read each file, apply the targeted substring replacements above, write back.

Step 6 — verify counts immediately after editing each file:
- `grep -c WHISPER_BIN purplevoice-record` MUST equal 2 (Pattern 2 invariant).
- `! grep -q whisper-cli purplevoice-lua/init.lua` (Lua module never references whisper-cli).
- `grep -c voice-cc tests/manual/*.md` should return only the runtime-path occurrences (Plan 02 will close those).
- `grep -rl "VOICE_CC_" --include="*.sh" tests/` should return ZERO matches (all env vars renamed in tests).
- `grep -rl "VOICE_CC_" --include="*.sh" .` should return ZERO matches outside tests/manual (which are .md, not .sh).

Per CONTEXT.md D-07 + RESEARCH.md "Approved exception list", `voice-cc` strings in `.planning/` are PRESERVED — never edit any file under `.planning/`.

  </action>
  <verify>
    <automated>[ -x purplevoice-record ] && [ -f purplevoice-lua/init.lua ] && [ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ] && grep -q "PurpleVoice: whisper-cli missing" purplevoice-record &amp;&amp; grep -q "PurpleVoice: Whisper model missing" purplevoice-record &amp;&amp; grep -q "PurpleVoice: Silero VAD weights missing" purplevoice-record &amp;&amp; grep -q "PURPLEVOICE_TEST_SKIP_SOX" purplevoice-record &amp;&amp; grep -q "PURPLEVOICE_MODEL" purplevoice-record &amp;&amp; ! grep -q "VOICE_CC_" purplevoice-record &amp;&amp; ! grep -rln "voice-cc-record\|VOICE_CC_" --include="*.sh" tests/ &amp;&amp; bash tests/run_all.sh</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `[ -x purplevoice-record ]` (file exists, executable)
    - [ ] `[ -f purplevoice-lua/init.lua ]` (directory rename succeeded)
    - [ ] `[ ! -e voice-cc-record ]` (old file gone)
    - [ ] `[ ! -d voice-cc-lua ]` (old directory gone)
    - [ ] `[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ]` (Pattern 2 invariant intact)
    - [ ] `! grep -q "VOICE_CC_" purplevoice-record` (env vars renamed)
    - [ ] `grep -q "PurpleVoice: whisper-cli missing" purplevoice-record` (stderr msg rebranded)
    - [ ] `grep -q "PURPLEVOICE_TEST_SKIP_SOX" purplevoice-record` (test hook renamed)
    - [ ] `! grep -rln "VOICE_CC_" --include="*.sh" tests/` (test env-var refs renamed)
    - [ ] `! grep -rln "voice-cc-record" --include="*.sh" tests/` (test references to old binary name renamed)
    - [ ] `bash tests/run_all.sh` exits 0 (all 6 unit tests still pass — confirms env-var rename is internally consistent + paths preserved)
  </acceptance_criteria>
  <done>
The bash glue file lives at `purplevoice-record`, the Lua module lives at `purplevoice-lua/`, all 5 user-facing env vars use the `PURPLEVOICE_*` prefix in both the script and the tests, the Pattern 2 boundary is intact (grep -c WHISPER_BIN == 2), all stderr error strings use `PurpleVoice:` prefix, and `bash tests/run_all.sh` reports 6 PASS / 0 FAIL.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite Lua module strings + add hs.notify orphan-tag cleanup</name>
  <files>purplevoice-lua/init.lua, .hammerspoon-init-snippet.lua</files>
  <read_first>
    - purplevoice-lua/init.lua (post-Task-1 file location; you must see all 14 voice-cc references — header comment lines 1-2, SCRIPT_PATH line 38, idle/recording menubar colours lines 39-40, audio-cues env-var line 73, hs.notify register tags lines 112+116, all 5 notification titles in handleExit, the load alert line 313, and the unknown-exit log message line 227)
    - .hammerspoon-init-snippet.lua (3 voice-cc references — comment header line 2, the require line context line 4, the source attribution line 18)
    - .planning/phases/02.5-branding/02.5-CONTEXT.md (D-06: ALL hs.notify titles get `PurpleVoice:` prefix; D-08: snippet shows `require("purplevoice")` plus brand-explanation comment; D-12: load alert form `PurpleVoice loaded — local dictation, cmd+shift+e`)
    - .planning/phases/02.5-branding/02.5-RESEARCH.md "Pattern 2: hs.notify orphaned-tag cleanup" + "Pitfall 4" (the `pcall(hs.notify.unregister, ...)` cleanup at module top is REQUIRED for VALIDATION.md predicate — old tag string must appear in the file)
    - .planning/phases/02.5-branding/02.5-VALIDATION.md (BRD-02 predicates: 5 notification titles, module load alert, orphan-tag cleanup, require("purplevoice") in snippet)
  </read_first>
  <action>
Step 1 — rewrite `purplevoice-lua/init.lua` strings IN PLACE. Apply these EXACT replacements:

  Line 1 (file header comment):
    OLD: `-- voice-cc — Phase 2 hardened Hammerspoon module`
    NEW: `-- purplevoice — Phase 2 hardened Hammerspoon module (PurpleVoice)`

  Line 2 (file header comment):
    OLD: `-- Wires cmd+shift+e (push-and-hold) to ~/.local/bin/voice-cc-record.`
    NEW: `-- Wires cmd+shift+e (push-and-hold) to ~/.local/bin/purplevoice-record.`

  Line 7 (audio cues comment):
    OLD: `--   - Audio cues: Pop on press, Tink on release; VOICE_CC_NO_SOUNDS=1 silences (FBK-02)`
    NEW: `--   - Audio cues: Pop on press, Tink on release; PURPLEVOICE_NO_SOUNDS=1 silences (FBK-02)`

  Line 29 (callback registration comment):
    OLD: `-- if the user dismissed the prompt without granting (defence-in-depth per`
    NEW (KEEP — no change here, just contextual)

  Line 29 was the wrong line; the actual reference is at the comment block referencing `voiceccOpenAccessibilitySettings` — leave the OLD-tag-name strings in unregister calls (Step 5 below requires them) and in the register comment as historical context.

  Line 38 (SCRIPT_PATH constant):
    OLD: `local SCRIPT_PATH = os.getenv("HOME") .. "/.local/bin/voice-cc-record"`
    NEW: `local SCRIPT_PATH = os.getenv("HOME") .. "/.local/bin/purplevoice-record"`

  Lines 39-40 (menubar colours — REPLACE these constants but DO NOT change the structural code that uses them; Plan 03 will replace these constants again with lavender — for THIS plan, leave the existing grey/red colours in place as RGB constants but rename the constant comments only):
    Lines 39-40 are:
      `local MENUBAR_IDLE_COLOR = "#888888"`
      `local MENUBAR_RECORDING_COLOR = "#FF3B30"`
    Leave both lines BYTE-IDENTICAL — Plan 03 (visual identity) is the owner of the lavender colour migration. This plan does NOT touch the menubar colours.

  Line 73 (env var read for audio cues):
    OLD: `local soundsEnabled = (os.getenv("VOICE_CC_NO_SOUNDS") ~= "1")`
    NEW: `local soundsEnabled = (os.getenv("PURPLEVOICE_NO_SOUNDS") ~= "1")`

  Lines 112-114 (hs.notify.register voiceccOpenMicSettings — KEEP THE TAG NAME `voiceccOpenMicSettings`. The orphan-cleanup step (Step 5) needs the OLD tag string to appear in this file. Therefore: DO NOT rename the existing register tag in this plan; instead REPLACE the register call with a register call using the NEW tag name `purplevoiceOpenMicSettings` AND ADD the unregister call at the top in Step 5):

    Lines 112-114 are currently:
      `hs.notify.register("voiceccOpenMicSettings", function(notification)`
      `  hs.urlevent.openURL("x-apple.systempreferences://com.apple.settings.PrivacySecurity.extension?Privacy_Microphone")`
      `end)`

    Replace with:
      `hs.notify.register("purplevoiceOpenMicSettings", function(notification)`
      `  hs.urlevent.openURL("x-apple.systempreferences://com.apple.settings.PrivacySecurity.extension?Privacy_Microphone")`
      `end)`

  Lines 116-118 (the second register call):
    Currently:
      `hs.notify.register("voiceccOpenAccessibilitySettings", function(notification)`
      `  hs.urlevent.openURL("x-apple.systempreferences://com.apple.settings.PrivacySecurity.extension?Privacy_Accessibility")`
      `end)`

    Replace with:
      `hs.notify.register("purplevoiceOpenAccessibilitySettings", function(notification)`
      `  hs.urlevent.openURL("x-apple.systempreferences://com.apple.settings.PrivacySecurity.extension?Privacy_Accessibility")`
      `end)`

  Line 199 (notification call site for exit 10):
    OLD: `      hs.notify.new("voiceccOpenMicSettings", {`
    NEW: `      hs.notify.new("purplevoiceOpenMicSettings", {`

  Line 200 (notification title for exit 10):
    OLD: `        title = "voice-cc: microphone blocked",`
    NEW: `        title = "PurpleVoice: microphone blocked",`

  Line 212 (notification title for exit 11):
    OLD: `        title = "voice-cc: install incomplete",`
    NEW: `        title = "PurpleVoice: install incomplete",`

  Line 213 (informativeText for exit 11):
    OLD: `        informativeText = "Run setup.sh — model or binary missing",`
    NEW (KEEP — no brand string in this line)

  Line 221 (notification title for exit 12):
    OLD: `        title = "voice-cc: transcription failed",`
    NEW: `        title = "PurpleVoice: transcription failed",`

  Line 222 (informativeText for exit 12 — references ~/.cache/voice-cc/error.log):
    OLD: `        informativeText = "Check ~/.cache/voice-cc/error.log for details",`
    NEW: KEEP path as `~/.cache/voice-cc/error.log` (Plan 02 owns runtime path migration). The brand prefix is in the title.

  Line 227 (console log for unknown exit):
    OLD: `    hs.console.printStyledtext("voice-cc unknown exit " .. tostring(exitCode))`
    NEW: `    hs.console.printStyledtext("PurpleVoice unknown exit " .. tostring(exitCode))`

  Line 230 (unknown-exit notification title):
    OLD: `        title = "voice-cc: unexpected exit " .. tostring(exitCode),`
    NEW: `        title = "PurpleVoice: unexpected exit " .. tostring(exitCode),`

  Line 254 (onExit error log):
    OLD: `      hs.console.printStyledtext("voice-cc onExit error: " .. tostring(err))`
    NEW: `      hs.console.printStyledtext("PurpleVoice onExit error: " .. tostring(err))`

  Line 260 (script-not-found alert):
    OLD: `    hs.alert.show("voice-cc: script not found at " .. SCRIPT_PATH, 4)`
    NEW: `    hs.alert.show("PurpleVoice: script not found at " .. SCRIPT_PATH, 4)`

  Line 282 (binding-failed alert):
    OLD: `  hs.alert.show("voice-cc: cmd+shift+e binding failed (in use?)", 4)`
    NEW: `  hs.alert.show("PurpleVoice: cmd+shift+e binding failed (in use?)", 4)`

  Line 302 (accessibility-deny notification call site):
    OLD: `      hs.notify.new("voiceccOpenAccessibilitySettings", {`
    NEW: `      hs.notify.new("purplevoiceOpenAccessibilitySettings", {`

  Line 303 (accessibility-deny notification title):
    OLD: `        title = "voice-cc: accessibility required",`
    NEW: `        title = "PurpleVoice: accessibility required",`

  Line 313 (the load alert — extends the brand spelling AND the tagline form factor per D-12):
    OLD: `hs.alert.show("voice-cc loaded (cmd+shift+e)", 1.5)`
    NEW: `hs.alert.show("PurpleVoice loaded — local dictation, cmd+shift+e", 1.5)`

  Internal informational comments that mention `voice-cc` historically should be updated to `PurpleVoice` ONLY if they're describing the project today (not history). Specifically:
    - Line 4 (`-- Phase 2 additions over Phase 1:`) — keep
    - Line 31 (`-- Plan 02-03 extension: capture return value so we can surface a notification`) — keep (historical)
    - Comments referencing `voiceccOpen*Settings` tag names by their literal Lua-symbol value — leave as-is (the orphan-cleanup step needs them).

Step 2 — add hs.notify orphan-tag cleanup at the very TOP of the new init.lua, BEFORE line 33's `local accessibilityOk = hs.accessibilityState(true)` call. Insert these two lines immediately after the file header comment block (insert AFTER line 17 `-- Phase 2 deliberately OMITS (deferred to Plan 02-03):` block ends, BEFORE `local M = {}` on line 18):

```lua
-- ----------------------------------------------------------------
-- hs.notify orphaned-tag cleanup (Phase 2.5 rebrand — RESEARCH §"Pattern 4")
-- ----------------------------------------------------------------
-- The Phase 2 init.lua registered callbacks under the old voice-cc tag names.
-- Notification Center may still hold notifications referencing those tags;
-- clicking them after rebrand would raise the Hammerspoon console (default
-- orphaned-tag behaviour) instead of opening Settings. Unregistering the old
-- tags is a safe no-op if they don't exist and protective if they do.
pcall(hs.notify.unregister, "voiceccOpenMicSettings")
pcall(hs.notify.unregister, "voiceccOpenAccessibilitySettings")
```

The OLD tag strings `voiceccOpenMicSettings` and `voiceccOpenAccessibilitySettings` MUST appear verbatim in this block — VALIDATION.md predicate `grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua && grep -q "hs.notify.unregister" purplevoice-lua/init.lua` requires it.

Step 3 — export brand constants near the top of init.lua so future Phase 3.5 HUD can `require()` them. Add directly after the cleanup block (still before `local M = {}` line):

```lua
-- ----------------------------------------------------------------
-- Brand constants (exposed for future Phase 3.5 HUD consumption)
-- ----------------------------------------------------------------
local BRAND = {
  NAME = "PurpleVoice",
  TAGLINE = "Local voice dictation. Nothing leaves your Mac.",
  COLOUR_LAVENDER = "#B388EB",
}
```

Then immediately BEFORE `return M` (currently line 315), add:
```lua
M.BRAND = BRAND
```

This stages the constants for Phase 3.5 without changing runtime behaviour. Plan 03 will reference `BRAND.COLOUR_LAVENDER` when it migrates the menubar to lavender.

Step 4 — rewrite `.hammerspoon-init-snippet.lua`. The file is 19 lines. Replace its content with:

```lua
-- .hammerspoon-init-snippet.lua
-- PurpleVoice Phase 2.5 — paste this into ~/.hammerspoon/init.lua at the top.
--
-- PurpleVoice is local voice dictation for macOS. Nothing leaves your Mac.
--
-- Per ARCHITECTURE.md Anti-Pattern 4, PurpleVoice NEVER auto-edits your init.lua.
-- Apply this manually. Idempotent: if `require("hs.ipc")` is already present
-- (grep -F 'require("hs.ipc")' ~/.hammerspoon/init.lua), skip — no second
-- require is needed.

-- Enables the `hs` CLI tool at /opt/homebrew/bin/hs by opening a Mach
-- message port in the Hammerspoon process. After this is loaded:
--   hs -c "1+1"                 → prints 2
--   hs -c "hs.reload()"         → reloads Hammerspoon scriptably
--   echo 'print("hi")' | hs    → remote eval
--
-- TCC requirement: NONE (no system-protected resources accessed).
-- Side effects: one Mach port at Hammerspoon launch, no measurable overhead.
--
-- Source: PurpleVoice Phase 2 Plan 02-02; .planning/phases/02-hardening/02-RESEARCH.md §12.
require("hs.ipc")

-- After installing PurpleVoice (run setup.sh), also paste:
-- require("purplevoice")
```

The trailing `require("purplevoice")` comment is a recommendation for the user — they paste it after running setup.sh. The active line stays `require("hs.ipc")` from Phase 2; setup.sh in Plan 02 will print the explicit `require("purplevoice")` for them to paste during install.

Step 5 — verify counts:
  - `grep -c "PurpleVoice:" purplevoice-lua/init.lua` should be ≥ 5 (the 5 notification titles + the binding-failed alert + the script-not-found alert + accessibility-required notification = 7 expected; minimum 5 per VALIDATION.md).
  - `grep -q "PurpleVoice loaded" purplevoice-lua/init.lua` MUST pass.
  - `grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua && grep -q "hs.notify.unregister" purplevoice-lua/init.lua` MUST pass (orphan cleanup).
  - `grep -q "purplevoiceOpenMicSettings" purplevoice-lua/init.lua` MUST pass (NEW tag registered + used).
  - `grep -q "PURPLEVOICE_NO_SOUNDS" purplevoice-lua/init.lua` MUST pass.
  - `grep -q 'require("purplevoice")' .hammerspoon-init-snippet.lua` MUST pass.
  - `! grep -q whisper-cli purplevoice-lua/init.lua` (Pattern 2 corollary — Lua never references whisper-cli).
  - `! grep -q "VOICE_CC_NO_SOUNDS" purplevoice-lua/init.lua` (env var renamed).

  Surviving `voice-cc` strings expected in init.lua: ZERO in code paths. The only allowed exceptions are: (a) the OLD tag names inside the two `pcall(hs.notify.unregister, ...)` calls, and (b) the `~/.cache/voice-cc/error.log` path (Plan 02 owns).
  </action>
  <verify>
    <automated>[ "$(grep -c "PurpleVoice:" purplevoice-lua/init.lua)" -ge 5 ] &amp;&amp; grep -q "PurpleVoice loaded" purplevoice-lua/init.lua &amp;&amp; grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua &amp;&amp; grep -q "hs.notify.unregister" purplevoice-lua/init.lua &amp;&amp; grep -q "purplevoiceOpenMicSettings" purplevoice-lua/init.lua &amp;&amp; grep -q "PURPLEVOICE_NO_SOUNDS" purplevoice-lua/init.lua &amp;&amp; grep -q "M.BRAND" purplevoice-lua/init.lua &amp;&amp; grep -q 'require("purplevoice")' .hammerspoon-init-snippet.lua &amp;&amp; ! grep -q "whisper-cli" purplevoice-lua/init.lua &amp;&amp; ! grep -q "VOICE_CC_NO_SOUNDS" purplevoice-lua/init.lua</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `[ "$(grep -c "PurpleVoice:" purplevoice-lua/init.lua)" -ge 5 ]` (D-06 — five notification titles + alerts use the prefix)
    - [ ] `grep -q "PurpleVoice loaded" purplevoice-lua/init.lua` (load alert per D-12)
    - [ ] `grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua && grep -q "hs.notify.unregister" purplevoice-lua/init.lua` (orphan cleanup; OLD tag string MUST appear in unregister call per RESEARCH Pitfall 4)
    - [ ] `grep -q "purplevoiceOpenMicSettings" purplevoice-lua/init.lua` (NEW tag registered)
    - [ ] `grep -q "PURPLEVOICE_NO_SOUNDS" purplevoice-lua/init.lua` (env var renamed)
    - [ ] `grep -q "M.BRAND" purplevoice-lua/init.lua` (brand constants exported for Phase 3.5)
    - [ ] `grep -q 'require("purplevoice")' .hammerspoon-init-snippet.lua` (snippet shows new require line per D-08)
    - [ ] `! grep -q "whisper-cli" purplevoice-lua/init.lua` (Pattern 2 corollary intact — Lua never names whisper-cli)
    - [ ] `! grep -q "VOICE_CC_NO_SOUNDS" purplevoice-lua/init.lua` (no leftover env-var refs)
    - [ ] `bash tests/run_all.sh` exits 0 (no Lua-side regression — tests don't load Lua but they do test the bash glue's interaction with VOICE_CC_TEST_SKIP_SOX which Task 1 already renamed)
  </acceptance_criteria>
  <done>
init.lua at `purplevoice-lua/init.lua` shows 5+ `PurpleVoice:` strings, the new load alert, the OLD hs.notify tag names referenced inside `pcall(hs.notify.unregister, ...)` calls at the top, the NEW tag names in the register + send call sites, the renamed env var `PURPLEVOICE_NO_SOUNDS`, and the exported `M.BRAND` constants table. The user-paste snippet at `.hammerspoon-init-snippet.lua` shows the recommended `require("purplevoice")` line for the user to paste after install. `bash tests/run_all.sh` still GREEN.
  </done>
</task>

</tasks>

<verification>
Plan-level verification (run after both tasks complete):

```bash
# Files exist at new locations
[ -x purplevoice-record ] && [ -f purplevoice-lua/init.lua ]

# Old files gone
[ ! -e voice-cc-record ] && [ ! -d voice-cc-lua ]

# Pattern 2 invariant intact
[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ] && ! grep -q "whisper-cli" purplevoice-lua/init.lua

# All 5 notification titles + load alert present
[ "$(grep -c "PurpleVoice:" purplevoice-lua/init.lua)" -ge 5 ]
grep -q "PurpleVoice loaded" purplevoice-lua/init.lua

# Orphan tag cleanup present (BOTH old tag string AND unregister call)
grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua
grep -q "hs.notify.unregister" purplevoice-lua/init.lua

# New module-name require shown to user
grep -q 'require("purplevoice")' .hammerspoon-init-snippet.lua

# Env vars renamed (5 sites in bash + 1 in Lua = 6+)
grep -q "PURPLEVOICE_TEST_SKIP_SOX" purplevoice-record
grep -q "PURPLEVOICE_NO_SOUNDS" purplevoice-lua/init.lua
! grep -rln "VOICE_CC_" --include="*.sh" tests/

# Existing 6 bash tests still GREEN
bash tests/run_all.sh

# Pre-Plan-02 expected residue: runtime XDG paths still reference voice-cc
# (these will be migrated by Plan 02, not Plan 01)
grep -q "/.config/voice-cc" purplevoice-record   # expected — Plan 02 owns
grep -q "/.local/share/voice-cc" purplevoice-record  # expected — Plan 02 owns
grep -q "/tmp/voice-cc" purplevoice-record   # expected — Plan 02 owns
```
</verification>

<success_criteria>
1. `purplevoice-record` exists, is executable, has Pattern 2 boundary intact (`grep -c WHISPER_BIN == 2`), uses `PurpleVoice:` for all stderr messages, uses `PURPLEVOICE_*` env vars, contains zero `VOICE_CC_*` references.
2. `purplevoice-lua/init.lua` exists (directory rename succeeded), has ≥5 `PurpleVoice:` notification/alert titles, contains the `PurpleVoice loaded — local dictation, cmd+shift+e` module load alert, contains `pcall(hs.notify.unregister, "voiceccOpen*Settings")` cleanup at the top (preserving the OLD tag strings per RESEARCH Pitfall 4), and uses the NEW `purplevoiceOpen*Settings` tag names in register + send call sites.
3. `.hammerspoon-init-snippet.lua` shows `require("purplevoice")` as the recommended new require line per D-08.
4. All 6 existing bash tests under `tests/test_*.sh` still GREEN via `bash tests/run_all.sh`.
5. Manual walkthroughs in `tests/manual/*.md` reference `purplevoice-record` / `purplevoice-lua/init.lua` / `PurpleVoice:` titles wherever they previously referenced `voice-cc-*` (runtime path references like `/tmp/voice-cc/` deliberately preserved for Plan 02).
6. `BRAND` constants table is exposed via `M.BRAND` for future Phase 3.5 HUD consumption.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-branding/02.5-01-SUMMARY.md` documenting:
- Files renamed (voice-cc-record → purplevoice-record, voice-cc-lua/ → purplevoice-lua/)
- All `voice-cc` strings updated in code paths + the runtime XDG paths INTENTIONALLY left for Plan 02
- Env vars renamed (5 names) — note this is a breaking change with no fallback (per RESEARCH §"State of the Art" decision)
- 5 hs.notify titles + load alert + orphan-tag cleanup verified
- Pattern 2 invariant grep -c WHISPER_BIN purplevoice-record == 2 verified
- All 6 bash tests still GREEN
- Brand constants `M.BRAND` exposed for Phase 3.5 HUD
</output>
