---
phase: 02.5-branding
plan: 01
subsystem: branding
tags: [rebrand, hammerspoon, lua, bash, hs.notify, brand-strings, env-vars]

# Dependency graph
requires:
  - phase: 02-hardening
    provides: voice-cc-record (Phase 2 hardened bash glue with Pattern 2 boundary), voice-cc-lua/init.lua (315-line Hammerspoon module with notification dispatch), tests/run_all.sh (6 unit/integration tests), tests/manual/* (7 walkthrough docs)
provides:
  - purplevoice-record (renamed from voice-cc-record; brand strings + env vars rebranded; Pattern 2 boundary preserved; 6 PURPLEVOICE_* env vars; PurpleVoice: stderr message prefix)
  - purplevoice-lua/init.lua (renamed from voice-cc-lua/init.lua; 5 PurpleVoice: notification titles; module load alert with tagline form factor; hs.notify orphan-tag cleanup at module top; new purplevoice* tag namespace; M.BRAND constants table for Phase 3.5 HUD; consolidated cache-path edit ~/.cache/purplevoice/error.log moved from Plan 02.5-02 per checker iter 1)
  - .hammerspoon-init-snippet.lua (rewritten to recommend require("purplevoice") + brand-explanation comment)
  - All 7 manual walkthroughs and 6 bash unit tests reference purplevoice-record / purplevoice-lua / PurpleVoice strings
affects: [02.5-02-xdg-migration, 02.5-03-visual-identity, 02.5-04-readme-tagline, 03.5-hud, 03-distribution]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Brand constants exported from Lua module (M.BRAND with NAME/TAGLINE/COLOUR_LAVENDER) — Phase 3.5 HUD will consume from same module"
    - "hs.notify orphaned-tag cleanup pattern (pcall(hs.notify.unregister, OLD_TAG) at module top) — applies to any future tag rename"
    - "Find-and-replace anchor-string editing pattern (avoid line-number drift after insertions)"

key-files:
  created:
    - .planning/phases/02.5-branding/02.5-01-SUMMARY.md
  modified:
    - purplevoice-record (renamed from voice-cc-record)
    - purplevoice-lua/init.lua (renamed from voice-cc-lua/init.lua)
    - .hammerspoon-init-snippet.lua
    - tests/run_all.sh
    - tests/lib/sample_audio.sh
    - tests/test_duration_gate.sh
    - tests/test_denylist.sh
    - tests/test_wav_cleanup.sh
    - tests/test_sigint_cleanup.sh
    - tests/test_tcc_grep.sh
    - tests/test_vad_silence.sh
    - tests/manual/test_clipboard_restore.md
    - tests/manual/test_transient_marker.md
    - tests/manual/test_menubar.md
    - tests/manual/test_audio_cues.md
    - tests/manual/test_tcc_notification.md
    - tests/manual/test_reentrancy.md
    - tests/manual/test_accessibility_prompt.md

key-decisions:
  - "Hard env-var rename with no fallback (per RESEARCH §State of the Art Q1) — VOICE_CC_* → PURPLEVOICE_* is a breaking change documented for the single user; backward-compat shim deemed unnecessary for personal-tool ethos"
  - "Cache-path edit (~/.cache/voice-cc → ~/.cache/purplevoice) for the exit-12 informativeText consolidated into this plan from Plan 02.5-02 per checker iter 1, eliminating the Wave 2 init.lua file-write race with Plan 02.5-03"
  - "OLD voicecc tag strings preserved verbatim inside pcall(hs.notify.unregister, ...) calls per RESEARCH Pitfall 4 — protects against stale notifications in macOS Notification Center raising the Hammerspoon console"
  - "Runtime XDG paths (/tmp/voice-cc/, ~/.config/voice-cc/, ~/.local/share/voice-cc/) intentionally preserved in purplevoice-record and tests for Plan 02.5-02 ownership of the migration block"

patterns-established:
  - "Pattern 2 boundary survives rebrand: grep -c WHISPER_BIN purplevoice-record == 2 (assignment + invocation) preserved through file rename + brand string edits"
  - "Pattern 2 corollary (Lua side): ! grep -q whisper-cli purplevoice-lua/init.lua — Lua module never names whisper-cli, even in comments"
  - "Per-file targeted edit with grep verification (over global sed) — preserves invariants and surfaces context-sensitive cases"

requirements-completed:
  - BRD-02

# Metrics
duration: 8min
completed: 2026-04-29
---

# Phase 02.5 Plan 01: Brand string propagation Summary

**voice-cc → PurpleVoice rebrand of bash glue + Lua module + user-paste snippet + 6 unit tests + 7 manual walkthroughs, with hs.notify orphan-tag cleanup, M.BRAND constants export, and the cache-path edit from Plan 02 consolidated to eliminate the Wave 2 race — Pattern 2 boundary intact, all 6 bash tests still GREEN.**

## Performance

- **Duration:** 8m 6s (486 seconds)
- **Started:** 2026-04-29T06:32:05Z
- **Completed:** 2026-04-29T06:40:11Z
- **Tasks:** 2
- **Files modified:** 18 (2 renamed, 16 edited)

## Accomplishments

- Renamed `voice-cc-record` → `purplevoice-record` (file rename + brand strings + 5 env-var renames in script + 1 in tests). Pattern 2 invariant verified post-edit: `grep -c WHISPER_BIN purplevoice-record == 2`.
- Renamed `voice-cc-lua/` → `purplevoice-lua/` (directory rename via `git mv`).
- Rewrote `purplevoice-lua/init.lua`:
  - 5 `hs.notify` titles use `PurpleVoice:` prefix (microphone blocked, install incomplete, transcription failed, accessibility required, unexpected exit N) — D-06 closed.
  - Module load alert: `PurpleVoice loaded — local dictation, cmd+shift+e` (D-12 form factor).
  - Two `hs.notify.register` tags + 2 `hs.notify.new` call sites use new `purplevoiceOpen*Settings` namespace.
  - Inserted `pcall(hs.notify.unregister, "voiceccOpen{Mic,Accessibility}Settings")` cleanup at module top — RESEARCH Pattern 4, protects against orphaned notifications in macOS Notification Center.
  - Inserted `local BRAND = { NAME, TAGLINE, COLOUR_LAVENDER }` constants block + `M.BRAND = BRAND` export — Phase 3.5 HUD will consume.
  - Cache-path edit consolidated from Plan 02.5-02 (checker iter 1): exit-12 `informativeText` now references `~/.cache/purplevoice/error.log`. Eliminates Wave 2 `init.lua` file-write race with Plan 02.5-03.
- Rewrote `.hammerspoon-init-snippet.lua` (D-08): recommended `require("purplevoice")` line + brand-explanation comment + privacy positioning ("Nothing leaves your Mac").
- Rebranded all 6 bash unit tests + helpers + run_all.sh + 7 manual walkthroughs.
- Verified all 6 bash unit tests still GREEN post-rebrand.

## Task Commits

Each task was committed atomically:

1. **Task 1: Rename file + directory and rewrite the bash glue strings** — `2c9a7f2` (feat)
2. **Task 2: Rewrite Lua module strings + add hs.notify orphan-tag cleanup + cache-path edit** — `090c1dd` (feat)

**Plan metadata:** TBD — final commit after SUMMARY + STATE updates.

## Files Created/Modified

**Renamed (via `git mv`):**
- `voice-cc-record` → `purplevoice-record` — Phase 2 hardened bash glue, brand strings + env vars rebranded; Pattern 2 boundary preserved
- `voice-cc-lua/init.lua` → `purplevoice-lua/init.lua` — Hammerspoon module, brand strings + tag namespace + cache-path edit applied; M.BRAND export added

**Modified:**
- `.hammerspoon-init-snippet.lua` — rewritten with `require("purplevoice")` recommendation + brand comment (D-08)
- `tests/run_all.sh` — log path renamed
- `tests/lib/sample_audio.sh` — comment refs to script name updated
- `tests/test_duration_gate.sh` — env var refs (`VOICE_CC_INTEGRATION`, `VOICE_CC_TEST_SKIP_SOX`) + script name + mktemp prefix renamed
- `tests/test_denylist.sh` — env var refs + script name renamed
- `tests/test_wav_cleanup.sh` — env var + script-name refs renamed
- `tests/test_sigint_cleanup.sh` — env var + script-name refs renamed
- `tests/test_tcc_grep.sh` — mktemp prefix renamed
- `tests/test_vad_silence.sh` — mktemp prefix renamed
- `tests/manual/test_*.md` (7 files) — voice-cc-* file refs and notification titles rebranded; runtime XDG paths intentionally preserved

**Created:**
- `.planning/phases/02.5-branding/02.5-01-SUMMARY.md` — this file

## Decisions Made

- **Hard env-var rename, no backwards compatibility.** Per RESEARCH §"State of the Art" Q1 + decision: simpler than supporting a fallback shim for one user. Documented breaking change for the single user (Oliver), who has no `VOICE_CC_*` exports observed in shell rc.
- **Cache-path edit consolidation from Plan 02.5-02 (revision iter 1).** Plan 02.5-02 originally also touched `init.lua` (one-line cache-path edit) creating a Wave 2 file-write race with Plan 02.5-03 (menubar styling). Consolidating into this plan's Task 2 makes Plan 02.5-03 the SOLE Wave 2 owner of `init.lua`.
- **OLD `voicecc*` tag strings preserved verbatim inside `pcall(hs.notify.unregister, ...)` calls** per RESEARCH Pitfall 4 — protects against orphaned-tag console-raise on stale notifications in macOS Notification Center.
- **Runtime XDG paths preserved for Plan 02.5-02.** All `/tmp/voice-cc/`, `~/.config/voice-cc/`, `~/.local/share/voice-cc/` references in `purplevoice-record` and tests/* are intentionally left as-is so Plan 02.5-02's idempotent migration block has the literal old strings to migrate FROM. Verified via plan-level checks: `grep -q "/.config/voice-cc" purplevoice-record` etc. all return success.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Stale comment referencing old `voiceccOpenAccessibilitySettings` tag**
- **Found during:** Task 2 (post-Step-3 verification grep)
- **Issue:** Comment at line 49 of `purplevoice-lua/init.lua` referred to `voiceccOpenAccessibilitySettings` but the active code now uses `purplevoiceOpenAccessibilitySettings`. Misleading.
- **Fix:** Changed `voiceccOpenAccessibilitySettings` → `purplevoiceOpenAccessibilitySettings` in the comment.
- **Files modified:** `purplevoice-lua/init.lua`
- **Verification:** Comment now matches actual register/send call sites.
- **Committed in:** 090c1dd (Task 2 commit)

**2. [Rule 1 - Bug] Stale comment referencing old `VOICE_CC_NO_SOUNDS` env var**
- **Found during:** Task 2 (post-Step-3 verification grep)
- **Issue:** Comment at line 89 of `purplevoice-lua/init.lua` said "VOICE_CC_NO_SOUNDS read once at module load" but the active code below reads `os.getenv("PURPLEVOICE_NO_SOUNDS")`. Misleading documentation.
- **Fix:** Changed `VOICE_CC_NO_SOUNDS` → `PURPLEVOICE_NO_SOUNDS` in the comment.
- **Files modified:** `purplevoice-lua/init.lua`
- **Verification:** `! grep -q "VOICE_CC_NO_SOUNDS" purplevoice-lua/init.lua` passes.
- **Committed in:** 090c1dd (Task 2 commit)

**3. [Rule 1 - Bug] Pre-existing comment violating Pattern 2 corollary**
- **Found during:** Task 2 (Step 5 verification)
- **Issue:** Comment at line 238 said "Generic sox / whisper-cli failure (non-TCC)". The plan's verify clause is `! grep -q "whisper-cli" purplevoice-lua/init.lua` (Pattern 2 corollary — Lua module never names whisper-cli). The pre-existing Phase 2 comment violated this invariant; only the rebrand surfaced it.
- **Fix:** Changed "whisper-cli" → "transcription" in the comment, preserving meaning.
- **Files modified:** `purplevoice-lua/init.lua`
- **Verification:** `! grep -q "whisper-cli" purplevoice-lua/init.lua` passes.
- **Committed in:** 090c1dd (Task 2 commit)

**4. [Rule 1 - Bug] Plan-required ZERO `voice-cc` strings violated by line-21 comment**
- **Found during:** Task 2 (Step 5 verification)
- **Issue:** The orphan-cleanup comment block I inserted said "registered callbacks under the old voice-cc tag names". Plan Step 3 final paragraph states `! grep -q "voice-cc" purplevoice-lua/init.lua` should pass post-edit (the OLD tags `voiceccOpen*Settings` don't contain a hyphen, so the file should be 100% `voice-cc`-free).
- **Fix:** Changed "old voice-cc tag names" → "old voicecc tag names" in the comment (matches the actual tag spellings without the hyphen).
- **Files modified:** `purplevoice-lua/init.lua`
- **Verification:** `! grep -q "voice-cc" purplevoice-lua/init.lua` passes.
- **Committed in:** 090c1dd (Task 2 commit)

---

**Total deviations:** 4 auto-fixed (4 Rule 1 bug fixes — all stale comments referring to old names, surfaced by post-edit verification grep)
**Impact on plan:** All four fixes were one-line comment corrections; no behavior change. Three of the four (deviations 1, 2, 4) were directly caused by my own initial edits — I had not propagated the rename consistently into comments. Deviation 3 was a pre-existing inconsistency in the Phase 2 source that the rebrand verification surfaced. No scope creep.

## Issues Encountered

- None. The plan's "Edit Order" note (revision iter 1) about doing insertions first then find-and-replace pairs worked exactly as described — no line-number drift issues. The four Rule 1 deviations above were trivially fixable and detected by the plan's own grep verification predicates.

## User Setup Required

None - no external service configuration required. The downstream user-facing change (paste `require("purplevoice")` into `~/.hammerspoon/init.lua`, set `PURPLEVOICE_*` env vars instead of `VOICE_CC_*`) is owned by Plan 02.5-02 (setup.sh migration block + symlink rewiring) and Plan 02.5-04 (README/setup.sh banner updates).

## Next Phase Readiness

Wave 1 complete. Plan 02.5-02 (Wave 2) and Plan 02.5-03 (Wave 2) are now unblocked:

- **Plan 02.5-02 (XDG path rename + setup.sh migration):** All file/binary names it references in the migration block are stabilised (`purplevoice-record`, `purplevoice-lua/`). Per consolidation, Plan 02.5-02 NO LONGER touches `init.lua` — it owns ONLY `setup.sh` + symlink hygiene. The Wave 2 race is eliminated.
- **Plan 02.5-03 (visual identity — menubar lavender + 256×256 PNG):** `purplevoice-lua/init.lua` is the SOLE file it modifies in Wave 2 with no concurrent writer. Menubar colour constants (`MENUBAR_IDLE_COLOR`, `MENUBAR_RECORDING_COLOR`) are still byte-identical from Phase 2 — Plan 02.5-03 will swap them for `MENUBAR_LAVENDER` (#B388EB).

**Brand-consistency test (Plan 02.5-04 task):** Will assert ZERO `voice-cc` strings outside `.planning/`/`.git/`/runtime XDG paths. This plan's verification confirms the code surfaces are clean except for the intentionally-preserved runtime XDG paths in `purplevoice-record` (Plan 02.5-02 owns those).

**No blockers.**

## Self-Check: PASSED

Verified after writing SUMMARY:

- [x] `purplevoice-record` exists, executable: FOUND
- [x] `purplevoice-lua/init.lua` exists: FOUND
- [x] `voice-cc-record`: GONE (rename complete)
- [x] `voice-cc-lua/`: GONE (rename complete)
- [x] Pattern 2: `grep -c WHISPER_BIN purplevoice-record == 2`: VERIFIED
- [x] `! grep -q whisper-cli purplevoice-lua/init.lua`: VERIFIED
- [x] `grep -c "PurpleVoice:" purplevoice-lua/init.lua >= 5` (actual: 7): VERIFIED
- [x] `grep -q "PurpleVoice loaded" purplevoice-lua/init.lua`: VERIFIED
- [x] `grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua` AND `grep -q "hs.notify.unregister"`: VERIFIED (orphan cleanup)
- [x] `grep -q "purplevoiceOpenMicSettings" purplevoice-lua/init.lua`: VERIFIED (NEW tag)
- [x] `grep -q "PURPLEVOICE_NO_SOUNDS" purplevoice-lua/init.lua`: VERIFIED
- [x] `grep -q "M.BRAND" purplevoice-lua/init.lua`: VERIFIED
- [x] `grep -q "/\.cache/purplevoice/error\.log" purplevoice-lua/init.lua`: VERIFIED
- [x] `! grep -q "/\.cache/voice-cc" purplevoice-lua/init.lua`: VERIFIED
- [x] `grep -q 'require("purplevoice")' .hammerspoon-init-snippet.lua`: VERIFIED
- [x] `! grep -q "VOICE_CC_NO_SOUNDS" purplevoice-lua/init.lua`: VERIFIED
- [x] `! grep -rln "VOICE_CC_" --include="*.sh" tests/`: VERIFIED (no env-var refs in shell tests)
- [x] `! grep -rln "voice-cc-record" --include="*.sh" tests/`: VERIFIED
- [x] `bash tests/run_all.sh` exits 0: VERIFIED (6 PASS / 0 FAIL)
- [x] Commit `2c9a7f2` exists in git log: VERIFIED
- [x] Commit `090c1dd` exists in git log: VERIFIED

---
*Phase: 02.5-branding*
*Completed: 2026-04-29*
