---
phase: 02.5-branding
plan: 04
type: execute
wave: 3
depends_on:
  - 02.5-01
  - 02.5-02
  - 02.5-03
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - README.md
  - CLAUDE.md
  - tests/test_brand_consistency.sh
  - tests/run_all.sh
autonomous: true
requirements:
  - BRD-01
  - BRD-02
  - BRD-03
must_haves:
  truths:
    - "PROJECT.md records the chosen name 'PurpleVoice' with a one-paragraph privacy-first rationale (BRD-01 success criterion)"
    - "REQUIREMENTS.md contains formal entries for BRD-01, BRD-02, BRD-03 with full descriptions matching CONTEXT.md decisions"
    - "REQUIREMENTS.md traceability table includes all three BRD-* rows mapped to Phase 2.5"
    - "README.md header carries the canonical tagline 'Local voice dictation. Nothing leaves your Mac.' directly under the H1"
    - "README.md install/permissions narrative uses PurpleVoice consistently (not voice-cc) for new prose; references the icon at assets/icon-256.png"
    - "CLAUDE.md project section uses PurpleVoice (not voice-cc) for current product references"
    - "tests/test_brand_consistency.sh exists, is executable, runs the canonical post-rebrand grep audit, and is picked up by tests/run_all.sh"
    - "All 7 tests still GREEN (6 existing + 1 new brand-consistency test)"
    - "After this plan, no voice-cc strings survive in user-visible code/doc surfaces (the .planning/ historical record + setup.sh migration FROM args remain by design)"
  artifacts:
    - path: ".planning/PROJECT.md"
      provides: "Project north star with PurpleVoice name + privacy-first rationale paragraph"
      contains: "PurpleVoice"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Formal BRD-01..03 entries + traceability mapping to Phase 2.5"
      contains: "BRD-01"
    - path: "README.md"
      provides: "Public-facing entry doc with PurpleVoice header + tagline + brand-consistent install narrative + icon reference"
      contains: "Local voice dictation. Nothing leaves your Mac."
    - path: "CLAUDE.md"
      provides: "Project-level instructions referencing PurpleVoice in the current-product section"
      contains: "PurpleVoice"
    - path: "tests/test_brand_consistency.sh"
      provides: "Regression-catch test ensuring no voice-cc strings sneak into user-facing surfaces"
      min_lines: 8
  key_links:
    - from: "tests/run_all.sh"
      to: "tests/test_brand_consistency.sh"
      via: "glob tests/test_*.sh"
      pattern: "test_brand_consistency.sh"
    - from: "README.md"
      to: "assets/icon-256.png"
      via: "Markdown image reference under the H1"
      pattern: 'assets/icon-256.png'
    - from: ".planning/REQUIREMENTS.md traceability table"
      to: "Phase 2.5"
      via: "BRD-01..03 rows added to the table"
      pattern: 'BRD-01.*Phase 2.5'
---

<objective>
Close the brand rebrand by formalising the documentation surface: update PROJECT.md to record `PurpleVoice` as the official name with the privacy-first positioning rationale (BRD-01 success criterion), add formal `BRD-01..03` entries to REQUIREMENTS.md with the full text per CONTEXT.md (and the traceability table), expand the README from its 35-line stub into a brand-consistent install doc with the canonical tagline directly under the H1 and an icon reference, update CLAUDE.md's two voice-cc references in the current-product section, and add a regression-catch `tests/test_brand_consistency.sh` that runs the canonical post-rebrand grep audit so any future drift is caught automatically.

Purpose: Plans 01-03 changed the running code; this plan changes the explanation. After this plan, every user-visible surface — README, in-app strings, install banner, project north star, and the requirements catalogue — uses `PurpleVoice` consistently and reinforces the privacy-first positioning. The brand-consistency test makes the rebrand auditable: any future commit that introduces `voice-cc` in a user-facing surface will fail `bash tests/run_all.sh`, providing automatic regression catch in Phase 3 distribution work and beyond.

Output: updated `.planning/PROJECT.md` (name + positioning paragraph), updated `.planning/REQUIREMENTS.md` (3 new requirement entries + 3 new traceability rows), expanded `README.md` (~50-80 lines, brand-consistent), updated `CLAUDE.md` (2 references), new `tests/test_brand_consistency.sh` (~8 lines), and `tests/run_all.sh` requires no edit because it globs `tests/test_*.sh` already.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02.5-branding/02.5-CONTEXT.md
@.planning/phases/02.5-branding/02.5-RESEARCH.md
@.planning/phases/02.5-branding/02.5-VALIDATION.md
@.planning/phases/02.5-branding/02.5-01-SUMMARY.md
@.planning/phases/02.5-branding/02.5-02-SUMMARY.md
@.planning/phases/02.5-branding/02.5-03-SUMMARY.md
@README.md
@CLAUDE.md

<interfaces>
<!-- Locked decisions from CONTEXT.md that this plan converts into prose. -->

D-01 product name: `PurpleVoice` (PascalCase brand) / `purplevoice` (lowercase paths)
D-02 positioning: privacy-conscious users who do not want or cannot use cloud-connected dictation tools — load-bearing differentiator vs Koe (cloud default), Wispr Flow (subscription cloud), Voicy (cloud), broader market.
D-11 tagline: `Local voice dictation. Nothing leaves your Mac.`
D-12 placement: README header (under H1), setup.sh banner (Plan 02 — done), Hammerspoon load alert (Plan 01 — done), Phase 3.5 HUD reserved (Plan 01 — staged in M.BRAND.TAGLINE)

PROJECT.md sections to update:
- H1 `# voice-cc` → `# PurpleVoice` (or insert a "## Name" section preserving voice-cc as the historical working name with PurpleVoice as the current public-facing name)
- "## What This Is" — replace `A local, push-to-talk voice input system for Claude Code on macOS` opening sentence to lead with PurpleVoice
- "## Core Value" — keep core value statement; reinforce privacy framing
- "## Requirements -> Active" line `Public-facing product name (rebrand from working name "voice-cc") (Phase 2.5 — added 2026-04-27; BRD-01..03)` → mark as completed via "[x]" + reference to this phase's SUMMARY
- INSERT a new "## Name & Positioning" section after "## Core Value" that records:
  - Name: PurpleVoice
  - Lowercase form: purplevoice (module / binary / paths)
  - Privacy-first positioning paragraph
  - Tagline

REQUIREMENTS.md additions (new section "### Branding" or appended to existing structure):
- BRD-01: Product name PurpleVoice recorded in PROJECT.md with rationale + privacy-first positioning paragraph
- BRD-02: All user-visible strings use PurpleVoice; voice-cc preserved only in repo path / git history / .planning/ artifacts
- BRD-03: Minimal visual identity — 256x256 PNG icon (lavender bg, white lips) at assets/icon-256.png + lavender (#B388EB) menubar glyph

Traceability table additions: 3 new rows
| BRD-01 | Phase 2.5: Branding | Complete |
| BRD-02 | Phase 2.5: Branding | Complete |
| BRD-03 | Phase 2.5: Branding | Complete |

Coverage summary update: per-phase counts include "Phase 2.5: Branding — 3 requirements (BRD-01..03)"; overall mapped count: 26 + 3 = 29 (or note total v1+rebrand=33-34 per ROADMAP).

README.md target structure (post-edit):
```
# PurpleVoice

**Local voice dictation. Nothing leaves your Mac.**

![PurpleVoice icon](assets/icon-256.png)

A local, push-to-talk voice input system for Claude Code on macOS Apple Silicon...

## Why PurpleVoice
[2-3 sentence privacy-first positioning paragraph]

## Status
[migrate from "Phase 1 spike" to current state per STATE.md]

## Setup
bash setup.sh
[unchanged narrative; replace voice-cc paths with purplevoice paths]

## Permissions
[unchanged]

## Hotkey
cmd+shift+e (push-and-hold)

## Conflicting macOS feature to disable
[unchanged]
```

CLAUDE.md edit target:
- Line 4: `**voice-cc**` → `**PurpleVoice**`
- Line 62 (in commented-out install instruction): `# curl -L -o ~/.local/share/voice-cc/models/ggml-medium.en.bin \` → `# curl -L -o ~/.local/share/purplevoice/models/ggml-medium.en.bin \`
  (Note: line 62 is inside the embedded STACK.md block — verify the file is hand-editable; if it's auto-generated by GSD, rewrite anyway since the source-of-truth STACK.md has the same outdated path. The user should propagate to STACK.md separately if they keep an external copy.)

tests/test_brand_consistency.sh contract:
- Shebang `#!/usr/bin/env bash`
- `set -euo pipefail`
- Find any `voice-cc` strings outside `.planning/` and `.git/`, exclude approved exception list (setup.sh's 3 migrate FROM args, init.lua's 2 unregister-call OLD-tag strings — both substring `voicecc` not `voice-cc` so don't match anyway, the cache/error.log path in init.lua was migrated in Plan 02)
- After exclusions, hits MUST be ZERO
- Exit 0 on PASS, exit 1 on FAIL with a list of unexpected occurrences
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update PROJECT.md (BRD-01) + formalise BRD-01..03 in REQUIREMENTS.md + update CLAUDE.md</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, CLAUDE.md</files>
  <read_first>
    - .planning/PROJECT.md (current 97 lines — needs to flip from voice-cc working name to PurpleVoice public name + add Name & Positioning section + update Active requirements line)
    - .planning/REQUIREMENTS.md (current 148 lines — has 26 v1 reqs already mapped; needs 3 new BRD-* entries + 3 new traceability rows + coverage summary update)
    - .planning/phases/02.5-branding/02.5-CONTEXT.md (D-01: name; D-02: positioning paragraph; D-11: tagline; D-13: privacy-first paragraph for PROJECT.md)
    - CLAUDE.md (line 4 + line 62 — only 2 voice-cc occurrences in this file)
    - .planning/ROADMAP.md (Phase 2.5 §"Requirements" lists BRD-01..03 as TBD-shapes — formalising them is what closes those TBDs)
  </read_first>
  <action>
Step 1 — update `.planning/PROJECT.md`. Make these edits:

  Line 1 (H1):
    OLD: `# voice-cc`
    NEW: `# PurpleVoice`

  Lines 3-5 ("## What This Is" section paragraph):
    OLD: `A local, push-to-talk voice input system for Claude Code on macOS. Hold a hotkey, speak, release — your transcript appears in the focused Claude Code terminal. Built for one user (Oliver) who wants hands-on-keyboard speed without typing fatigue, with zero subscriptions, zero rate limits, and full ownership of the stack.`
    NEW: `**PurpleVoice** is a local, push-to-talk voice input system for Claude Code on macOS Apple Silicon. Hold a hotkey, speak, release — your transcript appears in the focused Claude Code terminal. Built for one user (Oliver) who wants hands-on-keyboard speed without typing fatigue, with zero subscriptions, zero rate limits, and full ownership of the stack. Working name was `voice-cc`; renamed during Phase 2.5 (2026-04-29).`

  INSERT a new section directly after "## Core Value" (currently line 11) and before "## Requirements" (currently line 13). Insert:

```markdown
## Name & Positioning

**Name:** `PurpleVoice` (PascalCase brand). Lowercase `purplevoice` is the canonical form for the Hammerspoon module, the bash binary, and XDG paths.

**Tagline:** _Local voice dictation. Nothing leaves your Mac._

**Positioning:** PurpleVoice is built for users who do not want — or cannot use — cloud-connected dictation tools. Every alternative we considered (Koe, Wispr Flow, Voicy, SuperWhisper at any tier with cloud sync, the Mac's built-in Dictation when set to "use server-based") sends audio off the machine by default or by subscription. PurpleVoice runs whisper.cpp locally with the Metal backend on Apple Silicon. No network calls, no API keys, no quotas, no telemetry, no opt-in cloud features. Audio is captured to a temporary file, transcribed by a local binary, the WAV is deleted on every exit path, and the transcript is pasted into the focused window. Everything stays on the Mac.

This is the load-bearing differentiator: cloud dictation has cheaper development, smaller models, and arguably better accuracy on long-form English. PurpleVoice trades those for ownership. The user knows exactly where every byte of audio goes (nowhere) and exactly which binaries see the recording (one: whisper-cli, on disk, transient).

**Visual identity:** Lavender `#B388EB` for the menubar indicator and the icon background; white lips silhouette on lavender for the 256×256 icon at `assets/icon-256.png`. The lips imagery (rather than a generic microphone) signals the human gesture — speaking, intimately, on your own machine.

**Decided:** Phase 2.5 (2026-04-29). See `.planning/phases/02.5-branding/02.5-CONTEXT.md` for the full decision log (D-01 through D-12) and `.planning/phases/02.5-branding/02.5-01-SUMMARY.md` through `02.5-04-SUMMARY.md` for execution evidence.
```

  Update the "## Requirements -> Active" section. Current line 30 reads:
    `- [ ] **Public-facing product name (rebrand from working name "voice-cc")** *(Phase 2.5 — added 2026-04-27; BRD-01..03)*`
  Change to:
    `- [x] **Public-facing product name (rebrand from working name "voice-cc")** *(Phase 2.5 — completed 2026-04-29; BRD-01..03 — see `.planning/phases/02.5-branding/`)*`

  Update the trailing "*Last updated*" line at the bottom of PROJECT.md (currently line 97 reads `*Last updated: 2026-04-27 — ...*`):
    APPEND a new line: `*Updated: 2026-04-29 — Phase 2.5 Branding closed; PurpleVoice name + privacy-first positioning recorded; BRD-01..03 marked complete.*`

Step 2 — formalise BRD-01..03 in `.planning/REQUIREMENTS.md`. Add a new section AFTER the "### Distribution" section (which ends with DST-04 around line 52) and BEFORE the "## v2 Requirements" section. INSERT:

```markdown
### Branding

- [x] **BRD-01**: Product name `PurpleVoice` is recorded in `.planning/PROJECT.md` as the authoritative public-facing name with a one-paragraph rationale that anchors the privacy-first positioning (target audience = users who don't want or cannot use cloud-connected dictation tools)
- [x] **BRD-02**: All user-visible strings (README, Hammerspoon alerts and notification titles, setup.sh banner, install snippet, error messages, manual walkthroughs) use `PurpleVoice` (PascalCase) or `purplevoice` (lowercase paths/modules/binaries) consistently. The original working name `voice-cc` is preserved verbatim ONLY in: the repo working directory path on disk, the git history, all `.planning/` artifacts (historical record), and the `migrate_xdg_dir` FROM-arg literals in `setup.sh` (intentional — needed for the migration logic). The canonical tagline `Local voice dictation. Nothing leaves your Mac.` is placed in the README header, the setup.sh banner, and the Hammerspoon module load alert.
- [x] **BRD-03**: Minimal visual identity — a 256×256 PNG icon at `assets/icon-256.png` (lavender `#B388EB` background, centred white lips silhouette, flat-design) derived deterministically from a hand-authored `assets/icon.svg` source via `sips`; AND a Hammerspoon menubar glyph styled with the same lavender `#B388EB` for both idle and recording states (recording-state differentiation via glyph shape — outline circle U+25CB for idle, filled circle U+25CF for recording).
```

Update the traceability table — find it (lines 96-132 in current file). Insert these 3 rows in the right alphabetical position (after CAP/TRA/INJ/FBK/ROB and before DST):

```markdown
| BRD-01 | Phase 2.5: Branding | Complete |
| BRD-02 | Phase 2.5: Branding | Complete |
| BRD-03 | Phase 2.5: Branding | Complete |
```

Update the "## Coverage" summary at the bottom (lines 134-143 in current file):
- Current reads: `- v1 requirements: 26 total / Mapped to phases: 26 / 26 (100%)`
- Update to: `- v1 requirements: 26 original + 3 branding = 29 total / Mapped to phases: 29 / 29 (100%)`
- Add a new per-phase row to the per-phase counts table:
  `- Phase 2.5: Branding — 3 requirements (BRD-01, BRD-02, BRD-03)`
- Update the trailing `*Last updated:*` line: `*Last updated: 2026-04-29 — BRD-01..03 formalised + traceability rows added (Phase 2.5 closure)*`

Step 3 — update `CLAUDE.md`:
  Line 4:
    OLD: `**voice-cc**`
    NEW: `**PurpleVoice**`

  Line 62:
    OLD: `# curl -L -o ~/.local/share/voice-cc/models/ggml-medium.en.bin \`
    NEW: `# curl -L -o ~/.local/share/purplevoice/models/ggml-medium.en.bin \`

  Note that CLAUDE.md is partly auto-generated from `.planning/research/STACK.md` per the GSD project-start markers. The fix here is to the rendered file. If the user re-runs a CLAUDE.md regeneration, those references will revert unless the upstream STACK.md is updated separately — that's out of scope for this plan (Phase 3 distribution work will own STACK.md edits if/when they happen). Document this as a known follow-up in the plan SUMMARY.

  Final verification:
  - `grep -q "PurpleVoice" .planning/PROJECT.md`
  - `grep -A2 "PurpleVoice" .planning/PROJECT.md | grep -qiE "(local|on-device|privacy|nothing leaves)"` (privacy paragraph present)
  - `grep -q "BRD-01" .planning/REQUIREMENTS.md`
  - `grep -q "BRD-02" .planning/REQUIREMENTS.md`
  - `grep -q "BRD-03" .planning/REQUIREMENTS.md`
  - `grep -q "BRD-01.*Phase 2.5" .planning/REQUIREMENTS.md` (traceability row)
  - `grep -q "PurpleVoice" CLAUDE.md`
  - `! grep -q "^\\*\\*voice-cc\\*\\*" CLAUDE.md` (the bolded current-product reference is updated)

  </action>
  <verify>
    <automated>grep -q "PurpleVoice" .planning/PROJECT.md && grep -A5 "Name & Positioning" .planning/PROJECT.md | grep -qiE "(privacy|nothing leaves|on-device|local)" && grep -q "BRD-01" .planning/REQUIREMENTS.md && grep -q "BRD-02" .planning/REQUIREMENTS.md && grep -q "BRD-03" .planning/REQUIREMENTS.md && grep -E "BRD-0[123].*Phase 2.5" .planning/REQUIREMENTS.md | wc -l | grep -qE "^\s*3\s*$" && grep -q "^\*\*PurpleVoice\*\*" CLAUDE.md && grep -q "purplevoice/models/ggml-medium" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -q "PurpleVoice" .planning/PROJECT.md` (name recorded; per VALIDATION BRD-01)
    - [ ] `grep -A5 "Name & Positioning" .planning/PROJECT.md | grep -qiE "(privacy|nothing leaves|on-device|local)"` (privacy paragraph present; per VALIDATION BRD-01)
    - [ ] `grep -q "Local voice dictation. Nothing leaves your Mac." .planning/PROJECT.md` (tagline recorded in PROJECT)
    - [ ] `grep -q "BRD-01" .planning/REQUIREMENTS.md && grep -q "BRD-02" .planning/REQUIREMENTS.md && grep -q "BRD-03" .planning/REQUIREMENTS.md` (3 formal entries added)
    - [ ] `grep -E "BRD-0[123].*Phase 2.5" .planning/REQUIREMENTS.md | wc -l` returns 3 (3 traceability rows added)
    - [ ] `grep -q "^\*\*PurpleVoice\*\*" CLAUDE.md` (current-product H-equivalent header rebranded)
    - [ ] `grep -q "purplevoice/models/ggml-medium" CLAUDE.md` (the commented install path updated)
    - [ ] `! grep -q "^\*\*voice-cc\*\*" CLAUDE.md` (no leftover bolded voice-cc reference)
    - [ ] PROJECT.md "Active" line for the rebrand task is now `[x]` (closed)
  </acceptance_criteria>
  <done>
PROJECT.md has its H1, opening paragraph, new Name & Positioning section, and Active-requirements line all aligned with PurpleVoice + privacy-first positioning + tagline. REQUIREMENTS.md has 3 new entries (BRD-01..03), 3 new traceability rows mapping to Phase 2.5, and an updated coverage summary. CLAUDE.md's 2 voice-cc references are PurpleVoice / purplevoice. All grep predicates pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Expand README.md + write tests/test_brand_consistency.sh</name>
  <files>README.md, tests/test_brand_consistency.sh</files>
  <read_first>
    - README.md (current 35 lines — H1 says voice-cc, no tagline, no icon, no positioning; setup section references voice-cc XDG paths; this task expands it to ~50-80 lines, updates paths to purplevoice, adds tagline + icon + positioning)
    - .planning/phases/02.5-branding/02.5-CONTEXT.md (D-11 tagline; D-12 placement; D-02 positioning)
    - .planning/phases/02.5-branding/02.5-RESEARCH.md "Validation Architecture -> Sampling Rate -> Full suite command" (the canonical brand-consistency grep is the source pattern for the new test)
    - .planning/phases/02.5-branding/02.5-VALIDATION.md "Wave 0 Requirements" (codifying the post-rebrand grep audit as test_brand_consistency.sh — recommended)
    - tests/run_all.sh (must continue to glob test_*.sh; new test will be picked up automatically)
    - assets/icon-256.png (path the README will reference)
  </read_first>
  <action>
Step 1 — rewrite `README.md` from scratch. The current 35-line stub gets fully replaced with this content (keeps content discipline of the original — no marketing fluff, just facts):

```markdown
# PurpleVoice

**Local voice dictation. Nothing leaves your Mac.**

![PurpleVoice](assets/icon-256.png)

A local, push-to-talk voice input system for Claude Code on macOS Apple Silicon. Hold a hotkey, speak, release — your transcript appears in the focused Claude Code terminal. Audio never leaves your Mac.

## Why PurpleVoice

Every other dictation tool we considered defaults to cloud or pushes you toward a subscription: Koe (cloud default), Wispr Flow (subscription cloud), SuperWhisper (cloud sync at any paid tier), the Mac's built-in Dictation when "Enhanced" is enabled. PurpleVoice runs whisper.cpp locally on the Metal backend. No network calls, no API keys, no quotas, no telemetry, no opt-in cloud features. The audio file is captured, transcribed locally, and deleted on every exit path. The transcript is pasted into the focused window. Everything stays on the Mac.

This is the trade we're making consciously: cloud dictation has bigger models, faster development, and arguably better accuracy on long-form English. PurpleVoice trades those for ownership.

## Status

Phase 2 hardening complete. Phase 2.5 branding complete (this commit). Working under the public name `PurpleVoice`; the working-name `voice-cc` is preserved in repo path and git history.

## Hotkey

`cmd+shift+e` (push-and-hold).

(Locked decision; see `.planning/phases/01-spike/01-CONTEXT.md` D-01. Hotkey was changed from the original combo during Plan 01-01 execution. Known minor conflict: VS Code/Cursor "Show Explorer" sidebar — accepted.)

## Setup

```bash
bash setup.sh
```

`setup.sh` is idempotent — safe to re-run. It:
- Installs Homebrew dependencies (Hammerspoon, sox, whisper-cpp)
- Migrates any existing `~/.config/voice-cc/`, `~/.local/share/voice-cc/`, `~/.cache/voice-cc/` directories to the `purplevoice` equivalents (one-time, idempotent)
- Creates the XDG directory layout: `~/.config/purplevoice/`, `~/.local/share/purplevoice/models/`, `~/.cache/purplevoice/`, `~/.local/bin/`, `~/.hammerspoon/purplevoice/`
- Downloads the Whisper `small.en` model with SHA256 verification (resumable)
- Downloads the Silero VAD weights (size sanity-checked)
- Seeds a default vocabulary file (`vocab.txt` — never clobbered on re-run)
- Installs the canonical hallucination denylist (always overwritten — project-owned)
- Creates symlinks: `~/.local/bin/purplevoice-record` and `~/.hammerspoon/purplevoice/`

After setup completes, paste this line into your `~/.hammerspoon/init.lua`:

```lua
require("purplevoice")
```

(If you previously had `require("voice-cc")`, replace it with the line above.)

## Permissions

PurpleVoice piggybacks on Hammerspoon's permissions — there's no separate `.app` for macOS to track.

- **Microphone** — System Settings → Privacy & Security → Microphone → enable Hammerspoon.app
- **Accessibility** — System Settings → Privacy & Security → Accessibility → enable Hammerspoon.app

Both are required. Hammerspoon prompts for them automatically. If the prompt was dismissed, PurpleVoice surfaces a follow-up notification with a deep link to the right Settings pane.

## Conflicting macOS feature to disable

Disable the macOS Dictation hotkey to avoid conflicts with `cmd+shift+e`:

- System Settings → Keyboard → Dictation → Shortcut → Off

## File layout (post-install)

```
~/.config/purplevoice/                vocab.txt + denylist.txt
~/.local/share/purplevoice/models/    ggml-small.en.bin (~488 MB) + ggml-silero-v6.2.0.bin (~885 KB)
~/.cache/purplevoice/                 (transient logs, cleaned by setup.sh / runtime)
~/.local/bin/purplevoice-record       symlink to repo
~/.hammerspoon/purplevoice/           symlink to repo
/tmp/purplevoice/                     transient WAVs (cleaned via EXIT trap on every exit)
```

## Architecture

Brief: Hammerspoon binds the hotkey, spawns `purplevoice-record` (bash) on press, sends SIGTERM on release. Bash glue runs sox to capture, whisper-cli to transcribe, prints the transcript on stdout. Hammerspoon writes the transcript to the clipboard with a transient UTI marker (so clipboard managers don't keep it), synthesises `cmd+v`, then restores the prior clipboard after 250 ms.

Full architecture: `.planning/research/ARCHITECTURE.md`. Stack details: `.planning/research/STACK.md`. Pitfalls catalogued: `.planning/research/PITFALLS.md`.
```

Step 2 — verify the README. After write:
- `grep -q "^# PurpleVoice" README.md` (H1 rebranded)
- `grep -q "Local voice dictation. Nothing leaves your Mac." README.md` (tagline placed)
- `grep -q "assets/icon-256.png" README.md` (icon referenced)
- `grep -q 'require("purplevoice")' README.md` (new require line documented)
- `! grep -q "^# voice-cc" README.md` (no leftover voice-cc H1)
- `! grep -q "/.config/voice-cc" README.md` (XDG paths migrated)
- `! grep -q "/.hammerspoon/voice-cc" README.md`

Step 3 — write `tests/test_brand_consistency.sh`. Write this content verbatim:

```bash
#!/usr/bin/env bash
# test_brand_consistency.sh — Phase 2.5 regression catch.
#
# Asserts that no `voice-cc` strings survive in user-facing surfaces after
# the rebrand. Allowed exceptions: anything under .planning/ (historical
# record, per CONTEXT.md D-07) and .git/ (history). The setup.sh migrate_xdg_dir
# FROM-arg lines deliberately retain `voice-cc` and are exempted by line pattern.
#
# Source: .planning/phases/02.5-branding/02.5-VALIDATION.md "Sampling Rate -> Full suite command".
set -euo pipefail
cd "$(dirname "$0")/.."   # repo root

# Find any code/doc file containing `voice-cc`, excluding the historical record.
HITS=$(grep -rln "voice-cc" \
  --include="*.lua" --include="*.sh" --include="*.md" --include="*.txt" \
  . 2>/dev/null \
  | grep -v "^\./.planning/" \
  | grep -v "^\./.git/" || true)

# Filter out setup.sh — its 3 migrate_xdg_dir FROM-args legitimately contain `voice-cc`.
# Verify those exact 3 occurrences and only those exist.
HITS=$(echo "$HITS" | grep -v "^\./setup\.sh$" || true)
SETUP_COUNT=$(grep -c '"\$HOME/[^"]*voice-cc' setup.sh 2>/dev/null || echo 0)

if [ "$SETUP_COUNT" -ne 3 ]; then
  echo "FAIL: setup.sh has $SETUP_COUNT voice-cc path strings (expected exactly 3 — the migrate_xdg_dir FROM args)"
  exit 1
fi

if [ -n "$HITS" ]; then
  echo "FAIL: unexpected voice-cc references in user-facing files:"
  echo "$HITS"
  exit 1
fi

echo "PASS: no voice-cc strings in user-facing surfaces (setup.sh migration FROM-args verified == 3)"
exit 0
```

Make it executable:
```bash
chmod +x tests/test_brand_consistency.sh
```

Step 4 — verify the test integrates with the suite. tests/run_all.sh globs `tests/test_*.sh` (line 18), so no edit needed there. Run:
```bash
bash tests/run_all.sh
```

Expected output: `Results: 7 passed, 0 failed` (the 6 existing tests + 1 new brand-consistency test, all GREEN).

If `tests/test_brand_consistency.sh` reports `FAIL: setup.sh has N voice-cc path strings (expected exactly 3 — the migrate_xdg_dir FROM args)`, that's a Plan 02 deviation: setup.sh either has more or fewer than 3 voice-cc FROM-arg references. Re-read setup.sh and verify the migration block has exactly 3 calls (config / data / cache).

If `tests/test_brand_consistency.sh` reports `FAIL: unexpected voice-cc references in user-facing files: ...`, the listed files contain voice-cc strings that should have been rebranded by Plans 01-03 — fix the offending file (likely a manual walkthrough doc) and re-run.

Step 5 — final overall scan:
- `bash tests/run_all.sh` exits 0 with 7 PASS / 0 FAIL.
- Pattern 2 invariant intact: `[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ]`.
- Brand surface verifications all pass:
  - `grep -q "PurpleVoice" .planning/PROJECT.md`
  - `grep -q "BRD-01" .planning/REQUIREMENTS.md`
  - `grep -q "Local voice dictation. Nothing leaves your Mac." README.md`
  - `grep -q "assets/icon-256.png" README.md`
  - `grep -q 'require("purplevoice")' README.md`
  - `grep -q '^\*\*PurpleVoice\*\*' CLAUDE.md`
  - `[ -x tests/test_brand_consistency.sh ]`
  </action>
  <verify>
    <automated>grep -q "^# PurpleVoice" README.md && grep -q "Local voice dictation. Nothing leaves your Mac." README.md && grep -q "assets/icon-256.png" README.md && grep -q 'require("purplevoice")' README.md && [ -x tests/test_brand_consistency.sh ] && bash tests/run_all.sh | grep -qE "Results: 7 passed, 0 failed"</automated>
  </verify>
  <acceptance_criteria>
    - [ ] `grep -q "^# PurpleVoice" README.md` (H1 rebranded)
    - [ ] `grep -q "Local voice dictation. Nothing leaves your Mac." README.md` (tagline directly under H1 per D-12)
    - [ ] `grep -q "assets/icon-256.png" README.md` (icon image referenced)
    - [ ] `grep -q 'require("purplevoice")' README.md` (new require line documented for users)
    - [ ] `! grep -q "/.config/voice-cc" README.md` (XDG paths migrated)
    - [ ] `[ -x tests/test_brand_consistency.sh ]` (test exists and is executable)
    - [ ] `bash tests/test_brand_consistency.sh` exits 0 standalone (PASS)
    - [ ] `bash tests/run_all.sh | grep -qE "Results: 7 passed, 0 failed"` (suite GREEN with the new test included)
    - [ ] `[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ]` (Pattern 2 still intact across all 4 plans)
  </acceptance_criteria>
  <done>
README.md is fully rebranded (H1 + tagline + icon + positioning + new XDG paths + new require line); `tests/test_brand_consistency.sh` exists, is executable, and PASSES; `tests/run_all.sh` reports 7 passed / 0 failed; all brand-surface grep predicates pass; Pattern 2 invariant still holds.
  </done>
</task>

</tasks>

<verification>
Plan-level verification (run after both tasks complete):

```bash
# PROJECT.md (BRD-01)
grep -q "PurpleVoice" .planning/PROJECT.md
grep -A5 "Name & Positioning" .planning/PROJECT.md | grep -qiE "(privacy|nothing leaves|on-device|local)"
grep -q "Local voice dictation. Nothing leaves your Mac." .planning/PROJECT.md

# REQUIREMENTS.md (formal BRD-01..03 + traceability)
grep -q "BRD-01" .planning/REQUIREMENTS.md
grep -q "BRD-02" .planning/REQUIREMENTS.md
grep -q "BRD-03" .planning/REQUIREMENTS.md
[ "$(grep -E 'BRD-0[123].*Phase 2.5' .planning/REQUIREMENTS.md | wc -l | tr -d ' ')" = "3" ]

# README.md (BRD-02 + BRD-03 reference)
grep -q "^# PurpleVoice" README.md
grep -q "Local voice dictation. Nothing leaves your Mac." README.md
grep -q "assets/icon-256.png" README.md
grep -q 'require("purplevoice")' README.md

# CLAUDE.md
grep -q "^\*\*PurpleVoice\*\*" CLAUDE.md

# tests/test_brand_consistency.sh + suite GREEN
[ -x tests/test_brand_consistency.sh ]
bash tests/run_all.sh | grep -qE "Results: 7 passed, 0 failed"

# Pattern 2 invariant intact across all 4 plans
[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ]
! grep -q "whisper-cli" purplevoice-lua/init.lua

# Final post-rebrand audit (the brand-consistency test should already cover this)
! grep -rln "voice-cc" --include="*.lua" --include="*.sh" --include="*.md" --include="*.txt" . \
  | grep -v "^\./.planning/" | grep -v "^\./.git/" | grep -v "^\./setup\.sh$" | grep .
```
</verification>

<success_criteria>
1. PROJECT.md (BRD-01): contains the name `PurpleVoice` + a Name & Positioning section with the privacy-first paragraph + the tagline + the marked-complete Active line.
2. REQUIREMENTS.md (BRD-01, BRD-02, BRD-03 closure): formal entries for all three new requirements, traceability table populated with 3 new rows mapping to Phase 2.5, coverage summary updated.
3. README.md (BRD-02 prose + BRD-03 reference): H1 = PurpleVoice, tagline directly under H1, icon referenced, positioning paragraph included, install narrative uses purplevoice paths, `require("purplevoice")` documented.
4. CLAUDE.md: 2 voice-cc references in current-product section updated to PurpleVoice / purplevoice.
5. `tests/test_brand_consistency.sh` exists, is executable, runs the canonical post-rebrand grep audit, and is picked up by `tests/run_all.sh` automatically.
6. `bash tests/run_all.sh` reports 7 PASS / 0 FAIL.
7. Pattern 2 invariant intact: `grep -c WHISPER_BIN purplevoice-record == 2`.
8. The final phase-wide audit `! grep -rln "voice-cc" --include="*.lua" --include="*.sh" --include="*.md" --include="*.txt" . | grep -v ".planning" | grep -v ".git" | grep -v "setup.sh"` returns 0 hits — no `voice-cc` survives in any user-facing surface other than the .planning/ historical record and the setup.sh migration block.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-branding/02.5-04-SUMMARY.md` documenting:
- PROJECT.md edits (lines changed; new Name & Positioning section content)
- REQUIREMENTS.md additions (BRD-01..03 verbatim text; 3 traceability rows; coverage summary update)
- README.md final structure + line count
- CLAUDE.md note about its embedded STACK.md block — flag that any future regeneration from upstream STACK.md would need separate updating (out of scope here; logged as Phase 3 follow-up)
- tests/test_brand_consistency.sh source + final 7/7 PASS test suite output
- Final post-rebrand audit: literal `grep -rln voice-cc ...` output showing only the expected exceptions (.planning/ + setup.sh migrate FROM-args)
- Note any minor deviations (e.g., if a manual walkthrough still had a stray voice-cc reference that needed an inline fix during this plan's execution)
</output>
