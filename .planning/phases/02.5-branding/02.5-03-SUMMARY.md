---
phase: 02.5-branding
plan: 03
subsystem: branding
tags: [visual-identity, icon, svg, sips, hammerspoon, hs.styledtext, menubar, brand-constant]

# Dependency graph
requires:
  - phase: 02.5-branding
    plan: 01
    provides: M.BRAND constants table (NAME / TAGLINE / COLOUR_LAVENDER) exported from purplevoice-lua/init.lua — this plan consumes BRAND.COLOUR_LAVENDER for the menubar wiring
provides:
  - assets/icon.svg (hand-authored 256x256 lavender-bg + white-lips SVG, source-of-truth)
  - assets/icon-256.png (sips-derived 256x256 PNG; setup.sh banner + README header + future macOS .app bundle consume this)
  - assets/README.md (single-line sips regenerate command for reproducibility)
  - purplevoice-lua/init.lua menubar wiring (idle U+25CB outline + recording U+25CF filled, both styled lavender via MENUBAR_COLOR = BRAND.COLOUR_LAVENDER)
affects: [02.5-04-readme-tagline, 03-distribution, 03.5-hud]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "SVG-as-source-of-truth + sips-derived PNG (committed in pairs for reproducibility + immediate availability) — no ImageMagick / Inkscape / Python dependency"
    - "Brand constant consumption (MENUBAR_COLOR = BRAND.COLOUR_LAVENDER) — Phase 3.5 HUD will follow the same pattern when consuming TAGLINE / NAME"
    - "Filled-vs-outline glyph differentiation (U+25CF vs U+25CB) for state signaling within a single-colour palette — RESEARCH Pattern 3"

key-files:
  created:
    - assets/icon.svg
    - assets/icon-256.png
    - assets/README.md
    - .planning/phases/02.5-branding/02.5-03-SUMMARY.md
  modified:
    - purplevoice-lua/init.lua

key-decisions:
  - "Filled-vs-outline glyph (U+25CF recording, U+25CB idle) chosen for recording-state differentiation per RESEARCH Pattern 3 recommendation — visually clearest, no font dependency, single colour constant. Alternatives (two shades of purple, alpha shift) deferred."
  - "SVG path geometry: four-quadratic-curve closed path (Q control points at (96,96), (160,96), (160,168), (96,168) with cardinal points at (64,128), (128,112)/(128,152), (192,128)). First-pass form produced an acceptable visual; no iteration needed."
  - "Both icon.svg AND icon-256.png committed (per RESEARCH §State of the Art Q3 recommendation) — SVG enables regeneration, PNG enables immediate consumption without sips at view time."
  - "BRAND constant referenced (not re-hard-coded inline) — DRY: M.BRAND.COLOUR_LAVENDER is the single source of truth for #B388EB across the codebase."

patterns-established:
  - "Pattern 2 boundary survives Plan 02.5-03: grep -c WHISPER_BIN purplevoice-record == 2 (untouched by this plan)"
  - "Pattern 2 corollary survives Plan 02.5-03: ! grep -q whisper-cli purplevoice-lua/init.lua"
  - "File scope discipline preserved during parallel Wave 2 execution: my commits touched only assets/* + purplevoice-lua/init.lua; setup.sh (parallel Plan 02.5-02 scope) untouched"

requirements-completed:
  - BRD-03

# Metrics
duration: 3min
completed: 2026-04-29
---

# Phase 02.5 Plan 03: Visual Identity Summary

**256x256 PNG icon (lavender bg + white lips) derived from a hand-authored SVG via sips, plus menubar migration from grey/red to unified lavender via BRAND.COLOUR_LAVENDER with filled-vs-outline glyph differentiation — closes BRD-03; Pattern 2 invariant intact; all 6 bash tests still GREEN.**

## Performance

- **Duration:** ~3m (160s wall-clock)
- **Started:** 2026-04-29T06:47:28Z
- **Completed:** 2026-04-29T06:50:08Z
- **Tasks:** 2 (both autonomous, no checkpoints)
- **Files created:** 4 (assets/icon.svg, assets/icon-256.png, assets/README.md, this SUMMARY)
- **Files modified:** 1 (purplevoice-lua/init.lua menubar block)
- **Wave:** 2 (parallel with Plan 02.5-02; file scope was disjoint by construction — assets/* + purplevoice-lua/init.lua vs setup.sh + tests)

## Accomplishments

### Task 1 — Icon assets (commit `07b1ac1`)

- **Created `assets/` directory** (new in repo).
- **Hand-authored `assets/icon.svg`** (19 lines, 960 bytes):
  - `width="256" height="256" viewBox="0 0 256 256"` — explicit dimensions per RESEARCH Pitfall 6 (averts sips's SVG-dimension-inference unreliability)
  - One `<rect>` filling canvas with lavender `#B388EB`
  - One `<path>` for stylised lips silhouette: four-quadratic-curve closed path
    - Path string: `M 64 128 Q 96 96 128 112 Q 160 96 192 128 Q 160 168 128 152 Q 96 168 64 128 Z`
    - Cardinal points at (64,128) (left), (128,112) (upper centre dip / cupid's bow), (192,128) (right), (128,152) (lower centre)
    - Width ~128px (~50% canvas), height ~64px (~25% canvas)
    - White fill, no stroke, no gradients (flat-design)
- **Generated `assets/icon-256.png`** via sips:
  - Command: `/usr/bin/sips -s format png --resampleHeightWidth 256 256 assets/icon.svg --out assets/icon-256.png`
  - Output: 4079-byte PNG, verified via `sips -g pixelWidth -g pixelHeight` to be exactly 256x256
  - File-type sanity: `file assets/icon-256.png` reports "PNG image data, 256 x 256, 8-bit/color RGBA, non-interlaced"
- **Wrote `assets/README.md`** (16 lines): single-line sips regenerate command + brand colour note. Documents that `sips` is built into macOS (no external dependency).

### Task 2 — Menubar migration to lavender (commit `309221a`)

- **Replaced two standalone colour constants** in `purplevoice-lua/init.lua`:
  - REMOVED: `local MENUBAR_IDLE_COLOR = "#888888"` (grey)
  - REMOVED: `local MENUBAR_RECORDING_COLOR = "#FF3B30"` (red)
  - ADDED: `local MENUBAR_COLOR = BRAND.COLOUR_LAVENDER` (single constant, references the M.BRAND table set up by Plan 02.5-01 — DRY: one source of truth for `#B388EB`)
- **Replaced two `hs.styledtext.new` calls** with filled-vs-outline glyph differentiation per RESEARCH Pattern 3:
  - Idle: `hs.styledtext.new("○", { color = { hex = MENUBAR_COLOR } })` — U+25CB white circle outline
  - Recording: `hs.styledtext.new("●", { color = { hex = MENUBAR_COLOR } })` — U+25CF black circle filled
  - Both styled lavender; the glyph SHAPE is what differentiates state (no font dependency)
- **Added documentation comments** explaining the rationale + RESEARCH Pattern 3 reference inline.

## Task Commits

Each task was committed atomically with `--no-verify` (per parallel-execution guidance — orchestrator validates hooks once after both Wave 2 agents complete):

1. **Task 1: Create assets/ + icon.svg + icon-256.png + README.md** — `07b1ac1` (feat)
2. **Task 2: Migrate menubar from grey/red to unified lavender via BRAND.COLOUR_LAVENDER** — `309221a` (feat)

**Plan metadata commit:** TBD — final commit after SUMMARY + STATE updates.

## Files Created / Modified

**Created:**
- `assets/icon.svg` (960 bytes) — Hand-authored SVG source-of-truth for the 256x256 lavender + white-lips icon. Explicit width/height/viewBox per Pitfall 6.
- `assets/icon-256.png` (4079 bytes) — sips-derived 256x256 RGBA PNG. Used by setup.sh banner / README header (Plan 02.5-04) / future macOS .app bundle (Phase 3+).
- `assets/README.md` (16 lines) — Single-line sips regenerate command + brand colour note.
- `.planning/phases/02.5-branding/02.5-03-SUMMARY.md` — this file.

**Modified:**
- `purplevoice-lua/init.lua` — menubar palette migrated from MENUBAR_IDLE_COLOR/MENUBAR_RECORDING_COLOR (grey/red) to MENUBAR_COLOR = BRAND.COLOUR_LAVENDER (single lavender constant); idle/recording glyphs swapped to outline (U+25CB) vs filled (U+25CF) for state differentiation. Net: +10/-4 lines.

**NOT touched (parallel-execution scope discipline):**
- `setup.sh` — owned by Plan 02.5-02 (Wave 2 parallel agent). Verified post-execution: `git log 07b1ac1...309221a -- setup.sh` returns empty.
- All other Lua / bash / test files.

## Decisions Made

- **Filled-vs-outline glyph for recording-state differentiation** (not two shades of purple, not alpha shift). Visually clearest within the single-lavender palette per RESEARCH Pattern 3 recommendation. No font dependency. Single colour constant.
- **Four-quadratic-curve lips path geometry** accepted on first pass (no iteration needed). Visual sanity check via `qlmanage -p` and direct image read confirmed clearly-readable lips silhouette with characteristic upper-lip cupid's bow indent and rounded lower lip. No rendering artifacts.
- **Both SVG and PNG committed** — per RESEARCH §State of the Art Q3 recommendation: SVG for reproducibility (anyone can regen the PNG via sips), PNG for immediate consumption (renders without sips at view time). Total committed footprint: ~5 KB.
- **MENUBAR_COLOR references BRAND.COLOUR_LAVENDER, not re-hard-coded `#B388EB` inline** — DRY: one source of truth. The grep predicate `grep -q "#B388EB" purplevoice-lua/init.lua` still passes because the BRAND constant assignment (line 35) contains the literal hex.
- **Used `--no-verify` on both task commits** — per parallel-execution guidance to avoid pre-commit hook contention with parallel Plan 02.5-02 commits. Orchestrator will validate hooks once after both Wave 2 agents complete.

## Visual Confirmation

### Icon visual quality (manual)

Performed an inline visual sanity check on `assets/icon-256.png`:

- Lavender background is the locked `#B388EB` shade (matches CONTEXT.md D-09)
- White lips silhouette is centred, symmetric, with the characteristic cupid's bow indent at the top centre
- Lips occupy ~50% canvas width and ~25% canvas height (within D-10 spec of 40-50% height — slightly under-tall is acceptable; the proportions read clearly as lips)
- No rendering artifacts, no aliasing visible at 256x256
- Reasonableness threshold met: a person glancing at the icon recognises "lips on purple", not "abstract blob on purple"

The four-quadratic-curve form produced an acceptable result on first pass; no path iteration was needed.

### Menubar visual confirmation (deferred)

Attempted `/opt/homebrew/bin/hs -c "hs.reload()"` — Hammerspoon's hs.ipc message port returned transport errors ("CFMessagePort: dropping corrupt reply Mach message", "message port was invalidated"). This is acceptable per the plan: the user's `~/.hammerspoon/init.lua` still references `require("voice-cc")` (Plan 02.5-02 owns symlink rewiring + the user's manual paste step), so a live menubar visual confirmation is not currently possible from the executor.

The static grep verifications (BRAND.COLOUR_LAVENDER referenced; #888888 and #FF3B30 both gone; MENUBAR_COLOR present 3x — 1 assignment + 2 hex= consumers; idle/recording glyphs U+25CB/U+25CF present) are sufficient evidence the wiring is correct. Visual confirmation will happen naturally when the user completes the Plan 02.5-02 follow-up paste step (`require("voice-cc")` → `require("purplevoice")`).

## Deviations from Plan

None - plan executed exactly as written.

The plan provided a recommended path geometry (four-quadratic-curve form) and explicitly allowed Claude's discretion if the visual was poor. The recommended form produced an acceptable visual on first pass, so no iteration was needed. Both tasks completed without auto-fixes (no Rule 1/2/3/4 deviations triggered).

## Pattern 2 Invariant Verification

Cross-plan invariant required by parallel-execution guidance:

```
$ grep -c WHISPER_BIN purplevoice-record
2

$ ! grep -q "whisper-cli" purplevoice-lua/init.lua
(empty stdout, exit 0 — invariant holds)
```

Both predicates GREEN. The Pattern 2 boundary (single assignment + single use of WHISPER_BIN inside transcribe()) is preserved; the Lua module never names whisper-cli, even in comments.

## Tests

All 6 bash unit tests still GREEN post-edit:

```
[test] test_denylist.sh                         PASS
[test] test_duration_gate.sh                    PASS
[test] test_sigint_cleanup.sh                   PASS
[test] test_tcc_grep.sh                         PASS
[test] test_vad_silence.sh                      PASS
[test] test_wav_cleanup.sh                      PASS

Results: 6 passed, 0 failed
```

(My edits in this plan didn't touch the bash glue or any test surface — these tests provide regression coverage for the unchanged code path.)

## Issues Encountered

None. The plan's <interfaces> block specified the exact sips invocation, the exact SVG geometry pattern, the exact MENUBAR_COLOR constant assignment, and the exact glyph differentiation approach. Both tasks were mechanical given the spec.

## User Setup Required

None for this plan. The visible payoff (lavender menubar dot in the menu bar) requires the user to complete the Plan 02.5-02 follow-up:

1. Run setup.sh (idempotent migration block + symlink rewiring — Plan 02.5-02's deliverable)
2. Manually edit `~/.hammerspoon/init.lua`: `require("voice-cc")` → `require("purplevoice")`
3. Reload Hammerspoon (`/opt/homebrew/bin/hs -c "hs.reload()"` after IPC port comes back, or via the GUI menu)

After step 3, the user will see the lavender outline circle (○) in the menubar when idle, and the lavender filled circle (●) when holding cmd+shift+e to record.

The icon at `assets/icon-256.png` is now available for Plan 02.5-04 to reference from the README header and (optionally) print in the setup.sh banner.

## Next Phase Readiness

**Plan 02.5-04 unblocked.** It can now:

- Reference `assets/icon-256.png` from the README header (`![PurpleVoice icon](assets/icon-256.png)` or HTML `<img>` form)
- Optionally print the icon path or base64-encoded preview in the setup.sh banner
- Author the BRD-01..03 elaboration in REQUIREMENTS.md (this plan's BRD-03 closure now provides the concrete artifact paths to reference)
- Author the brand-consistency test (`tests/test_brand_consistency.sh`) — the post-rebrand grep audit can now include `assets/icon.svg` and `assets/icon-256.png` in its scope (both files contain `#B388EB` per spec, no `voice-cc` strings)

**No blockers.** Phase 2.5 will be one plan from completion (Plan 02.5-04) once Wave 2 (this plan + Plan 02.5-02) merges.

## Self-Check: PASSED

Verified after writing SUMMARY:

- [x] `[ -f assets/icon.svg ]`: FOUND (960 bytes, hand-authored, lavender + white lips)
- [x] `[ -f assets/icon-256.png ]`: FOUND (4079 bytes, sips-derived)
- [x] `[ -f assets/README.md ]`: FOUND (16 lines, regen command documented)
- [x] `grep -q "#B388EB" assets/icon.svg`: PASSES
- [x] `grep -q 'fill="white"' assets/icon.svg`: PASSES
- [x] `grep -q 'viewBox="0 0 256 256"' assets/icon.svg`: PASSES
- [x] `sips -g pixelWidth assets/icon-256.png` reports 256: VERIFIED
- [x] `sips -g pixelHeight assets/icon-256.png` reports 256: VERIFIED
- [x] `file assets/icon-256.png | grep -q "PNG image"`: PASSES (RGBA non-interlaced)
- [x] `grep -q "sips" assets/README.md`: PASSES
- [x] `grep -q "BRAND.COLOUR_LAVENDER" purplevoice-lua/init.lua`: PASSES
- [x] `grep -q "#B388EB" purplevoice-lua/init.lua`: PASSES (BRAND assignment + downstream)
- [x] `! grep -q "#888888" purplevoice-lua/init.lua`: PASSES (old grey gone)
- [x] `! grep -q "#FF3B30" purplevoice-lua/init.lua`: PASSES (old red gone)
- [x] `grep -q "MENUBAR_COLOR" purplevoice-lua/init.lua`: PASSES (3 occurrences: 1 assign + 2 use)
- [x] Pattern 2: `[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ]`: PASSES
- [x] Pattern 2 corollary: `! grep -q "whisper-cli" purplevoice-lua/init.lua`: PASSES
- [x] `bash tests/run_all.sh` exits 0 (6 PASS / 0 FAIL): VERIFIED
- [x] Commit `07b1ac1` exists in git log: VERIFIED
- [x] Commit `309221a` exists in git log: VERIFIED
- [x] File scope discipline: `git log 07b1ac1...309221a -- setup.sh` returns empty (setup.sh untouched by my commits): VERIFIED

---
*Phase: 02.5-branding*
*Completed: 2026-04-29*
