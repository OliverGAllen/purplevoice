---
phase: 02.5
slug: branding
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-29
---

# Phase 02.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: derived from `02.5-RESEARCH.md` §"Validation Architecture".

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | bash + grep (existing test suite) |
| **Config file** | `tests/run_all.sh` (39 lines, exec) |
| **Quick run command** | `bash tests/run_all.sh` (executes 6 unit tests, <30s) |
| **Full suite command** | `bash tests/run_all.sh && bash tests/test_brand_consistency.sh` (when Plan 02.5-04 lands) |
| **Estimated runtime** | ~30s (full suite + brand audit) |

---

## Sampling Rate

- **After every task commit:** Run `bash tests/run_all.sh`
- **After every plan wave:** Run `bash tests/run_all.sh` + post-rebrand grep audit (no `voice-cc` strings in code surfaces; preserved in `.planning/`)
- **Before `/gsd:verify-work`:** Full suite GREEN + manual Hammerspoon reload test (cmd+shift+e → transcript paste verifies the renamed module loads cleanly)
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

> Plan-and-task IDs are TBD until the planner generates plans. The verification predicates below are pre-mapped from RESEARCH.md and become per-task `<automated>` verify clauses when the planner assigns them.

| Req | Behavior | Test Type | Automated Command | Wave |
|-----|----------|-----------|-------------------|------|
| BRD-01 | Name `PurpleVoice` recorded in PROJECT.md | unit | `grep -q "PurpleVoice" .planning/PROJECT.md` | 4 |
| BRD-01 | Privacy positioning paragraph present in PROJECT.md | unit | `grep -A2 "PurpleVoice" .planning/PROJECT.md \| grep -qiE "(local\|on-device\|privacy\|nothing leaves)"` | 4 |
| BRD-02 | Zero `voice-cc` strings in non-`.planning` source files | unit | `! grep -rln "voice-cc" --include="*.lua" --include="*.sh" --include="*.md" --include="*.txt" . \| grep -v ".planning" \| grep -v ".git" \| grep .` | 4 |
| BRD-02 | All 5 notification titles use `PurpleVoice:` prefix | unit | `[ "$(grep -c "PurpleVoice:" purplevoice-lua/init.lua)" -ge 5 ]` | 1 |
| BRD-02 | Module load alert says `PurpleVoice loaded` | unit | `grep -q 'PurpleVoice loaded' purplevoice-lua/init.lua` | 1 |
| BRD-02 | `purplevoice-record` exists and is executable | unit | `[ -x purplevoice-record ]` | 1 |
| BRD-02 | `purplevoice-lua/init.lua` exists | unit | `[ -f purplevoice-lua/init.lua ]` | 1 |
| BRD-02 | `.hammerspoon-init-snippet.lua` shows `require("purplevoice")` | unit | `grep -q 'require("purplevoice")' .hammerspoon-init-snippet.lua` | 1 |
| BRD-02 | setup.sh migration block present for 3 XDG dirs | unit | `[ "$(grep -c "migrate_xdg_dir\|migrate_voice_cc" setup.sh)" -ge 3 ]` | 2 |
| BRD-02 | setup.sh banner contains tagline | unit | `grep -q "Local voice dictation. Nothing leaves your Mac." setup.sh` | 4 |
| BRD-02 | README.md header contains tagline | unit | `grep -q "Local voice dictation. Nothing leaves your Mac." README.md` | 4 |
| BRD-02 | Pattern 2 boundary preserved in renamed file | unit | `[ "$(grep -c WHISPER_BIN purplevoice-record)" = "2" ] && ! grep -q whisper-cli purplevoice-lua/init.lua` | 1 |
| BRD-02 | All 6 existing bash unit tests still GREEN after rebrand | integration | `bash tests/run_all.sh` exits 0 | 1, 2, 4 |
| BRD-02 | hs.notify orphaned-tag cleanup present | unit | `grep -q "voiceccOpenMicSettings" purplevoice-lua/init.lua && grep -q "hs.notify.unregister" purplevoice-lua/init.lua` | 1 |
| BRD-03 | `assets/icon-256.png` exists | unit | `[ -f assets/icon-256.png ]` | 3 |
| BRD-03 | Icon is 256×256 PNG | unit | `sips -g pixelWidth -g pixelHeight assets/icon-256.png \| grep -qE "pixelWidth: 256" && sips -g pixelWidth -g pixelHeight assets/icon-256.png \| grep -qE "pixelHeight: 256"` | 3 |
| BRD-03 | Icon source SVG exists for reproducibility | unit | `[ -f assets/icon.svg ]` | 3 |
| BRD-03 | Menubar uses lavender `#B388EB` | unit | `grep -q "#B388EB" purplevoice-lua/init.lua` | 3 |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

The post-rebrand grep audit can be codified for Phase 3 regression-catch:

- [ ] `tests/test_brand_consistency.sh` — 8-line script wrapping the post-rebrand grep audit (no `voice-cc` strings outside `.planning/`/`.git/`). Runs as part of `tests/run_all.sh`. Plan 02.5-04 task. Optional but recommended.

No new test framework needed — bash + grep covers all 18 verifications. Existing `tests/run_all.sh` continues to run; rebrand validations are inline `<automated>` predicates in plan tasks.

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Hammerspoon load test after rename | BRD-02 | Live OS-level reload behaviour cannot be grep-verified — the renamed module must actually load and bind cmd+shift+e without orphan-callback warnings | (1) edit `~/.hammerspoon/init.lua` from `require("voice-cc")` to `require("purplevoice")`; (2) run `/opt/homebrew/bin/hs -c "hs.reload()"`; (3) check `hs.console.getConsole()` for clean load (no errors, hotkey enabled message); (4) hold cmd+shift+e, speak "rebrand verification test", release; (5) confirm transcript pastes |
| Icon visual quality | BRD-03 | Aesthetic judgement (lip silhouette proportions, colour balance) cannot be automated | Open `assets/icon-256.png` in Preview; verify lavender background + centred white lips; reasonable visual weight; no compression artifacts |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify clauses or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references (`assets/` directory, brand-consistency test)
- [ ] No watch-mode flags in any verify command
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter (after planner uses these predicates and checker verifies)

**Approval:** pending
