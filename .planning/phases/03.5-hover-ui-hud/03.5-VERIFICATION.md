---
phase: 03.5-hover-ui-hud
verified: 2026-04-30T13:30:00Z
status: passed
score: 4/4 success criteria met; 4/4 HUD requirements complete; all invariants intact
re_verification: false
---

# Phase 3.5: Hover UI / HUD — Verification Report

**Phase Goal:** A small floating HUD that surfaces voice-cc state at-a-glance — visible "● Recording" indicator while the hotkey is held, hidden when idle. Complements (does NOT replace) Phase 2's menubar indicator. Optimised for low CPU when idle and zero distraction when not actively recording. Must not steal focus, must not interfere with paste, hidden from screen recordings by default — REFRAMED per RESEARCH Priority 2: PurpleVoice does NOT pursue NSWindowSharingNone exclusion (ScreenCaptureKit on macOS 15+ ignores it).

**Verified:** 2026-04-30
**Status:** passed
**Re-verification:** No (initial)

---

## Goal Achievement

### Success Criteria — Per-Criterion Verdict

| # | Success Criterion | Verdict | Evidence |
|---|---|---|---|
| C1 | Floating HUD with "● Recording" affordance using lavender brand from Phase 2.5 — appears on press | ✓ MET | `purplevoice-lua/init.lua` lines 244-273 — `hs.canvas.new` instance with rectangle (HUD_W=140, HUD_H=36, roundedRectRadii xR/yR=18, fillColor `{ hex = BRAND.COLOUR_LAVENDER, alpha = 0.70 }`) + text element `hs.styledtext.new("● Recording", {color={white=1,alpha=1}, font={name=".AppleSystemUIFont",size=14}, paragraphStyle={alignment="center"}})` via `appendElements`. `BRAND.COLOUR_LAVENDER` is `#B388EB` (line 35) inherited from Phase 2.5. Lifecycle wired in `onPress` line 477 via `pcall(showHUD)`. Live walkthrough A signed off "all working" (Plan 01 commit `aebd505` + alpha tuning `932ca65`). |
| C2 | HUD disappears within ~250ms of release; zero CPU when idle | ✓ MET | `hideHUD()` lines 292-295 calls `hudCanvas:hide(0.15)` → 150ms perceptual fade-out, well under 250ms budget. `resetState()` line 327 invokes `pcall(hideHUD)` on every exit path (success / cancel / error), symmetric with `setMenubarIdle()`. Idle CPU: hidden `hs.canvas` orders the underlying NSWindow out (`[NSWindow orderOut:]`); no `hs.timer.doEvery`/animation loop in code path. `wantsLayer(true)` at line 247 engages Core Animation GPU compositing during visible state (Pitfall 5). `tests/manual/test_hud_idle_cpu.md` documentation walkthrough signed off 2026-04-30 — user confirmed "all complete" after `top -pid` baseline check (no measurable bump). |
| C3 | `PURPLEVOICE_HUD_OFF=1` disables; menubar still ships | ✓ MET | Line 131: `local hudEnabled = (os.getenv("PURPLEVOICE_HUD_OFF") ~= "1")` — D-09 default-ON / opt-out idiom mirrors `PURPLEVOICE_NO_SOUNDS` at line 303. Line 243: `if hudEnabled then` gates canvas allocation; when `PURPLEVOICE_HUD_OFF=1`, `hudCanvas` stays nil and `showHUD/hideHUD` become no-ops via `if not hudCanvas then return end` guards (lines 284, 293). Menubar code at lines 96-110 is independent — `setMenubarIdle/setMenubarRecording` are NOT gated on `hudEnabled`. Live walkthrough B (Plan 03 sign-off 2026-04-30): user quit Hammerspoon + relaunched with env var set; verified `_G._purplevoice_hud == nil` and HUD did not appear; menubar still flips ○↔●; transcript still pastes. |
| C4 | HUD does not steal focus; does not interfere with paste; honest framing for screen-recording limitation per Priority 2 | ✓ MET | Focus: `hs.canvas` defaults `ignoresMouseEvents=YES` and `canBecomeKeyWindow=NO` (verified in libcanvas.m source per RESEARCH Priority 3). `:behaviorAsLabels({"canJoinAllSpaces", "stationary", "transient"})` at line 246. Live walkthrough C: focus stays in editor; HUD absent from cmd+tab + Mission Control. Paste: HUD code is purely additive — `pasteWithRestore()` (lines 376-409) and `hs.eventtap.keyStroke({"cmd"}, "v")` flow are untouched; functional suite (`test_clipboard_restore`-equivalent regressions in 10/0 GREEN) confirms no regression. Honest framing: `! grep -q "_asm.objc\|setSharingType" purplevoice-lua/init.lua` (PASS — no Lua-ObjC bridge attempted). README §"HUD privacy and screen-recording visibility" + SECURITY.md §"HUD visibility in screen recordings" + REQUIREMENTS.md HUD-04 all use **"does not pursue NSWindowSharingNone exclusion"** verbatim and cite Apple Forums thread 792152. PURPLEVOICE_HUD_OFF=1 documented as "the only privacy guarantee" in both surfaces. |

**Score:** 4/4 success criteria met.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `purplevoice-lua/init.lua` | HUD canvas creation + show/hide + lifecycle wired into onPress/resetState | ✓ VERIFIED | 548 lines (was 343 pre-phase). HUD block at lines 38-295 + lifecycle hooks at lines 327, 477. Substantive (non-stub), wired (callsites confirmed), data flowing (uses live env vars + screen frames per press). |
| `setup.sh` | HUD options banner line under existing tagline | ✓ VERIFIED | Line 322 (inside heredoc — verified via `awk '/cat <<.EOF./,/^EOF$/' setup.sh \| grep -q "HUD options"`): `HUD options: PURPLEVOICE_HUD_OFF=1 to disable, PURPLEVOICE_HUD_POSITION=top-right (or top-center, bottom-center, bottom-right, near-cursor, center)`. D-12 honoured. |
| `README.md` | `### HUD privacy and screen-recording visibility` H3 inside `## Security & Privacy` | ✓ VERIFIED | Lines 128-143. Honest framing per RESEARCH Priority 2 ("does not pursue NSWindowSharingNone"); Apple Forums citation; named tools (QuickTime, OBS, Zoom share-screen, Discord, Loom, Microsoft Teams); env var config table; launchctl + quit-and-relaunch caveat. |
| `SECURITY.md` | `### HUD visibility in screen recordings` H3 inside `## How to Verify These Claims` | ✓ VERIFIED | Lines 753-757 inside H2 §"How to Verify These Claims" (line 701). H2 count remains 18 (no new H2 added — H3 nested under existing H2 per RESEARCH Open Question §2). |
| `.planning/REQUIREMENTS.md` | HUD-01..04 promoted to `[x]` Complete with concrete language | ✓ VERIFIED | Lines 71-74 (HUD-01..04, all `[x]`); traceability rows 157-160 (Phase 3.5 Complete); coverage 39/39; per-phase counts row for Phase 3.5. |
| `tests/test_hud_env_off.sh` | HUD-02 string-level env-var wiring + brand/Pattern2 corollary | ✓ VERIFIED | 75 lines; runs GREEN. |
| `tests/test_hud_position_validation.sh` | HUD-04 six-position list + console fallback + near-cursor clamp | ✓ VERIFIED | 142 lines; full positive assertions wired (env-var read, six positions, validPositions table, hs.console.printStyledtext, math.max/math.min/hs.mouse.absolutePosition). Runs GREEN. |
| `tests/manual/test_hud_*.md` (5 walkthroughs) | All present with sign-off blocks | ✓ VERIFIED | All 5 files present; each ends with "## Sign-off (Phase 3.5 close 2026-04-30)" block referencing Plan 01/02 commits + tester=Oliver Allen + result=PASS. See Live Walkthrough Sign-Off Audit below. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `init.lua` HUD block | `BRAND.COLOUR_LAVENDER` constant (Phase 2.5 export) | `BRAND.COLOUR_LAVENDER` literal in fillColor | ✓ WIRED | Line 259 reads `BRAND.COLOUR_LAVENDER` (line 35); lavender brand inherited from Phase 2.5. |
| `onPress` callback | `showHUD()` | `pcall(showHUD)` alongside `setMenubarRecording()` | ✓ WIRED | Lines 476-478: `setMenubarRecording()` → `pcall(showHUD)` → `playStartCue()`. Order matches D-16 lifecycle pattern. `pcall` ensures HUD failure does not block recording. |
| `resetState()` | `hideHUD()` | `pcall(hideHUD)` alongside `setMenubarIdle()` | ✓ WIRED | Lines 327-328: `pcall(hideHUD)` → `setMenubarIdle()`. Symmetric mirror of onPress. |
| Module load | env-var reads | `os.getenv("PURPLEVOICE_HUD_OFF")` + `os.getenv("PURPLEVOICE_HUD_POSITION")` | ✓ WIRED | Lines 131-132 read both env vars at module load (matches D-11). Validation table at 134-141; fallback warning at 142-146. |
| `showHUD()` | `positionFor()` + `activeScreen()` | per-press resolution | ✓ WIRED | Lines 284-289: resolves `activeScreen()` → `screen:fullFrame()` → `positionFor(hudPosition, screenFrame)` → `hudCanvas:topLeft(pos)` → `:show(0)`. Per-press resolution honours D-06 (multi-monitor active screen). |
| `_G._purplevoice_hud` | reload-time canvas cleanup | `pcall(function() _G._purplevoice_hud:delete() end)` at module top + assignment after creation | ✓ WIRED | Lines 155-158 (cleanup) + 272 (re-assignment). Defends against Hammerspoon reload-time ghost-pill (Pitfall 6). |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| HUD canvas pill | `hudPosition` + `screenFrame` | `os.getenv` + `hs.window.focusedWindow():screen():fullFrame()` per press | Yes — live screen geometry, recomputed per press | ✓ FLOWING |
| Position dispatch | `validPositions[hudPosition]` | env-var name validated against locked dict | Yes — six concrete branches with arithmetic; no static fallback (top-center is documented default, not a stub) | ✓ FLOWING |
| Menubar lifecycle (independent) | `isRecording` boolean | onPress/onRelease/resetState transitions | Yes — pre-existing Phase 2 wiring; HUD does not interfere | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|---|---|---|---|
| Functional suite passes | `bash tests/run_all.sh` | 10 passed, 0 failed (8 pre-existing + test_hud_env_off + test_hud_position_validation) | ✓ PASS |
| Security suite passes | `bash tests/security/run_all.sh` | 5 passed, 0 failed (all Phase 2.7 verify_*.sh GREEN) | ✓ PASS |
| Pattern 2 invariant | `grep -c WHISPER_BIN purplevoice-record` | `2` | ✓ PASS |
| Pattern 2 corollary | `! grep -q whisper-cli purplevoice-lua/init.lua` | exits 0 (not present) | ✓ PASS |
| No Lua-ObjC bridge | `! grep -qE "_asm\.objc\|setSharingType" purplevoice-lua/init.lua` | exits 0 (not present) | ✓ PASS |
| No GitHub Actions workflows | `! [ -d .github/workflows ]` | exits 0 (no workflows dir; D-04 release-gate cadence honoured) | ✓ PASS |
| SECURITY.md framing lint | `bash tests/test_security_md_framing.sh` | PASS — no banned phrases | ✓ PASS |
| Brand consistency | `bash tests/test_brand_consistency.sh` | PASS — no new voice-cc strings | ✓ PASS |
| SECURITY.md H2 count | `grep -c "^## " SECURITY.md` | 18 (HUD content went into existing H2 as H3, not new H2) | ✓ PASS |
| HUD options inside heredoc | `awk '/cat <<.EOF./,/^EOF$/' setup.sh \| grep -q "HUD options"` | exit 0 — line is INSIDE banner heredoc | ✓ PASS |
| HUD lifecycle callsites present | `grep -nE "showHUD\|hideHUD" purplevoice-lua/init.lua` | 6 hits: definitions at 283/292 + callsites at 327 (resetState) and 477 (onPress) | ✓ PASS |

---

## Requirements Coverage

| Requirement | Source Plan(s) | Description | Status | Evidence |
|---|---|---|---|---|
| HUD-01 | 03.5-01 (closure); 03.5-00 (stub) | Floating canvas widget appears within ~50ms of press, disappears within ~250ms of release. Lavender pill (~140×36, alpha 0.70 without backdrop blur, white "● Recording" text) | ✓ SATISFIED | `init.lua` lines 244-273 (canvas creation) + 283-289 (`:show(0)` instant) + 292-294 (`:hide(0.15)` = 150ms < 250ms). Lifecycle in onPress/resetState. Walkthrough A signed off live. |
| HUD-02 | 03.5-02 (env-var); 03.5-03 (live walkthrough) | `PURPLEVOICE_HUD_OFF=1` disables HUD; read at module load; reload Hammerspoon to apply | ✓ SATISFIED | `init.lua` line 131 reads env var; line 243 gates canvas allocation; `if not hudCanvas then return end` guards in showHUD/hideHUD. `tests/test_hud_env_off.sh` GREEN. Walkthrough B signed off live (quit + relaunch). |
| HUD-03 | 03.5-01 (canvas hide); 03.5-03 (CPU walkthrough) | Effectively zero CPU when idle; no animation loops; canvas hidden via `:hide(0.15)`; only redraws on state change | ✓ SATISFIED | `init.lua` line 247 `:wantsLayer(true)` (Core Animation GPU compositing); no `hs.timer.doEvery`/animation loops in module; `:hide(0.15)` orders the NSWindow out. Walkthrough D documented signed off 2026-04-30. |
| HUD-04 | 03.5-01 (focus); 03.5-02 (position); 03.5-03 (recordings) | Six named positions; no focus stealing; no paste interference; honest screen-recording framing per RESEARCH Priority 2 | ✓ SATISFIED | `init.lua` lines 178-231 (`positionFor()` six branches + near-cursor clamp); `hs.canvas` defaults (ignoresMouseEvents=YES, canBecomeKeyWindow=NO); `:behaviorAsLabels({"canJoinAllSpaces","stationary","transient"})` line 246. README + SECURITY.md + REQUIREMENTS.md all use **"does not pursue NSWindowSharingNone exclusion"** with Apple Forums 792152 citation. Walkthroughs C (focus, live) + E (recordings, doc) signed off. |

**Coverage:** 4/4 HUD requirements complete. No orphaned requirements (REQUIREMENTS.md traceability rows 157-160 match the 4 plans' frontmatter).

---

## Invariants Check

| Invariant | Expected | Observed | Status |
|---|---|---|---|
| Pattern 2 invariant | `grep -c WHISPER_BIN purplevoice-record` == 2 | 2 | ✓ INTACT |
| Pattern 2 corollary | `! grep -q whisper-cli purplevoice-lua/init.lua` | not present | ✓ INTACT |
| Functional suite | 10/0 (8 prior + 2 HUD) | 10 passed, 0 failed | ✓ INTACT |
| Security suite | 5/0 (Phase 2.7 unaffected) | 5 passed, 0 failed | ✓ INTACT |
| Framing lint | GREEN (no banned phrases in SECURITY.md) | PASS | ✓ INTACT |
| Brand consistency | GREEN (no new voice-cc strings) | PASS | ✓ INTACT |
| SECURITY.md H2 count | exactly 18 | 18 | ✓ INTACT |
| No Lua-ObjC bridge | `! grep -q "_asm.objc\|setSharingType" purplevoice-lua/init.lua` | not present | ✓ INTACT |
| No GitHub Actions | `.github/workflows/` does not exist | does not exist (D-04 honoured) | ✓ INTACT |

All 9 invariants intact.

---

## 12 Pitfalls Coverage Audit

| # | Pitfall | Coverage | Evidence |
|---|---|---|---|
| 1 | "Best-effort" screen-recording shield (over-claim) | ✓ ADDRESSED | D-14 reframed per Priority 2. README L132, SECURITY.md L755, REQUIREMENTS.md L74 all use "does not pursue NSWindowSharingNone exclusion" + Apple Forums 792152 citation. Banned phrases ("guarantees absolute", "always hidden", "fully hidden", "prevents capture") absent. |
| 2 | Using `hs.drawing` instead of `hs.canvas` | ✓ ADDRESSED | `hs.canvas.new` used at line 244; no new `hs.drawing` usage in HUD code. `hs.alert` legacy use at line 545 untouched (pre-existing). |
| 3 | Canvas-level vs element-level alpha (double-composite) | ✓ ADDRESSED (variant) | Per commit `932ca65`: alpha moved from canvas-level to `rectangle.fillColor.alpha = 0.70` (line 259); text element uses opaque white (line 265 `color = { white = 1, alpha = 1 }`). Resolves the same root concern (single source of truth, no haze) — user-tuned approach validated live ("looks good"). Comment at lines 248-251 documents the divergence from RESEARCH recommendation. |
| 4 | Show/hide race during rapid press | ✓ ADDRESSED | `isRecording` re-entrancy guard (line 90 + 472-474) silently drops rapid double-press at the recording layer; HUD lifecycle gated to the same boundary. `:hide(0.15)` adequate per RESEARCH "don't over-engineer for v1". |
| 5 | Forgetting `wantsLayer(true)` → CPU pegged | ✓ ADDRESSED | Line 247: `:wantsLayer(true)`. Engages Core Animation GPU compositing. |
| 6 | macOS Sequoia hot-reload ghost canvas | ✓ ADDRESSED | Lines 155-158: `if _G._purplevoice_hud then pcall(:delete()) ; nil end` + line 272 re-assignment. Defensive cleanup explicit. |
| 7 | Position arithmetic forgetting global coordinates | ✓ ADDRESSED | All six branches in `positionFor()` (lines 178-231) add `screenFrame.x`/`screenFrame.y` to computed offsets. Multi-monitor verified live in Plan 02 sign-off (math correct on Screen 2 at x=2242, y=132). |
| 8 | `hs.styledtext` font key spelling | ✓ ADDRESSED | Line 264: `font = { name = ".AppleSystemUIFont", size = HUD_FONT_SIZE }` — table form. |
| 9 | `near-cursor` near screen edge (no clamp) | ✓ ADDRESSED | Lines 209-216: `math.max(...edge, math.min(mp.x - HUD_W/2, ...edge))` clamp on both axes; uses `hs.screen.find(mp)` to use cursor's screen (Priority 6). |
| 10 | Brand consistency catches `voice-cc` references | ✓ ADDRESSED | Brand consistency hook GREEN; tests assert no `VOICE_CC_HUD` namespace; all HUD env vars use `PURPLEVOICE_HUD_*`. |
| 11 | Manual walkthroughs missing from `tests/manual/` | ✓ ADDRESSED | All 5 walkthrough .md files present; all 5 have signed sign-off blocks dated 2026-04-30. |
| 12 | Canvas element coordinate space (canvas-local vs screen-global) | ✓ ADDRESSED | Element frames at lines 257 + 268 use `{x=0, y=0}` (canvas-local origin); canvas position via `hudCanvas:topLeft(pos)` at line 288 uses screen-global coords from `positionFor()`. |

**Coverage:** 12/12 pitfalls addressed.

---

## Honesty Audit (D-14 Reframe)

The phrase **"does not pursue `NSWindowSharingNone` exclusion"** must appear verbatim in three surfaces.

| Surface | Required Language | Status |
|---|---|---|
| README.md §"HUD privacy and screen-recording visibility" | "does not pursue `NSWindowSharingNone` exclusion in v1" | ✓ PRESENT (line 132) |
| SECURITY.md §"HUD visibility in screen recordings" | "does not pursue `NSWindowSharingNone` exclusion in v1" | ✓ PRESENT (line 755) |
| REQUIREMENTS.md HUD-04 | "does not pursue `NSWindowSharingNone` exclusion in v1" | ✓ PRESENT (line 74) |

**Apple Forums citation:** All three surfaces cite "Apple Developer Forums thread 792152, 2025" with hyperlink (README, SECURITY) or text reference (REQUIREMENTS).

**Named capture tools:** README + SECURITY both name "QuickTime, OBS, Zoom share-screen, Discord, Loom, Microsoft Teams" — concrete tool list, not vague.

**Privacy escape hatch:** All three surfaces document `PURPLEVOICE_HUD_OFF=1` as **"the only privacy guarantee"** (README L134, SECURITY L757, REQUIREMENTS L74).

**Banned phrase scan:**
```
$ grep -nE "guarantees absolute|always hidden|fully hidden|prevents capture" README.md SECURITY.md
(no matches)
```
✓ No over-claims.

**Framing lint:** `bash tests/test_security_md_framing.sh` → PASS.

**Verdict:** Honesty discipline (Phase 2.7 D-17 carried forward to D-14 reframe) intact across all three surfaces.

---

## Live Walkthrough Sign-Off Audit

All 5 manual walkthrough .md files end with a "## Sign-off (Phase 3.5 close 2026-04-30)" block.

| Walkthrough | Type | Tester | Date | Result |
|---|---|---|---|---|
| `test_hud_appearance.md` | A — live (HUD-01) | Oliver Allen | 2026-04-30 | PASS — pill at top-center, lavender α=0.70, "● Recording" white text, instant fade-in / 150ms fade-out (validated during Plan 01 sign-off `aebd505` / `932ca65`) |
| `test_hud_disable.md` | B — live (HUD-02) | Oliver Allen | 2026-04-30 | PASS — Hammerspoon quit + relaunched with `PURPLEVOICE_HUD_OFF=1`; `_G._purplevoice_hud == nil`; HUD did not appear; menubar still flips ○↔●; transcript still pastes |
| `test_hud_focus.md` | C — live (HUD-04 focus) | Oliver Allen | 2026-04-30 | PASS — focus stays in editor; HUD absent from cmd+tab + Mission Control |
| `test_hud_idle_cpu.md` | D — documentation (HUD-03) | Oliver Allen | 2026-04-30 | DOCUMENTED — `top -pid` baseline check; no measurable bump |
| `test_hud_screen_capture.md` | E — documentation (HUD-04 recordings) | Oliver Allen | 2026-04-30 | DOCUMENTED — legacy `screencapture` CLI + QuickTime Screen Recording capture both run; honest framing in README + SECURITY.md matches observed behaviour |

**Counts:** 3 live + 2 documentation-only = 5 of 5. Matches VALIDATION.md frontmatter contract.

---

## Anti-Patterns Found

None. Scanned `purplevoice-lua/init.lua`, `setup.sh` (HUD line only), `README.md` HUD section, `SECURITY.md` HUD H3.

- No TODO / FIXME / XXX / HACK / PLACEHOLDER / "coming soon" / "not yet implemented" tokens introduced by Phase 3.5 work.
- No empty implementations (`return null`, `return {}`, `return []`, `=> {}`).
- No console.log-only / placeholder handlers.
- No hardcoded empty data flowing to render.
- All HUD env vars use `PURPLEVOICE_HUD_*` namespace; no rejected `_X` / `_Y` / `_ALPHA` / `_COLOR` variants present.

---

## Human Verification Required

None outstanding. User completed all 5 manual walkthroughs (3 live + 2 documentation-only) on macOS Sequoia 15.7.5 on 2026-04-30 and signed off with "all complete". No deferred human-verification items remain for Phase 3.5.

---

## Gaps Summary

No gaps. All 4 success criteria met, all 4 HUD requirements complete, all 9 invariants intact, all 12 RESEARCH pitfalls addressed, all 5 manual walkthrough sign-offs in place, honest framing language verbatim in 3 surfaces, no anti-patterns introduced.

**Notable user-tuning deviation (accepted, documented):** Per commit `932ca65`, alpha was moved from canvas-level (RESEARCH Priority 1 + Pitfall 3 recommendation) to `rectangle.fillColor.alpha = 0.70` (CONTEXT D-03 Claude's-Discretion band extended downward from 0.80–0.90 to 0.70 per live observation). Text uses opaque white to avoid double-composite haze — the same root concern Pitfall 3 was meant to prevent. Phase 1 SUMMARY documents the iteration; user signed off "looks good".

**Surfaced finding promoted to Phase 3 backlog:** DST-06 (Hammerspoon-as-PurpleVoice wrapping decision; Options A/B/C trade-off) recorded in ROADMAP §"Phase 3" Success Criterion 6 per commit `52b08c6`. Out of scope for Phase 3.5; tracked.

---

*Verified: 2026-04-30T13:30:00Z*
*Verifier: Claude (gsd-verifier)*
