# Plan 03.5-01 — HUD Core (Phase 3.5 Wave 1) Summary

**Status:** Complete
**Closed:** 2026-04-30
**Tasks:** 2/2 (1 auto + 1 checkpoint:human-verify, both signed off live)

## Commits

| Hash | Type | Scope |
|------|------|-------|
| `aebd505` | feat | Task 1-1: HUD canvas + show/hide + lifecycle wiring (5 surgical insertions; +127 lines to init.lua) |
| `1052d77` | fix | Forward-declaration bug discovered during live verification: `hudEnabled` was declared at line ~223 but used at line ~154 — Lua locals are not forward-visible, so canvas was never created. Moved HUD config block above canvas creation block. |
| `932ca65` | fix | User-tuned alpha 0.85 → 0.70 + moved alpha from canvas-level to rectangle.fillColor.alpha so text renders crisply (canvas-level alpha was making text translucent too). |

## Live Walkthrough Sign-Off (Task 1-2)

User performed the following on macOS Sequoia 15.7.5 (Hammerspoon `hs.reload()` via `hs -c`):

| Sub-walkthrough | Requirement | Result |
|-----------------|-------------|--------|
| A — HUD-01 appearance | HUD pill appears top-center within ~50ms of press; disappears within ~250ms of release | ✅ "all working" |
| B — HUD-04 (focus half) | Focus passes through; HUD doesn't appear in `cmd+tab` | ✅ "all working" |
| C — HUD-03 (idle CPU, documentation-only) | `top -pid <hammerspoon>` baseline + active values | ✅ documented (no specific threshold required per VALIDATION.md) |
| D — Sanity (Phase 2 paste path + menubar lifecycle) | Transcript still pastes; menubar still flips ○↔● | ✅ "all working" |

**User feedback led to one tuning iteration:** "It should be more transparent — right now the colour is solid." Resolved by `932ca65` (alpha 0.85 → 0.70 + alpha moved from canvas to rectangle fill). User signed off "looks good".

## Verification Gates

- `bash tests/run_all.sh` → **10 PASS / 0 FAIL** (8 pre-existing functional + test_hud_env_off + test_hud_position_validation)
- `bash tests/security/run_all.sh` → **5 PASS / 0 FAIL** (Phase 2.7 security suite unaffected)
- Pattern 2 invariant: `grep -c WHISPER_BIN purplevoice-record` returns `2` ✓
- Pattern 2 corollary: `! grep -q whisper-cli purplevoice-lua/init.lua` ✓
- No `_asm.objc` / `setSharingType` strings introduced (RESEARCH Priority 2 honoured)
- No `setup.sh` edits (Plan 02 territory) ✓
- Brand consistency hook GREEN ✓
- Live HUD canvas presence verified via `hs -c "return _G._purplevoice_hud"` (returned canvas userdata after fix `1052d77`)

## Deviations

| # | Type | Description | Resolution |
|---|------|-------------|------------|
| 1 | Forward-declaration ordering bug | Task 1-1's initial commit `aebd505` placed `hudEnabled` declaration AFTER the canvas creation block that used it. Lua locals are not forward-visible → `if hudEnabled then` read nil → canvas never created. | Fixed in commit `1052d77` (moved HUD config block above canvas creation block; added comment documenting the ordering constraint). |
| 2 | Alpha tuning | User feedback after live observation: 0.85 baseline looked solid. CONTEXT.md D-03 documented "Claude's Discretion: tune within 0.80-0.90". User-driven tuning extended the band downward to 0.70. | Fixed in commit `932ca65`. Also moved alpha from canvas-level to rectangle fill (text now renders crisply). |

## Requirements Closed (Plan 01 share)

- **HUD-01** ✅ — Floating `hs.canvas` pill appears top-center within ~50ms of press; disappears within ~250ms of release. Lavender translucent (alpha 0.70) without backdrop blur. White "● Recording" text.
- **HUD-03** ✅ — `hs.canvas:hide(0.15)` when `resetState()` fires; canvas-level redraw only on `:show`/`:hide`. CPU baseline observed by user via `top -pid` (no measurable bump).
- **HUD-04 (focus half)** ✅ — Default `hs.canvas` behaviour: `ignoresMouseEvents=YES`, `canBecomeKeyWindow=NO`. Verified live (cursor stays in focused editor; HUD does not appear in `cmd+tab` or Mission Control).

## Plan 02 Readiness

`hudEnabled` + `hudPosition` declarations are now correctly above the canvas block. Plan 02 will:
- Replace `hudPosition = "top-center"` literal with `os.getenv("PURPLEVOICE_HUD_POSITION")` validation + 6-named-position dispatch
- Add the `positionFor(name)` arithmetic for 5 additional positions
- Add the `setup.sh` Step 7 banner one-liner

No file overlap risk — Plan 02 modifies the same init.lua file (sequential wave) but different sections.

## Files of Record

- `/Users/oliverallen/Temp video/voice-cc/purplevoice-lua/init.lua` (343 → ~470 lines after the 3 commits above)
- `/Users/oliverallen/Temp video/voice-cc/.planning/phases/03.5-hover-ui-hud/03.5-01-SUMMARY.md` (this file)
