---
phase: 03.5-hover-ui-hud
plan: 03
type: execute
wave: 3
depends_on:
  - 03.5-01
  - 03.5-02
files_modified:
  - README.md
  - SECURITY.md
  - .planning/REQUIREMENTS.md
  - tests/manual/test_hud_appearance.md
  - tests/manual/test_hud_disable.md
  - tests/manual/test_hud_focus.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - HUD-01
  - HUD-02
  - HUD-03
  - HUD-04

must_haves:
  truths:
    - "README.md ## Security & Privacy section has a new HUD privacy subsection (or paragraph) using the RESEARCH Priority 2 honest framing — never 'guarantees', never 'attempts to hide via NSWindowSharingNone' (because the code does not call setSharingType anywhere). Uses 'does not pursue', 'limited', 'ignored by ScreenCaptureKit on macOS 15+', 'incidental, not pursued'."
    - "SECURITY.md §'How to Verify These Claims' has a sentence-level acknowledgement of the HUD ScreenCaptureKit limitation matching the same honest framing — does NOT claim PurpleVoice attempts NSWindowSharingNone exclusion."
    - "REQUIREMENTS.md HUD-01..04 are no longer stubs — final language reflects actual implementation; HUD-04 uses the same honest 'does not pursue NSWindowSharingNone in v1' framing; traceability rows flip Pending → Complete; checkboxes flip [ ] → [x]."
    - "Three of the five manual walkthroughs (test_hud_appearance, test_hud_disable, test_hud_focus) are signed off live with Tester / Date / observations captured in the SUMMARY."
    - "Two manual walkthroughs (test_hud_idle_cpu, test_hud_screen_capture) are documentation-only — sign-off is 'Documented' rather than live-tested. test_hud_screen_capture.md captures the empirical outcome of the three sub-tests on Oliver's machine."
    - "Framing lint stays GREEN throughout (no banned phrases introduced in SECURITY.md; quoted forms allowed)."
    - "Brand consistency lint stays GREEN throughout (no new voice-cc strings)."
    - "All tests stay GREEN (10/0 functional + 5/0 security)."
    - "ROADMAP.md updates Phase 3.5 status: Plans count and progress reflect 4/4 complete; checkboxes flipped."
    - "STATE.md is updated to reflect Phase 3.5 completion."
  artifacts:
    - path: "README.md"
      provides: "## Security & Privacy section extended with HUD privacy subsection (paragraph) using RESEARCH Priority 2 honest framing"
      contains: "HUD"
    - path: "SECURITY.md"
      provides: "§'How to Verify These Claims' sub-paragraph cross-referencing the HUD ScreenCaptureKit limitation"
      contains: "ScreenCaptureKit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HUD-01..04 final language; 4 traceability rows flipped Pending → Complete; all 4 checkboxes [x]"
      contains: "HUD-01"
    - path: "tests/manual/test_hud_appearance.md"
      provides: "Live-signed-off walkthrough (Tester + Date filled)"
    - path: "tests/manual/test_hud_disable.md"
      provides: "Live-signed-off walkthrough"
    - path: "tests/manual/test_hud_focus.md"
      provides: "Live-signed-off walkthrough"
    - path: ".planning/STATE.md"
      provides: "Current Position + Progress block + Key Decisions table + Open TODOs reflect Phase 3.5 closure (Task 3-5)"
      contains: "Phase: 03.5"
    - path: ".planning/ROADMAP.md"
      provides: "Progress table row 5 flipped 0/0 Queued → 4/4 Complete; Phase 3.5 details block enumerates 4 plans; Coverage Summary flipped 4 (TBD) → 4 (Task 3-5)"
      contains: "Phase 3.5: Hover UI / HUD"
  key_links:
    - from: "README.md ## Security & Privacy"
      to: "RESEARCH Priority 2 honest framing"
      via: "paragraph or subsection naming ScreenCaptureKit + Apple Forums + PURPLEVOICE_HUD_OFF=1 escape hatch"
      pattern: "ScreenCaptureKit"
    - from: "SECURITY.md §'How to Verify These Claims'"
      to: "HUD limitation acknowledgement"
      via: "sub-paragraph linking back to README"
      pattern: "ScreenCaptureKit"
    - from: ".planning/REQUIREMENTS.md HUD-01..04"
      to: "[x] Complete + Phase 3.5: Hover UI / HUD | Complete"
      via: "requirements mark-complete sweep"
      pattern: "HUD-0[1234]"
---

<!--
  CONVENTION NOTE (re plan-checker iteration 1 issue #1):
  This plan's `checkpoint:human-verify` task (Task 3-4) intentionally
  satisfies the rigor requirement via its <how-to-verify> + <resume-signal>
  blocks rather than via <action>/<verify>/<done>. That is the convention
  for all checkpoint:human-verify tasks across the project — the human
  walkthrough IS the verification, and there is no autonomous code-side
  action for the executor to perform during the checkpoint.
-->

<objective>
Close Phase 3.5 by landing the documentation, completing the requirement traceability, and live-signing the three manual walkthroughs that exercise observable behaviour. After this plan, Phase 3.5 is closure-ready (awaiting `/gsd:verify-work 3.5` Sonnet sign-off).

Purpose: The HUD code lands in Plans 01 + 02; this plan ensures the privacy claims are honest (RESEARCH Priority 2), the requirements are formalised (no more TBD), and the live behaviour is empirically signed off on Oliver's machine. The framing-lint discipline established by Phase 2.7 D-17 must extend to every new sentence about the HUD's screen-recording behaviour — no "guarantees absolute privacy", no "compliant", no "certified", AND no "attempts to hide via NSWindowSharingNone" (the code does not call setSharingType anywhere — saying it "attempts to hide" is itself a soft over-claim per RESEARCH Priority 2 + Plan 01's explicit MUST-NOT list). Use "does not pursue NSWindowSharingNone exclusion in v1", "limited", "ignored by ScreenCaptureKit on macOS 15+", "incidental, not pursued".

Output:
  - `README.md` ## Security & Privacy section extended with HUD privacy subsection (1-2 paragraphs) using RESEARCH Priority 2 honest framing.
  - `SECURITY.md` §"How to Verify These Claims" extended with a 1-3 sentence HUD limitation acknowledgement (avoiding a 19th H2 section per RESEARCH Open Question §2 recommendation).
  - `.planning/REQUIREMENTS.md` HUD-01..04 promoted from stub to final language; 4 traceability rows flipped Pending → Complete; all 4 checkboxes flipped `[ ]` → `[x]`.
  - 3 manual walkthroughs (`test_hud_appearance.md`, `test_hud_disable.md`, `test_hud_focus.md`) live-signed off with Tester + Date + observations.
  - 2 walkthroughs (`test_hud_idle_cpu.md`, `test_hud_screen_capture.md`) marked Documentation-Only with empirical findings recorded.
  - `.planning/STATE.md` and `.planning/ROADMAP.md` updated to reflect Phase 3.5 closure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md
@.planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md
@.planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md
@.planning/phases/03.5-hover-ui-hud/03.5-00-SUMMARY.md
@.planning/phases/03.5-hover-ui-hud/03.5-01-SUMMARY.md
@.planning/phases/03.5-hover-ui-hud/03.5-02-SUMMARY.md
@README.md
@SECURITY.md
@tests/test_security_md_framing.sh
@tests/test_brand_consistency.sh
@tests/manual/test_hud_appearance.md
@tests/manual/test_hud_disable.md
@tests/manual/test_hud_focus.md
@tests/manual/test_hud_idle_cpu.md
@tests/manual/test_hud_screen_capture.md

<interfaces>
<!-- D-14 honest framing — use this language verbatim or close (RESEARCH Priority 2). -->

<!-- IMPORTANT FRAMING UPDATE (post-checker iteration 1, MAJOR #3):
     The phrase "PurpleVoice attempts to hide the HUD via NSWindowSharingNone"
     is itself a soft over-claim. PurpleVoice does NOT call `setSharingType`
     anywhere — Plan 01's MUST-NOT list explicitly forbids it, and `hs.canvas`
     does not call it either. Saying "attempts to hide" implies an attempt
     that does not actually happen. The honest framing below replaces it
     with "does not pursue NSWindowSharingNone exclusion in v1". -->

The README + SECURITY.md sentences MUST be drawn from this honest framing. The framing-lint allows "limited", "may", "ignored by", "honoured by", "does not pursue", "incidental"; it forbids "guarantees", "compliant", "certified" outside qualified contexts (quoted forms like "compliant" or "certified" pass).

**Recommended README paragraph (use verbatim or close):**

> ### HUD privacy and screen-recording visibility
>
> When recording, PurpleVoice shows a small lavender pill (`● Recording`) at the top-center of the active screen. The HUD complements the menubar indicator and disappears within ~250ms of releasing the hotkey. By default, no HUD is visible when idle.
>
> **Visibility in screen recordings is limited.** PurpleVoice does not pursue `NSWindowSharingNone` exclusion in v1. Even if applied, Apple has stated that ScreenCaptureKit on macOS 15+ ignores window-level sharing flags ([Apple Developer Forums thread 792152, 2025](https://developer.apple.com/forums/thread/792152)) — modern capture tools (QuickTime, OBS, Zoom share-screen, Discord, Loom, Microsoft Teams) capture the HUD regardless. The legacy `screencapture` CLI and CGWindowList-based tools may still honour sharing flags, but this is incidental, not pursued.
>
> **For sensitive sessions** (recorded demos, screen-shared meetings, journalist source-handling), run with `PURPLEVOICE_HUD_OFF=1` and rely on the menubar indicator alone — that is the only privacy guarantee. The Phase 2 menubar indicator is unaffected by HUD configuration.

**Recommended SECURITY.md sub-paragraph (use verbatim or close — insert into §"How to Verify These Claims" as an H3 sub-subsection; DO NOT add a 19th H2 section):**

> ### HUD visibility in screen recordings
>
> Phase 3.5 ships an optional floating HUD (`● Recording` pill) that appears while the dictation hotkey is held. PurpleVoice does not pursue `NSWindowSharingNone` exclusion in v1. Even if applied, Apple has stated that ScreenCaptureKit on macOS 15+ ignores window-level sharing flags ([Apple Developer Forums thread 792152, 2025](https://developer.apple.com/forums/thread/792152)) — modern capture tools (QuickTime, OBS, Zoom share-screen, Discord, Loom, Microsoft Teams) capture the HUD regardless. The legacy `screencapture` CLI and CGWindowList-based tools may still honour sharing flags, but this is incidental, not pursued. **For sensitive recording sessions, set `PURPLEVOICE_HUD_OFF=1` — that is the only privacy guarantee.** The menubar indicator is independent of HUD configuration. See [`README.md` §"HUD privacy and screen-recording visibility"](README.md#hud-privacy-and-screen-recording-visibility) for the user-facing configuration table.

<!-- HUD-01..04 final language for REQUIREMENTS.md (replaces Plan 00 stubs): -->

These match the Plan 00 stubs almost exactly but with crisper present-tense phrasing now that the implementation is real. The CHECKBOXES flip `[ ]` → `[x]` and the TRACEABILITY status flips Pending → Complete. **HUD-04 uses the corrected honest framing** (no "attempts to hide via NSWindowSharingNone" — instead "does not pursue NSWindowSharingNone exclusion in v1").

```markdown
### Hover UI / HUD

- [x] **HUD-01**: Floating canvas widget (`hs.canvas` overlay) appears within ~50ms of hotkey press and disappears within ~250ms of release. Implemented in `purplevoice-lua/init.lua` as a single `hs.canvas.new()` instance created at module load (hidden); `:show(0)` instant on press, `:hide(0.15)` on release. Lifecycle hooked into `onPress` (line ~272) and `resetState()` (line ~123) alongside `setMenubarRecording()`/`setMenubarIdle()`. Verified by `tests/manual/test_hud_appearance.md` (live walkthrough — sign-off in `03.5-03-SUMMARY.md`).
- [x] **HUD-02**: User-toggleable visibility via `PURPLEVOICE_HUD_OFF=1` env var (default-ON per D-09; mirrors `PURPLEVOICE_NO_SOUNDS` line 99 idiom). Read once at module load; reload Hammerspoon to apply changes. Verified by `tests/test_hud_env_off.sh` (string-level wiring) + `tests/manual/test_hud_disable.md` (live end-to-end walkthrough).
- [x] **HUD-03**: Effectively zero CPU when idle — hidden `hs.canvas` orders the NSWindow out (`[NSWindow orderOut:]`); no animation loops, no timers; `:wantsLayer(true)` engages Core Animation GPU compositing during visible state. Verified by `tests/manual/test_hud_idle_cpu.md` (documentation-driven `top -pid` baseline; no specific pass/fail threshold — empirical baseline + active CPU recorded in walkthrough sign-off).
- [x] **HUD-04**: Position configurable via `PURPLEVOICE_HUD_POSITION` env var (six locked named positions per D-07: `top-center` (default) | `top-right` | `bottom-center` | `bottom-right` | `near-cursor` | `center`). Invalid values fall back to `top-center` with a single warning to the Hammerspoon console at module load. HUD does not steal focus (`hs.canvas` defaults `ignoresMouseEvents = YES` and `canBecomeKeyWindow = NO` — verified in libcanvas.m source). HUD visibility in screen recordings is **limited**: PurpleVoice does not pursue `NSWindowSharingNone` exclusion in v1; even if applied, Apple has stated ScreenCaptureKit on macOS 15+ ignores window-level sharing flags (Apple Developer Forums thread 792152, 2025). Legacy `screencapture` CLI and CGWindowList-based tools may still honour sharing flags, but this is incidental, not pursued. For sensitive recording sessions, run with `PURPLEVOICE_HUD_OFF=1` — that is the only privacy guarantee. Verified by `tests/test_hud_position_validation.sh` + `tests/manual/test_hud_focus.md` (live walkthrough) + `tests/manual/test_hud_screen_capture.md` (documentation-only walkthrough capturing empirical outcomes).
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 3-1: Extend README.md ## Security & Privacy section with HUD privacy subsection (D-14 honest framing — corrected, no "attempts to hide via NSWindowSharingNone")</name>

  <read_first>
    - README.md (full file — current ## Security & Privacy section runs lines 74-126)
    - .planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md D-13, D-14, D-15
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md Priority 2 (the honest framing reference)
    - .planning/phases/03.5-hover-ui-hud/03.5-01-PLAN.md (the MUST NOT list — code does NOT call setSharingType anywhere)
    - tests/test_brand_consistency.sh (README is on the exemption list — but adding new voice-cc strings is still forbidden)
    - .planning/phases/02.7-security-posture/02.7-CONTEXT.md D-17 (framing constraint applies here too — "compatible with" / never "guarantees")
  </read_first>

  <action>
    Edit `README.md` to extend the existing `## Security & Privacy` section (around line 74-126) with a new H3 subsection **after** the existing `### Reporting a security issue` subsection (around line 124-126) and **before** the `## Visual identity` section (line 128).

    Insert this content (drawn from the honest framing in <interfaces> above — note the **corrected** language: "does not pursue NSWindowSharingNone exclusion in v1", NOT "attempts to hide via NSWindowSharingNone"):

    ```markdown
    ### HUD privacy and screen-recording visibility

    When recording, PurpleVoice shows a small lavender pill (`● Recording`) at the top-center of the active screen. The HUD complements the menubar indicator and disappears within ~250ms of releasing the hotkey. By default, no HUD is visible when idle.

    **Visibility in screen recordings is limited.** PurpleVoice does not pursue `NSWindowSharingNone` exclusion in v1. Even if applied, Apple has stated that ScreenCaptureKit on macOS 15+ ignores window-level sharing flags ([Apple Developer Forums thread 792152, 2025](https://developer.apple.com/forums/thread/792152)) — modern capture tools (QuickTime, OBS, Zoom share-screen, Discord, Loom, Microsoft Teams) capture the HUD regardless. The legacy `screencapture` CLI and CGWindowList-based tools may still honour sharing flags, but this is incidental, not pursued.

    **For sensitive sessions** (recorded demos, screen-shared meetings, journalist source-handling), run with `PURPLEVOICE_HUD_OFF=1` and rely on the menubar indicator alone — that is the only privacy guarantee. The Phase 2 menubar indicator is unaffected by HUD configuration.

    Configuration:

    | Env var | Effect | Default |
    |---|---|---|
    | `PURPLEVOICE_HUD_OFF=1` | Disable HUD entirely (menubar indicator unchanged) | unset (HUD enabled) |
    | `PURPLEVOICE_HUD_POSITION` | One of: `top-center` (default) / `top-right` / `bottom-center` / `bottom-right` / `near-cursor` / `center` | `top-center` |

    Both env vars are read once at module load — reload Hammerspoon (menubar → Reload Config) to apply changes.
    ```

    **What you MUST NOT do:**
    - Do NOT use the words `guarantees absolute`, `compliant`, `certified` (outside quoted forms — but you don't need them at all here). The phrase "that is the only privacy guarantee" is a qualified, true statement about PURPLEVOICE_HUD_OFF=1 — not a "guarantees absolute privacy" claim — and is permitted by the framing lint.
    - Do NOT use the phrase `attempts to hide.*NSWindowSharingNone` or any variant. The code does not call `setSharingType` (Plan 01 explicitly forbids it). Saying "attempts to hide via NSWindowSharingNone" is itself a soft over-claim that the verify clause now catches.
    - Do NOT introduce new `voice-cc` strings (README is on the brand-consistency exemption for the existing 3 historical mentions only — do not add a fourth).
    - Do NOT remove or alter any existing content in the ## Security & Privacy section — only ADD the new H3 subsection.
    - Do NOT alter the ## Visual identity section or any later content.
    - Do NOT add a runtime-toggle hotkey reference (D-11 — env-var-only).
    - Do NOT promise HUD invisibility in any tool — the framing is "limited", "does not pursue", "incidental, not pursued".

    **Framing lint compliance check (do this in your head while writing):** Read every sentence asking "could this be construed as 'guarantees absolute privacy', 'always hidden', 'fully hidden', 'prevents capture', or 'attempts to hide via NSWindowSharingNone'?" If yes, reword. Use "limited", "does not pursue", "may", "ignored by ScreenCaptureKit on macOS 15+", "incidental, not pursued" — these all pass.
  </action>

  <verify>
    <automated>set -e; F=README.md; grep -q "### HUD privacy and screen-recording visibility" "$F" || { echo "HUD privacy subsection missing"; exit 1; }; grep -q "ScreenCaptureKit" "$F" || { echo "ScreenCaptureKit honest framing missing"; exit 1; }; grep -q "PURPLEVOICE_HUD_OFF=1" "$F" || { echo "escape hatch reference missing"; exit 1; }; grep -q "PURPLEVOICE_HUD_POSITION" "$F" || { echo "position env var documented"; exit 1; }; grep -q "thread 792152" "$F" || { echo "Apple Developer Forums citation missing"; exit 1; }; grep -q "limited" "$F" || { echo "honest framing word 'limited' missing"; exit 1; }; grep -q "does not pursue" "$F" || { echo "honest framing 'does not pursue' missing"; exit 1; }; ! grep -qiE "(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)" "$F" || { echo "FAIL: over-claim framing introduced (one of: 'guarantees absolute' / 'always hidden' / 'fully hidden' / 'prevents capture' / 'attempts to hide.*NSWindowSharingNone')"; exit 1; }; ! grep -qiE "\bcompliant\b" "$F" || { echo "WARN: compliant introduced — check qualified context"; exit 1; }; bash tests/test_brand_consistency.sh || { echo "brand consistency regression"; exit 1; }; echo "OK: README HUD privacy subsection landed with corrected honest framing"</automated>
  </verify>

  <done>
    README.md has a new H3 `### HUD privacy and screen-recording visibility` subsection inside the existing `## Security & Privacy` section, placed after `### Reporting a security issue` and before `## Visual identity`. The subsection contains: 1-paragraph user-facing description; 1-paragraph honest framing about ScreenCaptureKit limitation citing Apple Forums thread 792152, using "does not pursue NSWindowSharingNone exclusion in v1" (NOT "attempts to hide via NSWindowSharingNone"); 1-paragraph escape hatch (PURPLEVOICE_HUD_OFF=1 — "the only privacy guarantee"); a configuration table for both env vars. The negative-grep pattern `(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)` returns ZERO matches. Brand consistency intact.
  </done>
</task>

<task type="auto">
  <name>Task 3-2: Add HUD limitation acknowledgement to SECURITY.md §'How to Verify These Claims' (corrected D-14 framing — no "attempts to hide via NSWindowSharingNone")</name>

  <read_first>
    - SECURITY.md (full file — §"How to Verify These Claims" runs from line 701 to end-of-file ~755)
    - tests/test_security_md_framing.sh (the framing lint we MUST pass; check 2 forbids adding a 19th required H2 section; check 3 forbids banned phrases outside qualified contexts)
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md Priority 2 + Open Question §2 (the recommendation: extend §"How to Verify" rather than add a 19th H2)
    - .planning/phases/03.5-hover-ui-hud/03.5-01-PLAN.md (the MUST NOT list — code does NOT call setSharingType anywhere; the framing must reflect that)
    - .planning/phases/02.7-security-posture/02.7-CONTEXT.md D-17 (framing discipline)
  </read_first>

  <action>
    Edit `SECURITY.md`. Add a new **H3 subsection** within the existing `## How to Verify These Claims` section. Place it AFTER the existing `### What if a verification fails?` subsection (currently the last subsection before EOF; runs around lines 745-755) and BEFORE the trailing horizontal rule / footer (if any).

    DO NOT add a new H2 section — that would require updating `tests/test_security_md_framing.sh`'s REQUIRED_SECTIONS array, which is out of scope for Phase 3.5. The H3-under-existing-H2 pattern preserves the framing-lint contract.

    Insert this content (drawn from the honest framing in <interfaces> above — note the **corrected** language: "does not pursue NSWindowSharingNone exclusion in v1", NOT "attempts to hide via NSWindowSharingNone"):

    ```markdown

    ### HUD visibility in screen recordings

    Phase 3.5 ships an optional floating HUD (`● Recording` pill) that appears while the dictation hotkey is held. PurpleVoice does not pursue `NSWindowSharingNone` exclusion in v1. Even if applied, Apple has stated that ScreenCaptureKit on macOS 15+ ignores window-level sharing flags ([Apple Developer Forums thread 792152, 2025](https://developer.apple.com/forums/thread/792152)) — modern capture tools (QuickTime, OBS, Zoom share-screen, Discord, Loom, Microsoft Teams) capture the HUD regardless. The legacy `screencapture` CLI and CGWindowList-based tools may still honour sharing flags, but this is incidental, not pursued.

    **For sensitive recording sessions, set `PURPLEVOICE_HUD_OFF=1` — that is the only privacy guarantee.** The menubar indicator is independent of HUD configuration. See [`README.md` §"HUD privacy and screen-recording visibility"](README.md#hud-privacy-and-screen-recording-visibility) for the user-facing configuration table.
    ```

    **What you MUST NOT do:**
    - Do NOT add a new `## ` (H2) section — preserves the framing-lint REQUIRED_SECTIONS list (RESEARCH Open Question §2 recommendation).
    - Do NOT use `compliant`, `certified`, or `guarantees absolute` outside qualified contexts. The text above uses "limited", "does not pursue", "incidental", "stated" — all pass. The phrase "that is the only privacy guarantee" is a qualified, true statement about PURPLEVOICE_HUD_OFF=1 and is permitted (it does NOT match the framing-lint banned `\bguarantees\b` pattern in unqualified contexts because it's qualified by "the only" and is referring to the kill-switch, not the HUD's hide-from-recording behaviour).
    - Do NOT use the phrase "attempts to hide.*NSWindowSharingNone" or any variant. The code does not call `setSharingType` (Plan 01 explicitly forbids it).
    - Do NOT introduce voice-cc strings (`grep -q "voice-cc" SECURITY.md` must continue to return 1; the framing lint Check 5 enforces this).
    - Do NOT modify any other section of SECURITY.md.
    - Do NOT add framework-specific compliance claims about the HUD — the HUD is not a security-relevant artefact for any of the 6 framed frameworks (FIPS / FedRAMP / CC / HIPAA / SOC 2 / ISO 27001). It's a user-affordance concern, not a security-control concern.

    **Framing lint check (run after edit):** `bash tests/test_security_md_framing.sh` MUST exit 0. If it fails on banned phrases, reword. The exemption pattern in the lint allows quoted forms (`"compliant"`, `"certified"`, `"guarantees"`) but the text above uses none of these in unqualified form.
  </action>

  <verify>
    <automated>set -e; F=SECURITY.md; grep -q "### HUD visibility in screen recordings" "$F" || { echo "HUD visibility H3 subsection missing"; exit 1; }; grep -q "ScreenCaptureKit" "$F" || { echo "ScreenCaptureKit reference missing"; exit 1; }; grep -q "thread 792152" "$F" || { echo "Apple Developer Forums citation missing"; exit 1; }; grep -q "PURPLEVOICE_HUD_OFF=1" "$F" || { echo "escape hatch reference missing"; exit 1; }; grep -q "limited" "$F" || { echo "honest framing word 'limited' missing"; exit 1; }; grep -q "does not pursue" "$F" || { echo "honest framing 'does not pursue' missing"; exit 1; }; ! grep -qiE "(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)" "$F" || { echo "FAIL: over-claim framing introduced"; exit 1; }; H2_COUNT=$(grep -cE "^## " "$F"); [ "$H2_COUNT" -eq 18 ] || { echo "H2 count is $H2_COUNT (expected 18 — DID NOT add a 19th H2)"; exit 1; }; bash tests/test_security_md_framing.sh || { echo "framing lint failed — check banned phrases / required sections / voice-cc"; exit 1; }; bash tests/test_brand_consistency.sh || { echo "brand consistency regression"; exit 1; }; echo "OK: SECURITY.md HUD limitation acknowledgement landed; H2 count preserved at 18; framing lint GREEN"</automated>
  </verify>

  <done>
    SECURITY.md has a new H3 subsection `### HUD visibility in screen recordings` inside the existing `## How to Verify These Claims` H2 (NO new H2 added — H2 count stays at 18). The subsection cites Apple Developer Forums thread 792152, names ScreenCaptureKit and the affected tools, names the escape hatch (PURPLEVOICE_HUD_OFF=1 — "the only privacy guarantee"), and cross-links to README. The framing uses "does not pursue NSWindowSharingNone exclusion in v1" (NOT "attempts to hide via NSWindowSharingNone"). The negative-grep pattern `(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)` returns ZERO matches. Framing lint stays GREEN. Brand consistency stays GREEN.
  </done>
</task>

<task type="auto">
  <name>Task 3-3: Promote REQUIREMENTS.md HUD-01..04 from Plan-00 stubs to final language; flip checkboxes; update traceability (HUD-04 uses corrected D-14 framing)</name>

  <read_first>
    - .planning/REQUIREMENTS.md (current state with Plan 00's stubs)
    - .planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md (final D-decisions language)
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md Priority 2 (honest D-14 framing for HUD-04)
    - .planning/phases/03.5-hover-ui-hud/03.5-01-PLAN.md (MUST NOT list confirms code does not call setSharingType)
    - .planning/phases/03.5-hover-ui-hud/03.5-01-SUMMARY.md + 03.5-02-SUMMARY.md (line numbers / API references to cite accurately)
  </read_first>

  <action>
    Three edits to `.planning/REQUIREMENTS.md`:

    **Edit 1 — Replace HUD-01..04 stub language with final language.**

    Plan 00 left the four entries with stub text. Replace them with the language from <interfaces> above (which uses `[x]` checkboxes — final state, AND uses the corrected HUD-04 framing). Locate the existing `### Hover UI / HUD` subsection (added by Plan 00) and replace the four bullet entries with:

    ```markdown
    - [x] **HUD-01**: Floating canvas widget (`hs.canvas` overlay) appears within ~50ms of hotkey press and disappears within ~250ms of release. Implemented in `purplevoice-lua/init.lua` as a single `hs.canvas.new()` instance created at module load (hidden); `:show(0)` instant on press, `:hide(0.15)` on release. Lifecycle hooked into `onPress` (line ~272) and `resetState()` (line ~123) alongside `setMenubarRecording()`/`setMenubarIdle()`. Verified by `tests/manual/test_hud_appearance.md` (live walkthrough — sign-off in `03.5-03-SUMMARY.md`).
    - [x] **HUD-02**: User-toggleable visibility via `PURPLEVOICE_HUD_OFF=1` env var (default-ON per D-09; mirrors `PURPLEVOICE_NO_SOUNDS` line 99 idiom). Read once at module load; reload Hammerspoon to apply changes. Verified by `tests/test_hud_env_off.sh` (string-level wiring) + `tests/manual/test_hud_disable.md` (live end-to-end walkthrough).
    - [x] **HUD-03**: Effectively zero CPU when idle — hidden `hs.canvas` orders the NSWindow out (`[NSWindow orderOut:]`); no animation loops, no timers; `:wantsLayer(true)` engages Core Animation GPU compositing during visible state. Verified by `tests/manual/test_hud_idle_cpu.md` (documentation-driven `top -pid` baseline; no specific pass/fail threshold — empirical baseline + active CPU recorded in walkthrough sign-off).
    - [x] **HUD-04**: Position configurable via `PURPLEVOICE_HUD_POSITION` env var (six locked named positions per D-07: `top-center` (default) | `top-right` | `bottom-center` | `bottom-right` | `near-cursor` | `center`). Invalid values fall back to `top-center` with a single warning to the Hammerspoon console at module load. HUD does not steal focus (`hs.canvas` defaults `ignoresMouseEvents = YES` and `canBecomeKeyWindow = NO` — verified in libcanvas.m source). HUD visibility in screen recordings is **limited**: PurpleVoice does not pursue `NSWindowSharingNone` exclusion in v1; even if applied, Apple has stated ScreenCaptureKit on macOS 15+ ignores window-level sharing flags (Apple Developer Forums thread 792152, 2025). Legacy `screencapture` CLI and CGWindowList-based tools may still honour sharing flags, but this is incidental, not pursued. For sensitive recording sessions, run with `PURPLEVOICE_HUD_OFF=1` — that is the only privacy guarantee. Verified by `tests/test_hud_position_validation.sh` + `tests/manual/test_hud_focus.md` (live walkthrough) + `tests/manual/test_hud_screen_capture.md` (documentation-only walkthrough capturing empirical outcomes).
    ```

    All four checkboxes flipped `[ ]` → `[x]`.

    **Edit 2 — Flip the four traceability rows from Pending → Complete.**

    Locate the four rows added by Plan 00:

    ```markdown
    | HUD-01 | Phase 3.5: Hover UI / HUD | Pending |
    | HUD-02 | Phase 3.5: Hover UI / HUD | Pending |
    | HUD-03 | Phase 3.5: Hover UI / HUD | Pending |
    | HUD-04 | Phase 3.5: Hover UI / HUD | Pending |
    ```

    Replace `Pending` with `Complete` on all four rows.

    **Edit 3 — Update Per-phase counts table to reflect Phase 3.5 completion.**

    Plan 00 added:
    ```markdown
    - Phase 3.5: Hover UI / HUD — 4 requirements (HUD-01..04) — Pending
    ```

    Change `Pending` → `Complete`.

    **Edit 4 — Update trailing footer.**

    Plan 00 set:
    ```markdown
    *Last updated: 2026-04-30 — four HUD requirements added for Phase 3.5 (Pending); traceability table extended.*
    ```

    Change to:
    ```markdown
    *Last updated: 2026-04-30 — four HUD requirements completed for Phase 3.5 (Complete); traceability table extended.*
    ```

    DO NOT modify any other entries in REQUIREMENTS.md.

    **What you MUST NOT do (HUD-04 framing constraint):**
    - Do NOT use the phrase `attempts to hide.*NSWindowSharingNone` in HUD-04 — the code does not call `setSharingType` (Plan 01 explicitly forbids it). Use "does not pursue NSWindowSharingNone exclusion in v1" instead.
    - Do NOT use `guarantees absolute`, `always hidden`, `fully hidden`, `prevents capture` anywhere in HUD-01..04.
  </action>

  <verify>
    <automated>set -e; F=.planning/REQUIREMENTS.md; for id in HUD-01 HUD-02 HUD-03 HUD-04; do grep -qE "^- \[x\] \*\*${id}\*\*" "$F" || { echo "$id checkbox not flipped to [x]"; exit 1; }; grep -qE "^\| ${id} \| Phase 3\.5: Hover UI / HUD \| Complete \|" "$F" || { echo "$id traceability row not flipped to Complete"; exit 1; }; done; grep -qE "Phase 3\.5: Hover UI / HUD — 4 requirements .* — Complete" "$F" || { echo "Per-phase counts row not flipped"; exit 1; }; grep -qE "completed for Phase 3\.5 \(Complete\)" "$F" || { echo "footer not updated"; exit 1; }; grep -q "ScreenCaptureKit" "$F" || { echo "HUD-04 honest framing language missing — should reference ScreenCaptureKit per RESEARCH Priority 2"; exit 1; }; grep -q "thread 792152" "$F" || { echo "Apple Forums citation missing in HUD-04"; exit 1; }; grep -q "does not pursue" "$F" || { echo "HUD-04 corrected framing missing — must use 'does not pursue NSWindowSharingNone exclusion in v1'"; exit 1; }; ! grep -qiE "(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)" "$F" || { echo "FAIL: over-claim framing in HUD-04"; exit 1; }; bash tests/test_brand_consistency.sh || { echo "brand consistency regression"; exit 1; }; echo "OK: REQUIREMENTS.md HUD-01..04 promoted to final + Complete; HUD-04 uses corrected D-14 framing; traceability + per-phase counts + footer updated"</automated>
  </verify>

  <done>
    REQUIREMENTS.md HUD-01..04 entries have final crisp language; all 4 checkboxes are `[x]`; HUD-04 references ScreenCaptureKit + thread 792152 (honest D-14 framing per RESEARCH Priority 2) AND uses the corrected "does not pursue NSWindowSharingNone exclusion in v1" framing (NOT "attempts to hide"); 4 traceability rows flipped to Complete; per-phase counts row flipped to Complete; footer updated. Negative-grep for over-claim phrases returns ZERO matches. brand consistency intact.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Documentation closure for Phase 3.5: README.md HUD privacy subsection (corrected honest framing), SECURITY.md §"How to Verify" extension with HUD limitation, REQUIREMENTS.md HUD-01..04 promoted to final + Complete, traceability fully wired. Tasks 3-1 / 3-2 / 3-3 are autonomous; this checkpoint executes the three live manual walkthroughs and the two documentation-only walkthroughs.
  </what-built>

  <how-to-verify>
    Run the five manual walkthroughs and capture sign-off / observations. After this checkpoint resumes, Task 3-5 will fold the captured observations into the SUMMARY.md.

    **Live sub-walkthrough A — `tests/manual/test_hud_appearance.md` (HUD-01):**
    1. Open `tests/manual/test_hud_appearance.md` and follow its Steps section.
    2. Observe: pill appearance / disappearance behaviour matches Expected Outcome.
    3. Edit the file in place to fill the Sign-off section: check the four sign-off boxes, fill `Tester:` (your name) and `Date: 2026-04-30` (or whatever today is — UTC).

    **Live sub-walkthrough B — `tests/manual/test_hud_disable.md` (HUD-02):**
    1. Follow Steps. Confirm PURPLEVOICE_HUD_OFF=1 + reload disables HUD; menubar still cycles; paste path unaffected; unset + reload re-enables HUD.
    2. Fill the Sign-off section.

    **Live sub-walkthrough C — `tests/manual/test_hud_focus.md` (HUD-04 focus half):**
    1. Follow Steps. Confirm focus stays in editor; clicks pass through HUD; HUD absent from cmd+tab + Mission Control.
    2. Fill the Sign-off section.

    **Documentation-only walkthrough D — `tests/manual/test_hud_idle_cpu.md` (HUD-03):**
    1. Follow Steps to record BASELINE and ACTIVE CPU% values.
    2. Document the actual values in the Sign-off section (no live tester sign-off required — this is documentation-only).
    3. Set `Tester: [Documented]` and `Date: 2026-04-30` (today).

    **Documentation-only walkthrough E — `tests/manual/test_hud_screen_capture.md` (HUD-04 recordings):**
    1. Run sub-test A (`screencapture -x /tmp/purplevoice_hud_test_legacy.png` while HUD visible). Document outcome (HUD visible Y/N) — likely N on macOS 15.
    2. Run sub-test B (QuickTime screen recording while HUD visible). Document outcome — likely Y on macOS 15+ per RESEARCH Priority 2.
    3. Sub-test C (Zoom) optional — skip if Zoom not installed; mark as skipped.
    4. Fill the Documentation Output table with empirical findings.
    5. Set `Tester: [Documented]` and `Date: 2026-04-30`.

    **Final smoke checks before resuming:**
    - `bash tests/run_all.sh` reports 10 PASS / 0 FAIL.
    - `bash tests/security/run_all.sh` reports 5 PASS / 0 FAIL.
    - `bash tests/test_brand_consistency.sh` exits 0.
    - `bash tests/test_security_md_framing.sh` exits 0.
  </how-to-verify>

  <resume-signal>
    Type "approved" if A + B + C live-signed off and D + E documented with empirical values. Include the empirical findings in the resume signal so Task 3-5 can fold them into the SUMMARY:
    - D: BASELINE CPU% = ____, ACTIVE CPU% = ____
    - E: Sub-test A (screencapture CLI) HUD visible: Y/N. Sub-test B (QuickTime) HUD visible: Y/N. Sub-test C (Zoom) HUD visible: Y/N/skipped.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3-5: Update STATE.md + ROADMAP.md to reflect Phase 3.5 closure</name>

  <read_first>
    - .planning/STATE.md (full file — Current Position section, Progress block, Accumulated Context > Key Decisions)
    - .planning/ROADMAP.md (full file — Progress table at line ~180; Phase 3.5 entry at line ~108)
    - .planning/phases/03.5-hover-ui-hud/03.5-00-SUMMARY.md
    - .planning/phases/03.5-hover-ui-hud/03.5-01-SUMMARY.md
    - .planning/phases/03.5-hover-ui-hud/03.5-02-SUMMARY.md
  </read_first>

  <action>
    **Edit 1 — STATE.md `Current Position` block.**

    Find the lines:
    ```
    Phase: 02.7 (security-posture) — COMPLETE (awaiting `/gsd:verify-work 2.7` Sonnet verifier sign-off)
    Plan: 6 of 6 (all waves complete — Wave 0 / Wave 1 / Wave 2 / Wave 3 / Wave 4)
    Next: `/gsd:verify-work 2.7` (orchestrator-side Sonnet verification of all 6 Phase 2.7 plans + cross-plan invariants). After verifier sign-off + Phase 2.7 close, Phase 3.5 (HUD) is next per ROADMAP order.
    ```

    Replace with (or append after, depending on whether 02.7 sign-off has happened):
    ```
    Phase: 03.5 (hover-ui-hud) — COMPLETE (4 of 4 plans landed — Wave 0 / Wave 1 / Wave 2 / Wave 3; awaiting `/gsd:verify-work 3.5` Sonnet sign-off)
    Plan: 4 of 4 — Plan 03.5-00 (Wave 0 test infra + REQUIREMENTS stubs) + Plan 03.5-01 (HUD core in init.lua) + Plan 03.5-02 (config surface + setup.sh banner) + Plan 03.5-03 (docs closure + 3 live walkthroughs + 2 documentation walkthroughs + REQUIREMENTS finalisation)
    Next: `/gsd:verify-work 3.5` (orchestrator-side Sonnet verification of all 4 Phase 3.5 plans). After verifier sign-off + Phase 3.5 close, Phase 4 (QoL) is next per ROADMAP order.
    ```

    Also update the milestone progress fields:
    - `last_updated:` → today's date in ISO 8601
    - `progress.completed_phases: 4` → `5`
    - `progress.total_plans: 17` → `21`
    - `progress.completed_plans: 17` → `21`

    **Edit 2 — STATE.md Progress block.**

    Update the ASCII progress bar block:
    ```
    Phase 3.5: Hover UI / HUD                 ░░░░░░░░░░  0%   [Added 2026-04-27 — needs planning]
    ```
    becomes:
    ```
    Phase 3.5: Hover UI / HUD                 ██████████  100% [4/4 plans complete 2026-04-30 — Wave 0 (03.5-00 Wave-0 infra) + Wave 1 (03.5-01 HUD core) + Wave 2 (03.5-02 config surface + setup.sh banner) + Wave 3 (03.5-03 docs closure + 3 live walkthroughs); awaiting `/gsd:verify-work 3.5`]
    ```

    Also update the overall percentage:
    ```
    Overall v1 (Phases 1, 2, 2.5, 2.7, 3.5, 4, 3): █████░░░░░  ~57% (4 of 7 effective v1 phases complete — Phase 2.7 added during planning evolution)
    ```
    becomes:
    ```
    Overall v1 (Phases 1, 2, 2.5, 2.7, 3.5, 4, 3): ███████░░░  ~71% (5 of 7 effective v1 phases complete)
    ```

    **Edit 3 — STATE.md `### Key Decisions` table — append rows for Phase 3.5.**

    Append after the last existing row in the Key Decisions table:
    ```
    | hs.canvas (not hs.drawing) is the right API for Phase 3.5 HUD per RESEARCH Priority 1; hs.drawing is officially deprecated. New code uses hs.canvas only. | Plan 03.5-01 Task 1-1 | 2026-04-30 |
    | D-14 reframe — HUD screen-recording exclusion is **limited**: PurpleVoice does not pursue NSWindowSharingNone exclusion in v1 (the code does not call setSharingType anywhere — Plan 01 explicitly forbids it). Even if applied, ScreenCaptureKit on macOS 15+ ignores window-level sharing flags (Apple Developer Forums thread 792152, 2025). Legacy `screencapture` CLI / CGWindowList may still honour the flag but this is incidental, not pursued. PURPLEVOICE_HUD_OFF=1 is the privacy escape hatch. README + SECURITY.md use this honest framing per Phase 2.7 D-17 discipline. | RESEARCH Priority 2 + Plan 03.5-03 Tasks 3-1 + 3-2 | 2026-04-30 |
    | _G._purplevoice_hud defensive cleanup at module load (RESEARCH Pattern 1; mirrors pcall(hs.notify.unregister, ...) precedent at init.lua line 26-27). Closes a hypothetical orphan-canvas leak on Hammerspoon partial-reload paths. | Plan 03.5-01 Task 1-1 | 2026-04-30 |
    | HUD canvas alpha set at canvas-level (:alpha(0.85)) NOT at element-level (RESEARCH Pitfall 3). Single source of truth for translucency; prevents text-edge double-composite haze. | Plan 03.5-01 Task 1-1 | 2026-04-30 |
    | wantsLayer(true) on HUD canvas to engage Core Animation GPU compositing — drops visible-state CPU to ~0% (RESEARCH Pitfall 5). | Plan 03.5-01 Task 1-1 | 2026-04-30 |
    | near-cursor position uses cursor's screen (hs.screen.find(hs.mouse.absolutePosition())) NOT focused-window screen, with a clamp to keep the pill on-screen near edges (RESEARCH Pitfall 9). All other named positions use focused-window screen per D-06. | Plan 03.5-02 Task 2-1 | 2026-04-30 |
    | No Lua-ObjC bridge / hs._asm.objc / hs._asm.uitk introduced. RESEARCH Priority 2 + Pitfall 1 explicitly recommend NOT pursuing setSharingType:NSWindowSharingNone via private APIs — hs.canvas doesn't expose it AND ScreenCaptureKit ignores it on macOS 15+ regardless. PURPLEVOICE_HUD_OFF=1 is the real privacy escape hatch. | Plan 03.5-01 Task 1-1 | 2026-04-30 |
    ```

    **Edit 4 — STATE.md Open TODOs section.**

    Append a Phase 3.5 follow-up if any was surfaced in walkthroughs (typically empty for this phase — note explicitly):
    ```
    - [x] Phase 3.5: Wave 0 (03.5-00) — test infra + REQUIREMENTS HUD-01..04 stubs landed 2026-04-30.
    - [x] Phase 3.5: HUD core (03.5-01) — hs.canvas overlay + lifecycle wiring landed 2026-04-30.
    - [x] Phase 3.5: Config surface (03.5-02) — PURPLEVOICE_HUD_POSITION + setup.sh banner landed 2026-04-30.
    - [x] Phase 3.5: Docs closure (03.5-03) — README + SECURITY.md HUD caveats + REQUIREMENTS finalisation + 3 live walkthroughs signed off + 2 documentation walkthroughs landed 2026-04-30.
    ```

    **Edit 5 — STATE.md "Last updated" line at top of file.**

    Replace the existing last-updated paragraph with a Phase 3.5 closure summary. Keep the Phase 2.7 closure paragraph intact for history; add a new Phase 3.5 paragraph above the "## Project Reference" header.

    **Edit 6 — ROADMAP.md Progress table (line ~180).**

    Find the row:
    ```
    | 5 | Phase 3.5: Hover UI / HUD | 0/0 | Queued | - |
    ```

    Replace with:
    ```
    | 5 | Phase 3.5: Hover UI / HUD | 4/4 | Complete (awaiting `/gsd:verify-work 3.5`) | 2026-04-30 |
    ```

    **Edit 7 — ROADMAP.md Phase 3.5 details block (around line ~108).**

    Find the section:
    ```
    ### Phase 3.5: Hover UI / HUD
    ...
    **Plans**: 4 plans
    ```

    Update the Plans line to enumerate the four plans with their objectives:
    ```
    **Plans:** 4 plans
      - [x] 03.5-00-PLAN.md — Wave 0 test infrastructure + REQUIREMENTS.md HUD-01..04 stubs + 5 manual walkthrough skeletons (no requirements; foundation) — completed 2026-04-30
      - [x] 03.5-01-PLAN.md — HUD core (hs.canvas overlay + show/hide + lifecycle wiring in onPress/resetState; HUD_OFF env var); HUD-01, HUD-03, HUD-04 (focus half) — completed 2026-04-30
      - [x] 03.5-02-PLAN.md — Configuration surface (PURPLEVOICE_HUD_POSITION + six-named-position arithmetic + console fallback warning + setup.sh banner D-12); HUD-02, HUD-04 (position half) — completed 2026-04-30
      - [x] 03.5-03-PLAN.md — Documentation closure (README + SECURITY.md HUD privacy subsections with RESEARCH Priority 2 honest framing + REQUIREMENTS.md HUD-01..04 finalisation + 3 live manual walkthroughs + 2 documentation walkthroughs); HUD-01..04 — completed 2026-04-30
    ```

    Also update ROADMAP.md `## Coverage Summary` table:
    ```
    | 3.5. Hover UI / HUD | HUD-01, HUD-02, HUD-03, HUD-04 | 4 (TBD) |
    ```
    becomes:
    ```
    | 3.5. Hover UI / HUD | HUD-01, HUD-02, HUD-03, HUD-04 | 4 |
    ```

    Update the **Roadmap extended** trailing footer to add a 2026-04-30 entry noting Phase 3.5 closure.
  </action>

  <verify>
    <automated>set -e; F=.planning/STATE.md; grep -q "Phase: 03.5 (hover-ui-hud) — COMPLETE" "$F" || { echo "STATE.md current position not updated"; exit 1; }; grep -q "Phase 3.5: Hover UI / HUD                 ██████████  100%" "$F" || { echo "STATE.md progress bar not updated"; exit 1; }; grep -qE "completed_phases: 5" "$F" || { echo "STATE.md frontmatter completed_phases not 5"; exit 1; }; R=.planning/ROADMAP.md; grep -qE "\| 5 \| Phase 3\.5: Hover UI / HUD \| 4/4 \| Complete" "$R" || { echo "ROADMAP progress table not updated"; exit 1; }; grep -q "03.5-00-PLAN.md — Wave 0" "$R" || { echo "ROADMAP Phase 3.5 plans list not updated"; exit 1; }; grep -qE "\| 3\.5\. Hover UI / HUD \| HUD-01, HUD-02, HUD-03, HUD-04 \| 4 \|" "$R" || { echo "ROADMAP Coverage Summary not flipped from 4 (TBD) to 4"; exit 1; }; bash tests/test_brand_consistency.sh || { echo "brand consistency regression"; exit 1; }; bash tests/test_security_md_framing.sh || { echo "framing lint regression"; exit 1; }; echo "OK: STATE.md + ROADMAP.md updated to Phase 3.5 closure-ready"</automated>
  </verify>

  <done>
    STATE.md Current Position reflects Phase 3.5 COMPLETE; Progress block shows 100% for Phase 3.5; frontmatter counters bumped (completed_phases 4→5, plan counters); 7 new Key Decisions rows added (with corrected D-14 framing — "does not pursue NSWindowSharingNone exclusion in v1"); Open TODOs reflects Phase 3.5 closure. ROADMAP.md Progress table row 5 flipped from 0/0 Queued to 4/4 Complete; Phase 3.5 details block enumerates the 4 plans with completion stamps; Coverage Summary flipped from "4 (TBD)" to "4". Brand consistency + framing lint stay GREEN.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Functional suite all green:** `bash tests/run_all.sh` reports 10 PASS / 0 FAIL.
2. **Security suite all green:** `bash tests/security/run_all.sh` reports 5 PASS / 0 FAIL.
3. **Brand consistency:** `bash tests/test_brand_consistency.sh` exits 0.
4. **Framing lint:** `bash tests/test_security_md_framing.sh` exits 0 (zero new banned phrases; H2 count preserved at 18).
5. **REQUIREMENTS coverage:** `grep -c '\[x\] \*\*HUD-' .planning/REQUIREMENTS.md` returns 4. All four HUD traceability rows say `Complete`.
6. **README HUD subsection present:** `grep -q "### HUD privacy and screen-recording visibility" README.md`.
7. **SECURITY HUD subsection present:** `grep -q "### HUD visibility in screen recordings" SECURITY.md`.
8. **Live walkthrough sign-off:** A + B + C have Tester / Date filled.
9. **Documentation walkthroughs filled:** D + E have empirical values + Tester: [Documented].
10. **STATE.md + ROADMAP.md reflect Phase 3.5 closure.**
11. **Pattern 2 invariant + corollary still preserved.** No init.lua / setup.sh changes in this plan; should be untouched.
12. **No new XDG paths:** still env-var-only config.
13. **Over-claim framing absent in all D-14 surfaces:** `! grep -qiE "(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)" README.md SECURITY.md .planning/REQUIREMENTS.md` returns clean across all three D-14 documentation surfaces.
</verification>

<success_criteria>
- README.md ## Security & Privacy section has `### HUD privacy and screen-recording visibility` H3 subsection with corrected RESEARCH-Priority-2 honest framing (uses "does not pursue NSWindowSharingNone exclusion in v1", NOT "attempts to hide via NSWindowSharingNone")
- SECURITY.md §"How to Verify These Claims" has `### HUD visibility in screen recordings` H3 subsection (NO new H2 added — H2 count stays at 18) using the same corrected framing
- REQUIREMENTS.md HUD-01..04: all 4 checkboxes flipped to `[x]`; final language present (not Plan 00 stubs); HUD-04 uses corrected D-14 framing; 4 traceability rows say Complete; per-phase counts row says Complete
- 3 live manual walkthroughs (test_hud_appearance, test_hud_disable, test_hud_focus) signed off with Tester + Date
- 2 documentation walkthroughs (test_hud_idle_cpu, test_hud_screen_capture) filled with empirical values
- STATE.md Current Position + Progress + Key Decisions + Open TODOs reflect Phase 3.5 closure (Key Decisions row uses corrected D-14 framing)
- ROADMAP.md Progress table + Phase 3.5 details + Coverage Summary reflect Phase 3.5 closure
- Functional suite 10/0; security suite 5/0
- Framing lint + brand consistency lint GREEN
- Pattern 2 invariant + corollary preserved
- D-14 caveat language uses RESEARCH Priority 2 reframing — never "guarantees absolute"; never "always hidden", "fully hidden", "prevents capture", or "attempts to hide via NSWindowSharingNone"; uses "limited", "does not pursue", "ignored by ScreenCaptureKit", "incidental, not pursued"
- Negative-grep `(guarantees absolute|always hidden|fully hidden|prevents capture|attempts to hide.*NSWindowSharingNone)` returns ZERO matches across README.md, SECURITY.md, and REQUIREMENTS.md
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-hover-ui-hud/03.5-03-SUMMARY.md` documenting:
- Files modified (8: README.md, SECURITY.md, REQUIREMENTS.md, 3 manual walkthrough .md files, STATE.md, ROADMAP.md)
- Live walkthrough A/B/C outcomes (Tester names, observations from each)
- Documentation walkthrough D/E empirical values (CPU baseline / active; HUD visible in legacy screencapture / QuickTime / Zoom Y/N)
- Test suite final state (10/0 functional + 5/0 security; framing lint GREEN; brand consistency GREEN)
- Pattern 2 invariant + corollary status
- D-14 honest framing verbatim language landed in README + SECURITY.md (paste the exact paragraphs — note the corrected "does not pursue NSWindowSharingNone exclusion in v1" phrasing)
- Any deviations from this plan (Rule 1 / inline auto-fix patterns)
- Phase 3.5 closure-readiness checklist (every item from VALIDATION.md Sign-Off section now [x])
- Recommendation for `/gsd:verify-work 3.5` orchestrator invocation
</output>
</output>
