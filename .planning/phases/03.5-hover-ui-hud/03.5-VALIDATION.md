---
phase: 3.5
slug: hover-ui-hud
status: approved
nyquist_compliant: true
wave_0_complete: true
wave_0_completed: 2026-04-30
created: 2026-04-30
---

# Phase 3.5 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.
> Source: `03.5-RESEARCH.md` §"Validation Architecture" — derived from locked decisions in `03.5-CONTEXT.md`.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Bash test scripts (no formal framework — each `.sh` is standalone, exits 0 on pass) — mirrors existing `tests/test_*.sh` convention |
| **Config file** | None (auto-globbing aggregator) |
| **Quick run command** | `bash tests/run_all.sh` |
| **Full suite command** | `bash tests/run_all.sh && bash tests/security/run_all.sh` |
| **Manual walkthroughs** | Markdown checklists in `tests/manual/` (not auto-aggregated; tracked via this VALIDATION.md) |
| **Estimated runtime** | ~5s (functional) + ~30s (security) — HUD adds 2 new bash unit tests |

---

## Sampling Rate

- **After every task commit:** Run `bash tests/run_all.sh` (existing 8 PASS / 0 FAIL functional suite + 2 new `tests/test_hud_*.sh` after Wave 0 = 10 GREEN target)
- **After every plan wave:** Run `bash tests/run_all.sh && bash tests/security/run_all.sh` (confirms HUD code in `init.lua` doesn't break Phase 2.7's 5 PASS / 0 FAIL security suite)
- **Phase gate (release):** Full suite GREEN + 3 of 5 manual walkthroughs signed off live + framing lint clean + brand consistency clean — verified by `/gsd:verify-work 3.5`
- **Max feedback latency:** ~35 seconds for full suite

---

## Per-Task Verification Map

| Req ID | Behavior | Plan | Test Type | Automated Command | File Exists | Status |
|--------|----------|------|-----------|-------------------|-------------|--------|
| HUD-01 | HUD appears on press, disappears on release (~50ms appear, ~250ms disappear) | 03.5-01 | manual (live) | `cat tests/manual/test_hud_appearance.md` (visual stopwatch check) | ❌ Wave 0 | ⬜ pending |
| HUD-02 (env-var wiring) | `PURPLEVOICE_HUD_OFF=1` disables HUD entirely; string-level wiring | 03.5-01, 03.5-02 | unit | `bash tests/test_hud_env_off.sh` | ❌ Wave 0 | ⬜ pending |
| HUD-02 (live integration) | `PURPLEVOICE_HUD_OFF=1` honoured in live Hammerspoon; reload picks up change | 03.5-03 | manual (live) | `cat tests/manual/test_hud_disable.md` | ❌ Wave 0 | ⬜ pending |
| HUD-03 | Zero CPU when idle (no animation loops; only redraws on state change) | 03.5-01, 03.5-03 | manual (documentation) | `cat tests/manual/test_hud_idle_cpu.md` (`top -pid <hammerspoon>` baseline measurement) | ❌ Wave 0 | ⬜ pending |
| HUD-04 (focus) | HUD does not steal focus; clicks on app behind pass through | 03.5-01 | manual (live) | `cat tests/manual/test_hud_focus.md` | ❌ Wave 0 | ⬜ pending |
| HUD-04 (paste) | HUD does not interfere with paste — existing pasteWithRestore() flow unaffected | 03.5-01 | unit (existing) | `bash tests/run_all.sh` (8 GREEN tests including the existing functional suite) | ✅ existing | ⬜ pending |
| HUD-04 (position) | Six named positions resolve to coordinates; invalid value falls back to `top-center` with warning | 03.5-02 | unit | `bash tests/test_hud_position_validation.sh` | ❌ Wave 0 | ⬜ pending |
| HUD-04 (recordings) | HUD limited-visibility in screen recordings — honest framing per RESEARCH Priority 2 (ScreenCaptureKit on macOS 15+ ignores NSWindowSharingNone) | 03.5-03 | manual (documentation-only) | `cat tests/manual/test_hud_screen_capture.md` (documents the limitation; NOT a pass/fail gate) | ❌ Wave 0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

**Notes:**
- HUD-04 (recordings) is **documentation-only**, not a pass/fail gate. Per RESEARCH Priority 2: macOS Sequoia 15+ ScreenCaptureKit-based capture tools (modern QuickTime, OBS, Zoom share-screen, Discord, Loom, Teams) capture the HUD regardless of NSWindowSharingNone. The honest framing in README + SECURITY.md is "limited; ScreenCaptureKit-based tools on macOS 15+ will capture the HUD regardless. For sensitive sessions, run with `PURPLEVOICE_HUD_OFF=1`." This still passes the Phase 2.7 framing lint (no "guarantees" / "compliant" / "certified" claims).
- HUD-01 (manual live) and HUD-03 (manual documentation) — per RESEARCH Open Questions §1, planner picks 3 of 5 walkthroughs to run live: `test_hud_appearance`, `test_hud_disable`, `test_hud_focus`. The other 2 (`test_hud_idle_cpu`, `test_hud_screen_capture`) are documentation-only.
- HUD-04 (paste) is covered by the existing functional suite — Wave 0 confirms `bash tests/run_all.sh` exits 0 with the existing 8 GREEN tests; HUD code in `init.lua` must not regress them.

---

## Wave 0 Requirements

### New test files (`tests/`)
- [ ] `tests/test_hud_env_off.sh` — HUD-02 unit test; asserts `init.lua` reads `PURPLEVOICE_HUD_OFF` at module-load and that the read pattern matches the existing `PURPLEVOICE_NO_SOUNDS` idiom (line 99 of init.lua)
- [ ] `tests/test_hud_position_validation.sh` — HUD-04 position unit test; asserts the six valid named positions are recognised AND that an invalid value falls back to `top-center` with a console warning

### New manual walkthroughs (`tests/manual/`)
- [ ] `tests/manual/test_hud_appearance.md` — HUD-01 live walkthrough (visual stopwatch check; user holds hotkey, observes HUD pill appearing top-center within ~50ms, disappearing within ~250ms after release)
- [ ] `tests/manual/test_hud_disable.md` — HUD-02 live end-to-end walkthrough (set `PURPLEVOICE_HUD_OFF=1`, reload Hammerspoon, confirm HUD does not appear)
- [ ] `tests/manual/test_hud_idle_cpu.md` — HUD-03 documentation-driven walkthrough (open Activity Monitor or `top -pid <hammerspoon>`; record idle CPU baseline; confirm HUD does not introduce CPU draw when not visible)
- [ ] `tests/manual/test_hud_focus.md` — HUD-04 focus live walkthrough (click on an app window behind the HUD; confirm focus passes through; HUD does not become focused)
- [ ] `tests/manual/test_hud_screen_capture.md` — HUD-04 recordings documentation walkthrough (uses `screencapture` CLI for the legacy CGWindowList path, optionally tries OBS/QuickTime to confirm the ScreenCaptureKit limitation; documents results as evidence; NOT a pass/fail gate)

### Tooling
- No new framework install needed — bash + `top` + `screencapture` are macOS built-ins
- No new env vars beyond the two locked in CONTEXT.md (`PURPLEVOICE_HUD_OFF`, `PURPLEVOICE_HUD_POSITION`)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| HUD appears within ~50ms of press, disappears within ~250ms of release | HUD-01 | Animation timing is observable but not unit-testable from bash. Hammerspoon's `hs.canvas:show(fadeIn)` accepts seconds-as-float; the planner picks 0 (instant) or 0.05 (~50ms) for show. Live observation via stopwatch on phone or `time` over a known-duration press is the verification. | (1) Run `tests/manual/test_hud_appearance.md` (2) Press cmd+shift+e and hold for 2s, release (3) Observe HUD pill appearing at top-center of focused screen within ~50ms (4) Observe disappearing within ~250ms (5) Sign off |
| HUD does not steal focus from active window | HUD-04 (focus) | `hs.canvas` defaults to `ignoresMouseEvents = YES` per RESEARCH Priority 3 (verified in libcanvas.m source); HUD never appears in `cmd+tab` switcher. Verifier confirms in live test. | (1) Open a text editor with focus (2) Press cmd+shift+e to show HUD (3) Click somewhere on the focused editor; confirm typing still goes to editor, not HUD (4) Try cmd+tab; confirm HUD does not appear in app switcher |
| Zero CPU when idle | HUD-03 | Activity Monitor / `top -pid` is the measurement tool; "zero" means flat baseline, no measurable bump from HUD when not visible. Documentation-only — not a pass/fail gate. | (1) Open `top -pid $(pgrep Hammerspoon | head -1)` (2) Observe baseline CPU% with HUD hidden (3) Press cmd+shift+e (HUD visible during press); observe CPU% (4) Release; observe return to baseline (5) Document baseline + active values; no specific threshold required |
| Limited-visibility in screen recordings (ScreenCaptureKit limitation) | HUD-04 (recordings) | Per RESEARCH Priority 2: NSWindowSharingNone is ignored by ScreenCaptureKit on macOS 15+. This walkthrough documents the limitation honestly. NOT a pass/fail gate. | (1) Use legacy `screencapture` CLI (`screencapture -x /tmp/test.png` while HUD visible) — HUD likely absent (CGWindowList honours sharing flags) (2) Optionally try modern recording tool (e.g., QuickTime Screen Recording on macOS 15+) — HUD likely PRESENT (ScreenCaptureKit ignores sharing flags) (3) Document results as evidence (4) For sensitive sessions, run with `PURPLEVOICE_HUD_OFF=1` |
| Pre-release: HUD-disable end-to-end on Oliver's actual machine | HUD-02 | The bash unit test asserts string-level wiring; live Hammerspoon reload + env var pickup is observable behaviour. | (1) Set `export PURPLEVOICE_HUD_OFF=1` in shell (2) `hs -c "hs.reload()"` or open Hammerspoon → Reload Config (3) Press cmd+shift+e (4) Confirm HUD does NOT appear; menubar indicator DOES appear (5) Unset env var, reload, confirm HUD reappears |

---

## Validation Sign-Off

- [x] All HUD-XX requirements have `<automated>` verify (unit tests) OR `<manual>` walkthrough OR Wave 0 dependency
- [x] Sampling continuity: no 3 consecutive tasks without automated verify (achieved via per-commit `bash tests/run_all.sh` baseline)
- [x] Wave 0 covers all MISSING references (7 Wave 0 items above) — note: 2 tests + 5 walkthroughs = 7
- [x] No watch-mode flags
- [x] Feedback latency < 60s
- [ ] 3 of 5 manual walkthroughs run live + signed off in plan SUMMARY.md (HUD-01, HUD-02 live, HUD-04 focus)
- [ ] 2 of 5 manual walkthroughs documentation-only (HUD-03 idle CPU, HUD-04 recordings)
- [ ] D-14 caveat language matches RESEARCH Priority 2 reframing (honest about ScreenCaptureKit limitation)
- [x] `nyquist_compliant: true` set in frontmatter (after Wave 0 plan completes)

**Approval:** approved 2026-04-30 — Wave 0 plan 03.5-00 closed: 2 unit tests (RED-and-expected pre-wiring) + 5 manual walkthrough skeletons + REQUIREMENTS.md HUD-01..04 stubs + 4 traceability rows landed.

---

*Generated from `03.5-RESEARCH.md` §"Validation Architecture" on 2026-04-30.*
*Phase: 03.5-hover-ui-hud*
