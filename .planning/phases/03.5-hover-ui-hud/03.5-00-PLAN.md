---
phase: 03.5-hover-ui-hud
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/test_hud_env_off.sh
  - tests/test_hud_position_validation.sh
  - tests/manual/test_hud_appearance.md
  - tests/manual/test_hud_disable.md
  - tests/manual/test_hud_idle_cpu.md
  - tests/manual/test_hud_focus.md
  - tests/manual/test_hud_screen_capture.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md
autonomous: true
requirements:
  - HUD-01
  - HUD-02
  - HUD-03
  - HUD-04

must_haves:
  truths:
    - "Two new bash unit tests exist and exit 0 against the *current* (pre-HUD) init.lua — they assert NEGATIVE invariants now (HUD env vars NOT yet wired) so they will RED on Wave 0 close and GREEN after Plans 01/02 land the wiring."
    - "Five manual walkthrough skeletons exist in tests/manual/ with prerequisites, steps, expected outcome, and sign-off blocks — fillable by Plan 03."
    - "REQUIREMENTS.md HUD-01..04 are no longer 'TBD'; each has a one-sentence stub derived from CONTEXT.md decisions and a Pending traceability row."
    - "tests/run_all.sh aggregator auto-discovers the two new test_hud_*.sh files (it globs tests/test_*.sh) — the existing 8 PASS / 0 FAIL functional suite becomes 10 RED-and-expected during Wave 0 (the new tests assert HUD wiring that does not yet exist), then 10 PASS / 0 FAIL after Plans 01/02 land. Wave 0 commits the tests in their PRE-WIRING state assertion form so they go GREEN once init.lua adds the wiring; no test file should ever be RED at commit time."
    - "VALIDATION.md frontmatter wave_0_complete flips false → true and nyquist_compliant: true is set."
  artifacts:
    - path: "tests/test_hud_env_off.sh"
      provides: "HUD-02 string-level env-var wiring assertion; will go GREEN once init.lua reads PURPLEVOICE_HUD_OFF"
      contains: "PURPLEVOICE_HUD_OFF"
    - path: "tests/test_hud_position_validation.sh"
      provides: "HUD-04 position-name list assertion; will go GREEN once init.lua exposes the six valid positions and the fallback warning"
      contains: "top-center"
    - path: "tests/manual/test_hud_appearance.md"
      provides: "HUD-01 live walkthrough skeleton (visual stopwatch check)"
      contains: "PurpleVoice HUD: appearance"
    - path: "tests/manual/test_hud_disable.md"
      provides: "HUD-02 live end-to-end walkthrough skeleton"
      contains: "PURPLEVOICE_HUD_OFF=1"
    - path: "tests/manual/test_hud_idle_cpu.md"
      provides: "HUD-03 documentation-driven walkthrough skeleton"
      contains: "top -pid"
    - path: "tests/manual/test_hud_focus.md"
      provides: "HUD-04 focus walkthrough skeleton"
      contains: "focus passes through"
    - path: "tests/manual/test_hud_screen_capture.md"
      provides: "HUD-04 recordings documentation walkthrough skeleton (limitations from RESEARCH Priority 2)"
      contains: "ScreenCaptureKit"
    - path: ".planning/REQUIREMENTS.md"
      provides: "HUD-01..04 with stub language and 4 new traceability rows (Pending)"
      contains: "HUD-01"
  key_links:
    - from: "tests/run_all.sh"
      to: "tests/test_hud_*.sh"
      via: "for f in tests/test_*.sh glob"
      pattern: "tests/test_\\*\\.sh"
    - from: ".planning/REQUIREMENTS.md HUD-01..04"
      to: ".planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md D-01..D-16"
      via: "stub language paraphrases CONTEXT decisions"
      pattern: "HUD-0[1234]"
    - from: ".planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md"
      to: "wave_0_complete: true + nyquist_compliant: true"
      via: "frontmatter flip"
      pattern: "wave_0_complete: true"
---

<objective>
Wave 0 staging for Phase 3.5. Establish the test scaffolding, manual-walkthrough skeletons, and REQUIREMENTS.md formalisation BEFORE any HUD code lands. Mirrors the Phase 2 Plan 02-00 / Phase 2.7 Plan 02.7-00 Wave-0 pattern: write the tests that the implementation plans will satisfy, so each subsequent plan ships with a green automated check.

Purpose: De-risk the implementation phase. Writing tests first means Plans 01/02 cannot accidentally drift from the locked CONTEXT.md decisions — every env-var name, every position string, every fallback behaviour is pinned by a bash test before the Lua is written. Also reframes the four TBD HUD-XX requirements into concrete stubs with traceability rows so the REQUIREMENTS.md state is unambiguous from Wave 0 onward.

Output:
  - 2 new `tests/test_hud_*.sh` bash unit tests (committed in their pre-wiring assertion form — they ASSERT the wiring exists; they will RED before Plan 01 lands and GREEN after).
  - 5 new `tests/manual/test_hud_*.md` walkthrough skeletons (Prerequisites / Steps / Expected Outcome / Failure modes / Sign-off blocks — fillable by Plan 03).
  - `.planning/REQUIREMENTS.md` HUD-01..04 stubs + 4 Pending traceability rows.
  - `.planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md` frontmatter `wave_0_complete: true` + `nyquist_compliant: true`.

CRITICAL pre-condition: at Wave 0 commit time, `bash tests/run_all.sh` will report 8 PASS / 2 FAIL (the two new test_hud_*.sh tests RED, the existing 8 still GREEN). This is the **expected** Wave 0 close state — Plan 01 will turn the 2 RED tests GREEN by wiring init.lua. Document this in the SUMMARY explicitly so the orchestrator does not flag the regression.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md
@.planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md
@.planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md
@purplevoice-lua/init.lua
@tests/run_all.sh
@tests/test_brand_consistency.sh
@tests/test_denylist.sh
@tests/manual/test_menubar.md

<interfaces>
<!-- Reference patterns the executor must mirror. -->

From tests/run_all.sh (auto-globbing aggregator):
```bash
for f in tests/test_*.sh; do
  [ -f "$f" ] || continue
  printf "  [test] %-40s " "$(basename "$f")"
  if bash "$f" >/tmp/purplevoice-test.log 2>&1; then
    echo "PASS"; PASS=$((PASS + 1))
  else
    echo "FAIL"; FAIL=$((FAIL + 1)); FAILED+=("$f")
  fi
done
```
New test_hud_*.sh scripts are picked up automatically. No edit to run_all.sh needed.

From tests/test_denylist.sh (existing pattern for grep-based bash tests):
```bash
#!/usr/bin/env bash
set -uo pipefail
# ... grep / case / exit 1 on first failure with descriptive FAIL message
echo "PASS: <description>"
exit 0
```

From tests/manual/test_menubar.md (existing markdown walkthrough skeleton):
```markdown
# Manual Test: <Name> (<REQ-ID>)

**Requirement:** <REQ-ID> — <one sentence>

**Prerequisites:** <what must be set up>

## Steps
1. ...
2. ...

## Expected Outcome
- ...

## Failure modes
- <symptom> → <diagnostic>

## Sign-off
- [ ] <criterion>

**Tester:** _____________  **Date:** _____________
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0-1: Write tests/test_hud_env_off.sh (HUD-02 string-level wiring assertion)</name>

  <read_first>
    - tests/test_denylist.sh (full file — pattern for grep-based bash unit tests)
    - tests/test_brand_consistency.sh (full file — pattern for set -uo pipefail + grep + exit codes)
    - tests/run_all.sh (aggregator behaviour — auto-globs tests/test_*.sh)
    - purplevoice-lua/init.lua lines 95-99 (the existing PURPLEVOICE_NO_SOUNDS read pattern this test mirrors)
    - .planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md sections D-09..D-11 (env-var locked decisions)
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md Priority 7 + Priority 9 (env-var pattern + test discipline)
  </read_first>

  <behavior>
    The test asserts string-level wiring. It does NOT launch Hammerspoon or test live behaviour.

    Test cases (each is a grep on purplevoice-lua/init.lua):
    - Test 1 (POSITIVE): the file contains a line reading PURPLEVOICE_HUD_OFF via os.getenv (e.g., `os.getenv("PURPLEVOICE_HUD_OFF")`).
    - Test 2 (POSITIVE): the file contains the negation idiom `~= "1"` adjacent to the PURPLEVOICE_HUD_OFF read (matches the existing PURPLEVOICE_NO_SOUNDS line 99 idiom — `(os.getenv("PURPLEVOICE_NO_SOUNDS") ~= "1")`). This pins the default-ON semantics (D-09).
    - Test 3 (NEGATIVE): the file does NOT contain `VOICE_CC_HUD` (Phase 2.5 brand-consistency invariant — no legacy namespace; per D-10 + Pitfall 10).
    - Test 4 (NEGATIVE): the file does NOT contain `whisper-cli` (Pattern 2 corollary — HUD code stays whisper-cli-free).

    Initial state at Wave 0 commit time: Tests 1 + 2 will FAIL (init.lua does not yet read PURPLEVOICE_HUD_OFF). This is the **expected RED state**. Plan 01 turns it GREEN by adding the read at module load.

    Tests 3 + 4 should already PASS at Wave 0 commit time (init.lua has neither string yet). They function as regression catches for Plans 01/02.
  </behavior>

  <files>tests/test_hud_env_off.sh</files>

  <action>
    Create the file with `chmod +x` semantics (executable bit). Use the existing tests/test_denylist.sh as the structural template. Required content:

    ```bash
    #!/usr/bin/env bash
    # tests/test_hud_env_off.sh — HUD-02 string-level env-var wiring assertion (Phase 3.5)
    #
    # Asserts that purplevoice-lua/init.lua reads PURPLEVOICE_HUD_OFF at module load
    # using the same idiom as the existing PURPLEVOICE_NO_SOUNDS line 99 read.
    # Default-ON semantics (D-09): HUD enabled unless env var equals "1".
    #
    # This test is COMMITTED in Wave 0 (Plan 03.5-00). At that commit it will FAIL
    # because init.lua does not yet read PURPLEVOICE_HUD_OFF. Plan 03.5-01 lands
    # the read and turns this test GREEN. Wave 0 close is documented as 8 PASS /
    # 2 FAIL (this + test_hud_position_validation.sh) — expected RED state.
    #
    # Exit 0 = wiring present + brand-clean; exit 1 = wiring absent or drift.
    set -uo pipefail
    cd "$(dirname "$0")/.."   # repo root

    INIT="purplevoice-lua/init.lua"

    if [ ! -r "$INIT" ]; then
      echo "FAIL [test_hud_env_off.sh]: $INIT not readable"
      exit 1
    fi

    FAIL=0

    # 1. POSITIVE: PURPLEVOICE_HUD_OFF is read via os.getenv.
    if ! grep -qF 'os.getenv("PURPLEVOICE_HUD_OFF")' "$INIT"; then
      echo "FAIL: $INIT does not call os.getenv(\"PURPLEVOICE_HUD_OFF\") — HUD-02 wiring absent"
      FAIL=1
    fi

    # 2. POSITIVE: default-ON semantics — the read uses ~= "1" (mirrors line 99).
    #    Single grep that requires both the env-var name and the ~= "1" idiom on
    #    the same line. (Plan 03.5-01 must follow the existing PURPLEVOICE_NO_SOUNDS
    #    pattern: `local hudEnabled = (os.getenv("PURPLEVOICE_HUD_OFF") ~= "1")`.)
    if ! grep -qE 'PURPLEVOICE_HUD_OFF.*~= ?"1"' "$INIT"; then
      echo "FAIL: $INIT PURPLEVOICE_HUD_OFF read does not use ~= \"1\" default-ON idiom (D-09)"
      FAIL=1
    fi

    # 3. NEGATIVE: no legacy VOICE_CC_HUD namespace (Phase 2.5 brand invariant + D-10).
    if grep -qE 'VOICE_CC_HUD' "$INIT"; then
      echo "FAIL: $INIT contains legacy VOICE_CC_HUD reference — Phase 2.5 brand invariant violated (D-10 PURPLEVOICE_ namespace)"
      FAIL=1
    fi

    # 4. NEGATIVE: Pattern 2 corollary — no whisper-cli reference in init.lua.
    if grep -q "whisper-cli" "$INIT"; then
      echo "FAIL: $INIT contains 'whisper-cli' reference — Pattern 2 corollary violated"
      FAIL=1
    fi

    if [ "$FAIL" -eq 0 ]; then
      echo "PASS [test_hud_env_off.sh]: HUD-02 wiring present + brand-clean + Pattern 2 corollary intact"
      exit 0
    fi
    exit 1
    ```

    After writing, run `chmod +x tests/test_hud_env_off.sh`.
  </action>

  <verify>
    <automated>[ -x tests/test_hud_env_off.sh ] &amp;&amp; bash -n tests/test_hud_env_off.sh &amp;&amp; echo "syntax OK; runtime exit code expected RED at Wave 0 commit because init.lua not yet wired" &amp;&amp; bash tests/test_hud_env_off.sh; rc=$?; if [ "$rc" -eq 1 ]; then echo "RED-and-expected (exit 1) — Plan 03.5-01 will turn GREEN"; exit 0; else echo "UNEXPECTED: test exited $rc — investigate"; exit 1; fi</automated>
  </verify>

  <done>
    File exists at tests/test_hud_env_off.sh with executable bit set, valid bash syntax (`bash -n` exits 0), runs as a standalone script, and currently exits 1 with the expected FAIL messages (init.lua does not yet contain PURPLEVOICE_HUD_OFF). The deviation is captured in the SUMMARY as "Wave 0 RED-and-expected — Plan 01 turns GREEN".
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 0-2: Write tests/test_hud_position_validation.sh (HUD-04 position-name list assertion)</name>

  <read_first>
    - tests/test_hud_env_off.sh (just created — same structural template)
    - tests/test_denylist.sh (existing pattern reference)
    - .planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md D-07 (position list locked: top-center | top-right | bottom-center | bottom-right | near-cursor | center)
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md Priority 6 (position arithmetic) + Priority 9 (test patterns)
  </read_first>

  <behavior>
    String-level wiring assertion. Asserts the six locked named positions appear as string literals in init.lua, AND that PURPLEVOICE_HUD_POSITION is read via os.getenv, AND that an invalid-fallback warning string is present.

    Test cases:
    - Test 1 (POSITIVE): file contains `os.getenv("PURPLEVOICE_HUD_POSITION")`.
    - Test 2 (POSITIVE): all six position name literals present — `top-center`, `top-right`, `bottom-center`, `bottom-right`, `near-cursor`, `center` (each as a quoted string).
    - Test 3 (POSITIVE): the default position string `top-center` appears at least twice (once in the validation map, once as the `or "top-center"` fallback per RESEARCH Priority 6 / Pattern 4).
    - Test 4 (POSITIVE): the file contains a fallback-warning console message — `hs.console.printStyledtext` adjacent to a string mentioning "PURPLEVOICE_HUD_POSITION" or "HUD" + "fall" / "fallback" / "invalid" (matches D-07 spec: "on invalid value, falls back to top-center and prints a single warning to the Hammerspoon console").
    - Test 5 (NEGATIVE): no `PURPLEVOICE_HUD_X` or `PURPLEVOICE_HUD_Y` (coordinate overrides explicitly rejected per Deferred Ideas in CONTEXT.md).
    - Test 6 (NEGATIVE): no `PURPLEVOICE_HUD_ALPHA` or `PURPLEVOICE_HUD_COLOR` (rejected per D-10 — two knobs only).

    Initial state at Wave 0 commit time: Tests 1-4 will FAIL (init.lua not yet wired). Tests 5-6 PASS as regression catches.
  </behavior>

  <files>tests/test_hud_position_validation.sh</files>

  <action>
    Mirror the structure of test_hud_env_off.sh. Use a `for pos in ...` loop for Test 2 to cleanly enumerate the six. Required content:

    ```bash
    #!/usr/bin/env bash
    # tests/test_hud_position_validation.sh — HUD-04 position-name wiring (Phase 3.5)
    #
    # Asserts purplevoice-lua/init.lua exposes the six locked named positions
    # (D-07): top-center | top-right | bottom-center | bottom-right | near-cursor | center,
    # reads PURPLEVOICE_HUD_POSITION via os.getenv, and prints a fallback warning
    # to the Hammerspoon console on invalid values (D-07).
    #
    # COMMITTED in Wave 0 (Plan 03.5-00) in pre-wiring RED state. Plan 03.5-02
    # turns it GREEN by adding the validation map + fallback warning.
    #
    # Exit 0 = wiring present + no rejected env vars; exit 1 = wiring absent.
    set -uo pipefail
    cd "$(dirname "$0")/.."   # repo root

    INIT="purplevoice-lua/init.lua"

    if [ ! -r "$INIT" ]; then
      echo "FAIL [test_hud_position_validation.sh]: $INIT not readable"
      exit 1
    fi

    FAIL=0

    # 1. PURPLEVOICE_HUD_POSITION is read via os.getenv.
    if ! grep -qF 'os.getenv("PURPLEVOICE_HUD_POSITION")' "$INIT"; then
      echo "FAIL: $INIT does not call os.getenv(\"PURPLEVOICE_HUD_POSITION\")"
      FAIL=1
    fi

    # 2. All six locked named positions present as quoted string literals (D-07).
    for pos in top-center top-right bottom-center bottom-right near-cursor center; do
      if ! grep -qF "\"$pos\"" "$INIT"; then
        echo "FAIL: $INIT missing position name literal \"$pos\" (D-07)"
        FAIL=1
      fi
    done

    # 3. Default 'top-center' appears at least twice (validation map + fallback).
    COUNT=$(grep -cF '"top-center"' "$INIT" 2>/dev/null || echo 0)
    if [ "$COUNT" -lt 2 ]; then
      echo "FAIL: $INIT \"top-center\" appears $COUNT times (expected ≥2: validation map + fallback per D-07/Pattern 4)"
      FAIL=1
    fi

    # 4. Fallback-warning console message present (D-07: 'falls back to top-center
    #    and prints a single warning to the Hammerspoon console at module load').
    if ! grep -qE 'hs\.console\.printStyledtext' "$INIT"; then
      echo "FAIL: $INIT does not call hs.console.printStyledtext (D-07 fallback warning required)"
      FAIL=1
    elif ! grep -B1 -A1 'hs\.console\.printStyledtext' "$INIT" | grep -qiE 'PURPLEVOICE_HUD_POSITION|invalid|fall'; then
      echo "FAIL: $INIT hs.console.printStyledtext call not associated with HUD-position fallback message"
      FAIL=1
    fi

    # 5. NEGATIVE: rejected coordinate-override env vars must not appear (Deferred Ideas).
    for rejected in PURPLEVOICE_HUD_X PURPLEVOICE_HUD_Y; do
      if grep -qF "$rejected" "$INIT"; then
        echo "FAIL: $INIT contains rejected env var $rejected (Deferred — coordinate-precise overrides excluded)"
        FAIL=1
      fi
    done

    # 6. NEGATIVE: rejected visual-knob env vars must not appear (D-10 — two knobs only).
    for rejected in PURPLEVOICE_HUD_ALPHA PURPLEVOICE_HUD_COLOR PURPLEVOICE_HUD_COLOUR; do
      if grep -qF "$rejected" "$INIT"; then
        echo "FAIL: $INIT contains rejected env var $rejected (D-10 — only HUD_OFF + HUD_POSITION allowed)"
        FAIL=1
      fi
    done

    if [ "$FAIL" -eq 0 ]; then
      echo "PASS [test_hud_position_validation.sh]: HUD-04 position wiring + fallback warning present; no rejected env vars"
      exit 0
    fi
    exit 1
    ```

    Run `chmod +x tests/test_hud_position_validation.sh`.
  </action>

  <verify>
    <automated>[ -x tests/test_hud_position_validation.sh ] &amp;&amp; bash -n tests/test_hud_position_validation.sh &amp;&amp; bash tests/test_hud_position_validation.sh; rc=$?; if [ "$rc" -eq 1 ]; then echo "RED-and-expected at Wave 0 — Plan 03.5-02 will turn GREEN"; exit 0; else echo "UNEXPECTED rc=$rc"; exit 1; fi</automated>
  </verify>

  <done>
    File exists at tests/test_hud_position_validation.sh, executable, valid bash syntax, currently exits 1 with FAIL messages indicating PURPLEVOICE_HUD_POSITION not yet read. RED-and-expected captured in SUMMARY.
  </done>
</task>

<task type="auto">
  <name>Task 0-3: Create five tests/manual/test_hud_*.md walkthrough skeletons</name>

  <read_first>
    - tests/manual/test_menubar.md (existing skeleton — the structural template)
    - tests/manual/test_clipboard_restore.md (additional reference)
    - tests/manual/test_tcc_notification.md (additional reference for live walkthroughs)
    - .planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md (Wave 0 list of 5 walkthroughs + their pass criteria)
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md Priority 2 (honest D-14 framing for test_hud_screen_capture.md)
    - .planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md D-13..D-15 (recordings privacy + caveat)
  </read_first>

  <action>
    Create five markdown skeletons under tests/manual/. Each follows the test_menubar.md pattern: H1 title with REQ-ID, Requirement bullet, Prerequisites, Steps (numbered), Expected Outcome, Failure modes, Sign-off (checkbox + Tester/Date).

    The skeletons are FILLABLE — Plan 03.5-03 will populate the steps with live values once the HUD ships. For Wave 0 the skeletons must contain enough scaffolding that any executor can complete them without re-reading CONTEXT.md.

    Do NOT mark any walkthrough as live-tested at Wave 0; the sign-off blocks stay unchecked.

    File 1 — `tests/manual/test_hud_appearance.md` (HUD-01):
    ```markdown
    # Manual Test: HUD appearance + disappearance timing (HUD-01)

    **Requirement:** HUD-01 — Floating canvas widget appears within ~50ms of hotkey press and disappears within ~250ms of release.

    **Prerequisites:**
    - Phase 3.5 PurpleVoice HUD deployed (Plans 03.5-01 + 03.5-02 complete)
    - Hammerspoon launched + permissions granted (Microphone + Accessibility)
    - `~/.hammerspoon/init.lua` contains `require("purplevoice")`
    - PURPLEVOICE_HUD_OFF unset (or = anything other than "1")
    - Default position (top-center) — i.e. PURPLEVOICE_HUD_POSITION unset or = "top-center"

    ## Steps
    1. Reload Hammerspoon (menubar → Reload Config). Confirm the load alert appears.
    2. Note the menubar dot is in idle (○ outline lavender) state.
    3. Hold cmd+shift+e for ~2 seconds while watching the top-center of the active screen.
    4. Observe: a lavender pill (~140×36) reading "● Recording" should appear at top-center, ~50px below the menubar, within one perceptual frame of the press (no visible lag).
    5. Release cmd+shift+e. Observe: the pill should fade out within ~250ms (perceptually "just disappeared").
    6. Repeat 3-5 times. Behaviour should be consistent.
    7. Optional: a stopwatch on a phone can be used to confirm appearance < 100ms and disappearance < 300ms (the 50/250 numbers in HUD-01 are below human perception thresholds; observation suffices).

    ## Expected Outcome
    - Press → pill appears at top-center, lavender, ● Recording text, no perceptible delay.
    - Hold → pill stays visible.
    - Release → pill fades out within a quarter second.
    - Repeated press cycles produce the same behaviour.
    - No HUD when idle.

    ## Failure modes
    - Pill does not appear → check Hammerspoon console for `hs.canvas` errors; verify `_G._purplevoice_hud` is non-nil.
    - Pill stuck visible after release → check `hideHUD()` is called from `resetState()` (RESEARCH Priority 12).
    - Pill appears but on wrong screen → multi-monitor focused-window screen resolution check (`hs.window.focusedWindow():screen()` per RESEARCH Pattern 3).
    - Pill flickers / double-render → check the defensive `_G._purplevoice_hud` cleanup at module load (RESEARCH Pitfall 6).

    ## Sign-off
    - [ ] Pill appears within one frame of press (no perceptible delay)
    - [ ] Pill is centered horizontally at top of active screen, ~50px below menubar
    - [ ] Pill text is "● Recording" in white on lavender (#B388EB α≈0.85)
    - [ ] Pill disappears within ~250ms of release
    - [ ] Behaviour consistent across 3+ press cycles

    **Tester:** _____________  **Date:** _____________
    ```

    File 2 — `tests/manual/test_hud_disable.md` (HUD-02 live end-to-end):
    ```markdown
    # Manual Test: HUD disabled via PURPLEVOICE_HUD_OFF=1 (HUD-02)

    **Requirement:** HUD-02 — User-toggleable visibility via env var read once at module load.

    **Prerequisites:**
    - Phase 3.5 HUD deployed (Plans 03.5-01 + 03.5-02 complete)
    - Hammerspoon launched + permissions granted
    - test_hud_appearance.md previously confirmed PASS (HUD-01 baseline)

    ## Steps
    1. From the shell: `export PURPLEVOICE_HUD_OFF=1`
    2. Reload Hammerspoon: `hs -c "hs.reload()"` OR menubar → Reload Config.
    3. Confirm the load alert appears.
    4. Hold cmd+shift+e for ~2 seconds.
    5. Observe: NO HUD pill appears.
    6. Confirm the menubar indicator DOES change to recording (filled ●) — Phase 2 menubar still ships per HUD-02 spec.
    7. Confirm paste still works — type into a focused text editor first, hold hotkey, speak, release; transcript should paste normally.
    8. Unset: `unset PURPLEVOICE_HUD_OFF` (or `export PURPLEVOICE_HUD_OFF=0` — anything other than "1").
    9. Reload Hammerspoon.
    10. Hold cmd+shift+e — HUD should reappear (default-ON per D-09).

    ## Expected Outcome
    - With PURPLEVOICE_HUD_OFF=1, no HUD on press; menubar indicator unchanged in behaviour; paste path unaffected.
    - With env var unset / not "1", HUD reappears.

    ## Failure modes
    - HUD still appears with PURPLEVOICE_HUD_OFF=1 → env var not being read at module load; check the `os.getenv("PURPLEVOICE_HUD_OFF")` call landed in init.lua and that Hammerspoon was actually reloaded after setting the env var (parent shell env propagates only on Hammerspoon launch from that shell, OR via `launchctl setenv PURPLEVOICE_HUD_OFF 1` for system-wide). If still failing: check `hs.execute("env | grep PURPLEVOICE")` from Hammerspoon console.
    - Menubar indicator stops working when HUD off → `setMenubarRecording()` accidentally gated on `hudEnabled`; fix to keep menubar lifecycle independent.
    - Paste broken with HUD off → HUD code accidentally interfered with `pasteWithRestore()`; should not be possible if HUD is purely additive.

    ## Sign-off
    - [ ] PURPLEVOICE_HUD_OFF=1 + reload → no HUD on press
    - [ ] Menubar still changes state (filled ● during press)
    - [ ] Paste path unaffected (transcript appears in focused text field)
    - [ ] Env var unset + reload → HUD returns

    **Tester:** _____________  **Date:** _____________
    ```

    File 3 — `tests/manual/test_hud_idle_cpu.md` (HUD-03 documentation-only):
    ```markdown
    # Manual Test: HUD idle CPU baseline (HUD-03)

    **Requirement:** HUD-03 — Effectively zero CPU when idle (no animation loops; only redraws on state change).

    **Status:** Documentation-only walkthrough — captures the baseline measurement; no specific pass/fail threshold.

    **Prerequisites:**
    - Phase 3.5 HUD deployed
    - Activity Monitor available (or `top` CLI)
    - HUD enabled (PURPLEVOICE_HUD_OFF unset)

    ## Steps
    1. Open Terminal: `top -pid $(pgrep Hammerspoon | head -1) -stats pid,command,cpu`
    2. Wait ~10 seconds for the CPU% column to stabilise. Record the BASELINE value (e.g., "0.2%").
    3. Hold cmd+shift+e for 2 seconds. Observe the CPU% column during the press. Record the ACTIVE value (e.g., "0.6%").
    4. Release. Wait ~5 seconds. Observe the CPU% column should return to BASELINE ± noise (~0.2% on M-series).
    5. Repeat the press cycle 5 times in quick succession. Confirm CPU% returns to baseline after each release.
    6. Idle for 30 seconds with HUD hidden. Confirm CPU% does NOT drift upward.

    ## Expected Outcome
    - Idle CPU: ~0.1-0.5% (matches Hammerspoon baseline; HUD adds no measurable cost when hidden).
    - Active CPU during press: small bump (1-3%) — animation context + redraw, expected.
    - Returns to baseline within ~5s of release.
    - No CPU drift after sustained idle.

    ## Failure modes
    - CPU stays elevated after release → an animation loop or timer is still running; check `:hide()` call ordering and that no `hs.timer.doEvery` was added accidentally.
    - Baseline drifts upward over time → memory leak from canvas elements re-created per press; verify the create-once + show/hide pattern from RESEARCH Pattern 1 (no per-press `hs.canvas.new`).
    - High baseline (>1%) before any press → `wantsLayer(true)` not set; Core Animation not engaged; AppKit fallback path is heavier (RESEARCH Pitfall 5).

    ## Sign-off (documentation-only)
    - [ ] BASELINE CPU% recorded: _________ %
    - [ ] ACTIVE CPU% during press recorded: _________ %
    - [ ] CPU returns to baseline within 5s of release
    - [ ] No drift after 30s sustained idle

    **Tester:** _____________  **Date:** _____________
    ```

    File 4 — `tests/manual/test_hud_focus.md` (HUD-04 focus live):
    ```markdown
    # Manual Test: HUD does not steal focus (HUD-04 focus)

    **Requirement:** HUD-04 — HUD does not steal focus, does not interfere with paste, and does not appear in screen recordings (this walkthrough covers the focus half).

    **Prerequisites:**
    - Phase 3.5 HUD deployed
    - HUD enabled (default state)
    - Any text editor or terminal available for the focus check

    ## Steps
    1. Open a text editor (TextEdit, VS Code, Terminal). Click into it. Confirm the cursor is blinking (text-input focus).
    2. Hold cmd+shift+e. Observe the HUD appear at top-center. The cursor should STILL be in the editor (focus did not move to the HUD).
    3. While holding, type a few keys (the keys may go to recording — that's the point; we're just confirming focus is in the editor, not the HUD).
    4. Release. Observe HUD disappear. Cursor stays in editor.
    5. Click somewhere on the HUD pill area while a press cycle is active (rapid: press hotkey, click on the pill area, release — or use a second hotkey if needed). The click should pass THROUGH the HUD to whatever app is behind. The HUD should not become focused or move.
    6. Trigger cmd+tab while idle (no press). The HUD should NOT appear in the app switcher list.
    7. Open Mission Control (F3 or 4-finger swipe up). The HUD should NOT appear as a window in any Space (it has `canJoinAllSpaces` + `transient` behavior — system-managed-overlay style).

    ## Expected Outcome
    - Cursor stays in editor across press / hold / release.
    - Clicks on the HUD pass through to the app behind.
    - HUD does not appear in cmd+tab.
    - HUD does not appear in Mission Control as a manageable window.

    ## Failure modes
    - Cursor moves out of editor during press → `canBecomeKeyWindow` returning true on the HUD canvas; check the canvas has no input elements that override this (RESEARCH Priority 3).
    - Clicks land on HUD instead of app behind → `ignoresMouseEvents` not set; default is YES per `hs.canvas` libcanvas.m source — should not happen unless explicitly overridden.
    - HUD shows in cmd+tab → window collection behavior missing `transient`; verify `:behaviorAsLabels({"canJoinAllSpaces", "stationary", "transient"})` was applied (RESEARCH Pattern 1).

    ## Sign-off
    - [ ] Focus stays in editor during press / hold / release
    - [ ] Clicks pass through HUD to app behind
    - [ ] HUD absent from cmd+tab switcher
    - [ ] HUD absent from Mission Control window list

    **Tester:** _____________  **Date:** _____________
    ```

    File 5 — `tests/manual/test_hud_screen_capture.md` (HUD-04 recordings — documentation-only per RESEARCH Priority 2):
    ```markdown
    # Manual Test: HUD visibility in screen recordings (HUD-04 recordings — DOCUMENTATION-ONLY)

    **Requirement:** HUD-04 — HUD hidden from screen recordings by default (best-effort / limited scope per RESEARCH Priority 2).

    **Status:** **Documentation-only walkthrough.** This is NOT a pass/fail gate. Per RESEARCH Priority 2 (Apple Developer Forums thread 792152, 2025): on macOS 15+ (Sequoia), ScreenCaptureKit-based capture tools (modern QuickTime, OBS, Zoom share-screen, Discord, Loom, Teams) capture the HUD regardless of NSWindowSharingNone. Apple has stated there is no public API to prevent ScreenCaptureKit capture. This walkthrough documents the empirical state on Oliver's machine (macOS Sequoia 15.7.5).

    **Prerequisites:**
    - Phase 3.5 HUD deployed
    - HUD enabled (default state)
    - macOS Sequoia 15.7.5 or later (Oliver's current machine)

    ## Steps

    ### Sub-test A: legacy `screencapture` CLI
    1. Trigger HUD: hold cmd+shift+e (HUD visible at top-center).
    2. While HUD is visible, in a SEPARATE terminal: `screencapture -x /tmp/purplevoice_hud_test_legacy.png` (the `-x` flag suppresses the camera-shutter sound).
    3. Release cmd+shift+e.
    4. Open `/tmp/purplevoice_hud_test_legacy.png` in Preview.
    5. **Document:** is the HUD pill visible in the screenshot? (Expected on Sequoia 15.7+: HUD likely ABSENT — legacy screencapture uses CGWindowList which honours kCGWindowSharingNone... but this is unverified for 15.7+; record the actual outcome.)

    ### Sub-test B: modern QuickTime screen recording
    1. Open QuickTime Player → File → New Screen Recording.
    2. Start recording the entire screen.
    3. Trigger HUD: hold cmd+shift+e for ~3 seconds.
    4. Stop the QuickTime recording.
    5. Play back the recording.
    6. **Document:** is the HUD pill visible in the playback? (Expected on macOS 15+: HUD PRESENT — QuickTime uses ScreenCaptureKit which ignores NSWindowSharingNone.)

    ### Sub-test C (optional): Zoom share-screen
    Only run if Zoom is installed.
    1. Start a Zoom meeting (with yourself, no other participants needed).
    2. Click "Share Screen" → "Entire Screen".
    3. Trigger HUD via cmd+shift+e.
    4. Use Zoom's "view what's being shared" option (or screenshot the share-preview).
    5. **Document:** is the HUD visible? (Expected: PRESENT — Zoom uses ScreenCaptureKit on macOS 15+.)

    ## Expected Outcome (NOT a pass/fail gate)
    - Sub-test A (legacy screencapture): HUD likely ABSENT (CGWindowList path).
    - Sub-test B (QuickTime): HUD likely PRESENT (ScreenCaptureKit path; Apple has no public API to prevent this).
    - Sub-test C (Zoom): HUD likely PRESENT (ScreenCaptureKit path).
    - **The README + SECURITY.md HUD caveat (Plan 03.5-03) is the load-bearing artefact for this requirement, not this walkthrough.** This walkthrough produces evidence for the caveat's accuracy.

    ## Documentation Output
    Capture results in this format and link from the SUMMARY.md:

    | Sub-test | Tool | macOS version | HUD visible in capture? |
    |----------|------|---------------|-------------------------|
    | A | screencapture CLI | 15.7.5 | Y / N |
    | B | QuickTime screen recording | 15.7.5 | Y / N |
    | C | Zoom share-screen | 15.7.5 | Y / N |

    ## Sign-off (documentation-only)
    - [ ] Sub-test A run; result documented
    - [ ] Sub-test B run; result documented
    - [ ] Sub-test C run OR explicitly skipped (Zoom not available)
    - [ ] README ## Security & Privacy + SECURITY.md §"How to Verify" caveat language matches observed behaviour

    **Tester:** _____________  **Date:** _____________

    ## Reference: honest framing language for README + SECURITY.md (per RESEARCH Priority 2)
    > "PurpleVoice attempts to hide the HUD via NSWindowSharingNone (Hammerspoon hs.canvas pathway). On macOS 15+ (Sequoia), ScreenCaptureKit-based capture tools (modern QuickTime, OBS, Zoom share-screen, Discord, Loom, Teams) capture the HUD regardless — Apple has not exposed a public API to exclude windows from ScreenCaptureKit. The legacy `screencapture` CLI and CGWindowList-based tools still honour NSWindowSharingNone. For sensitive sessions, run with `PURPLEVOICE_HUD_OFF=1`."
    ```

    Write all five files. Verify each is a valid markdown file (no shell heredoc artifacts) and contains the literal "Tester: _____" sign-off pattern matching the existing test_menubar.md style.
  </action>

  <verify>
    <automated>for f in tests/manual/test_hud_appearance.md tests/manual/test_hud_disable.md tests/manual/test_hud_idle_cpu.md tests/manual/test_hud_focus.md tests/manual/test_hud_screen_capture.md; do [ -f "$f" ] || { echo "MISSING $f"; exit 1; }; grep -q "## Sign-off" "$f" || { echo "$f missing ## Sign-off"; exit 1; }; grep -q "Tester:" "$f" || { echo "$f missing Tester: line"; exit 1; }; done; grep -q "ScreenCaptureKit" tests/manual/test_hud_screen_capture.md || { echo "screen_capture walkthrough missing ScreenCaptureKit honest framing"; exit 1; }; grep -q "PURPLEVOICE_HUD_OFF=1" tests/manual/test_hud_disable.md || { echo "disable walkthrough missing env var"; exit 1; }; echo "OK: 5 manual walkthrough skeletons present, structurally valid"</automated>
  </verify>

  <done>
    Five new files in tests/manual/ — test_hud_appearance.md, test_hud_disable.md, test_hud_idle_cpu.md, test_hud_focus.md, test_hud_screen_capture.md. Each has H1 title, Requirement, Prerequisites, Steps, Expected Outcome, Failure modes, Sign-off, Tester/Date. test_hud_screen_capture.md contains the RESEARCH Priority 2 honest-framing reference language verbatim (the same language Plan 03.5-03 will paste into README + SECURITY.md).
  </done>
</task>

<task type="auto">
  <name>Task 0-4: Formalise HUD-01..04 in REQUIREMENTS.md (stub language + traceability rows)</name>

  <read_first>
    - .planning/REQUIREMENTS.md (full file — current state has TBD HUD-01..04 in ROADMAP.md but no entries here)
    - .planning/ROADMAP.md §"Phase 3.5: Hover UI / HUD" (proposed shape for HUD-01..04)
    - .planning/phases/03.5-hover-ui-hud/03.5-CONTEXT.md sections D-01..D-16 (locked decisions to derive language from)
    - .planning/phases/03.5-hover-ui-hud/03.5-RESEARCH.md §"Phase Requirements" table (proposed language matrix)
  </read_first>

  <action>
    Edit `.planning/REQUIREMENTS.md` to add four HUD-* entries and four traceability rows. This is a STUB pass — final language refinements happen in Plan 03.5-03 Task 3-3. The stub is enough to unambiguously map plans → requirements via frontmatter.

    Step 1 — Add a new `### Hover UI / HUD` subsection under `## v1 Requirements`. Insert it AFTER the existing `### Security` subsection (Phase 2.7's SEC-01..06 block ending around line 67) and BEFORE the existing `## v2 Requirements` heading (around line 69).

    Insert text:

    ```markdown
    ### Hover UI / HUD

    - [ ] **HUD-01**: Floating canvas widget appears within ~50ms of hotkey press and disappears within ~250ms of release. Implemented as `hs.canvas` overlay in `purplevoice-lua/init.lua`; lifecycle hooked into existing `onPress` (line 266) and `resetState()` (line 116) alongside `setMenubarRecording()`/`setMenubarIdle()`. Verified by `tests/manual/test_hud_appearance.md`.
    - [ ] **HUD-02**: User-toggleable visibility via `PURPLEVOICE_HUD_OFF=1` env var read once at module load (mirrors existing `PURPLEVOICE_NO_SOUNDS` line 99 idiom; default-ON per D-09). Verified by `tests/test_hud_env_off.sh` (string-level wiring) + `tests/manual/test_hud_disable.md` (live end-to-end).
    - [ ] **HUD-03**: Effectively zero CPU when idle — no animation loops, no timers; `hs.canvas` `:hide()` orders the NSWindow out and stops compositing; `wantsLayer(true)` engages Core Animation GPU compositing during visible state. Verified by `tests/manual/test_hud_idle_cpu.md` (documentation-driven `top -pid` baseline measurement; no specific pass/fail threshold).
    - [ ] **HUD-04**: Position configurable via `PURPLEVOICE_HUD_POSITION` env var with six locked named positions (`top-center` (default) | `top-right` | `bottom-center` | `bottom-right` | `near-cursor` | `center`); invalid values fall back to `top-center` with a single warning to the Hammerspoon console at module load (D-07). HUD does not steal focus (`hs.canvas` defaults `ignoresMouseEvents=YES`, `canBecomeKeyWindow=NO`). HUD visibility in screen recordings is **limited** — `NSWindowSharingNone` is honoured by legacy `screencapture` CLI but ignored by ScreenCaptureKit on macOS 15+ (Apple Developer Forums thread 792152, 2025); for sensitive sessions, run with `PURPLEVOICE_HUD_OFF=1`. Verified by `tests/test_hud_position_validation.sh` + `tests/manual/test_hud_focus.md` + `tests/manual/test_hud_screen_capture.md` (documentation-only).
    ```

    Step 2 — Add four new rows to the `## Traceability` table. Insert after the existing SEC-06 row (around line 149):

    ```markdown
    | HUD-01 | Phase 3.5: Hover UI / HUD | Pending |
    | HUD-02 | Phase 3.5: Hover UI / HUD | Pending |
    | HUD-03 | Phase 3.5: Hover UI / HUD | Pending |
    | HUD-04 | Phase 3.5: Hover UI / HUD | Pending |
    ```

    Step 3 — Update the **Coverage** counters near the bottom of the file:
    - `v1 requirements: 35 total` → `v1 requirements: 39 total (35 prior + HUD-01..04 added 2026-04-30)`
    - `Mapped to phases: 35 / 35 (100%)` → `Mapped to phases: 39 / 39 (100%)`

    Step 4 — Update the **Per-phase counts** table to add a Phase 3.5 row:

    ```markdown
    - Phase 3.5: Hover UI / HUD — 4 requirements (HUD-01..04) — Pending
    ```

    Step 5 — Update the trailing footer comment:
    - `*Last updated: 2026-04-29 — six Security requirements added for Phase 2.7 (Pending); traceability table extended.*`
    → `*Last updated: 2026-04-30 — four HUD requirements added for Phase 3.5 (Pending); traceability table extended.*`

    DO NOT modify any other entries in REQUIREMENTS.md. The HUD additions are purely additive.
  </action>

  <verify>
    <automated>set -e; grep -q "HUD-01" .planning/REQUIREMENTS.md; grep -q "HUD-02" .planning/REQUIREMENTS.md; grep -q "HUD-03" .planning/REQUIREMENTS.md; grep -q "HUD-04" .planning/REQUIREMENTS.md; grep -q "### Hover UI / HUD" .planning/REQUIREMENTS.md; for id in HUD-01 HUD-02 HUD-03 HUD-04; do grep -qE "^\| ${id} \| Phase 3\.5" .planning/REQUIREMENTS.md || { echo "missing traceability row for ${id}"; exit 1; }; done; grep -qE "v1 requirements: 39" .planning/REQUIREMENTS.md || { echo "Coverage count not updated to 39"; exit 1; }; grep -qE "Phase 3\.5: Hover UI / HUD" .planning/REQUIREMENTS.md; grep -qE "Last updated: 2026-04-30" .planning/REQUIREMENTS.md; echo "OK: HUD-01..04 stubs present, 4 traceability rows added, coverage counters updated"</automated>
  </verify>

  <done>
    REQUIREMENTS.md contains a new `### Hover UI / HUD` subsection with 4 stub entries (HUD-01..04, all `[ ]` Pending), 4 new traceability rows (Phase 3.5: Hover UI / HUD — Pending), Coverage counters updated 35 → 39, Per-phase counts table includes Phase 3.5 (4 reqs), trailing footer dated 2026-04-30. No prior entries modified.
  </done>
</task>

<task type="auto">
  <name>Task 0-5: Flip VALIDATION.md frontmatter wave_0_complete + nyquist_compliant</name>

  <read_first>
    - .planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md (full file)
  </read_first>

  <action>
    Edit `.planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md` frontmatter:
    - `nyquist_compliant: false` → `nyquist_compliant: true`
    - `wave_0_complete: false` → `wave_0_complete: true`
    - `status: draft` → `status: approved`
    - Add: `wave_0_completed: 2026-04-30`

    Also update the trailing **Approval** line at the bottom from:
    > `**Approval:** pending (set to \`approved YYYY-MM-DD\` after Wave 0 plan completes and \`wave_0_complete: true\`)`

    to:

    > `**Approval:** approved 2026-04-30 — Wave 0 plan 03.5-00 closed: 2 unit tests (RED-and-expected pre-wiring) + 5 manual walkthrough skeletons + REQUIREMENTS.md HUD-01..04 stubs + 4 traceability rows landed.`

    Also flip the Validation Sign-Off checkboxes that this Wave-0 plan satisfies (the implementation-side ones — HUD-01 live walkthrough, etc. — stay unchecked since Plan 03.5-03 will do those):
    - [x] All HUD-XX requirements have `<automated>` verify (unit tests) OR `<manual>` walkthrough OR Wave 0 dependency
    - [x] Sampling continuity: no 3 consecutive tasks without automated verify (achieved via per-commit `bash tests/run_all.sh` baseline)
    - [x] Wave 0 covers all MISSING references (7 Wave 0 items above) — note: 2 tests + 5 walkthroughs = 7
    - [x] No watch-mode flags
    - [x] Feedback latency < 60s
    - [x] `nyquist_compliant: true` set in frontmatter (after Wave 0 plan completes)

    Leave UNchecked (Plan 03.5-03 closes these):
    - [ ] 3 of 5 manual walkthroughs run live + signed off
    - [ ] 2 of 5 manual walkthroughs documentation-only
    - [ ] D-14 caveat language matches RESEARCH Priority 2 reframing
  </action>

  <verify>
    <automated>set -e; F=.planning/phases/03.5-hover-ui-hud/03.5-VALIDATION.md; grep -qE "^nyquist_compliant: true" "$F" || { echo "nyquist_compliant not flipped"; exit 1; }; grep -qE "^wave_0_complete: true" "$F" || { echo "wave_0_complete not flipped"; exit 1; }; grep -qE "^status: approved" "$F" || { echo "status not approved"; exit 1; }; grep -qE "^wave_0_completed: 2026-04-30" "$F" || { echo "wave_0_completed date missing"; exit 1; }; grep -q "Approval:.* approved 2026-04-30" "$F" || { echo "Approval line not updated"; exit 1; }; echo "OK: VALIDATION.md frontmatter flipped"</automated>
  </verify>

  <done>
    VALIDATION.md frontmatter has nyquist_compliant: true, wave_0_complete: true, status: approved, wave_0_completed: 2026-04-30. Approval line updated. Six Wave-0-satisfied checkboxes flipped to [x]; three implementation-stage checkboxes remain [ ] for Plan 03.5-03.
  </done>
</task>

</tasks>

<verification>
After all tasks complete:

1. **Functional suite — RED-and-expected:** `bash tests/run_all.sh` reports 8 PASS / 2 FAIL. The 2 FAIL are `test_hud_env_off.sh` + `test_hud_position_validation.sh` (init.lua not yet wired). This is the **expected** Wave 0 close state.
2. **Bash syntax:** `bash -n tests/test_hud_env_off.sh && bash -n tests/test_hud_position_validation.sh` exits 0.
3. **Manual walkthroughs:** 5 markdown files exist in tests/manual/ with required structural elements (Steps, Expected Outcome, Failure modes, Sign-off, Tester:).
4. **REQUIREMENTS.md:** HUD-01..04 entries + 4 traceability rows + Coverage 35→39 + Phase 3.5 in per-phase counts + footer dated 2026-04-30.
5. **VALIDATION.md:** `wave_0_complete: true` + `nyquist_compliant: true` + `status: approved`.
6. **Brand consistency:** `bash tests/test_brand_consistency.sh` exits 0 (no new voice-cc strings introduced; new env vars use PURPLEVOICE_ namespace).
7. **Framing lint:** `bash tests/test_security_md_framing.sh` exits 0 (no SECURITY.md changes in this plan; lint should be unaffected).
8. **No regression in other tests:** all 8 pre-existing tests/test_*.sh still GREEN.

The SUMMARY.md MUST explicitly document the 2 RED tests as "RED-and-expected — Plan 03.5-01 + Plan 03.5-02 will turn GREEN" so the orchestrator does not flag the regression at this commit.
</verification>

<success_criteria>
- 2 new tests/test_hud_*.sh files exist, executable, valid bash syntax, currently exit 1 with descriptive FAIL messages indicating PURPLEVOICE_HUD_OFF / PURPLEVOICE_HUD_POSITION not yet wired.
- 5 new tests/manual/test_hud_*.md files exist with proper structure (H1 title, Prerequisites, Steps, Expected Outcome, Failure modes, Sign-off, Tester/Date).
- test_hud_screen_capture.md contains the RESEARCH Priority 2 honest-framing reference language ("ScreenCaptureKit ... ignores NSWindowSharingNone").
- REQUIREMENTS.md has 4 new HUD-XX entries + 4 traceability rows + updated Coverage stats + Phase 3.5 in per-phase table.
- VALIDATION.md frontmatter flipped to wave_0_complete: true + nyquist_compliant: true + status: approved.
- Brand consistency lint stays GREEN (no voice-cc drift).
- Framing lint stays GREEN (no SECURITY.md edits).
</success_criteria>

<output>
After completion, create `.planning/phases/03.5-hover-ui-hud/03.5-00-SUMMARY.md` documenting:
- Files created (7 new) + files edited (2: REQUIREMENTS.md, VALIDATION.md)
- The expected RED-and-expected state of `bash tests/run_all.sh` (8 PASS / 2 FAIL)
- Confirmation that Plans 01 + 02 will turn the 2 RED tests GREEN
- Brand-consistency + framing-lint final state (both GREEN)
- Any deviations from this plan (Rule 1 / inline auto-fix patterns)
</output>
