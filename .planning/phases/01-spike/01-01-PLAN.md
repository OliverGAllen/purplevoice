---
phase: 01-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - setup.sh
  - vocab.txt.default
  - .gitignore
  - README.md
autonomous: false
requirements: [TRA-01, CAP-03]
must_haves:
  truths:
    - "Hammerspoon.app exists in /Applications/"
    - "sox binary exists at /opt/homebrew/bin/sox"
    - "whisper-cli binary exists at /opt/homebrew/bin/whisper-cli"
    - "Whisper small.en model exists at ~/.local/share/voice-cc/models/ggml-small.en.bin with correct SHA256"
    - "All XDG directories exist (~/.config/voice-cc/, ~/.local/share/voice-cc/models/, ~/.cache/voice-cc/, ~/.local/bin/, ~/.hammerspoon/voice-cc/)"
    - "vocab.txt is seeded with AI/dev terminology if absent (never clobbers user edits)"
    - "Manual pipeline command (sox -d ... | whisper-cli ...) produces a correct transcript on stdout for a hand-spoken sentence"
  artifacts:
    - path: "setup.sh"
      provides: "Idempotent installer for Phase 1 dependencies + dirs + model + vocab seed"
      contains: "brew install --cask hammerspoon"
    - path: "vocab.txt.default"
      provides: "Seed vocabulary list for whisper --prompt biasing"
      contains: "Anthropic"
    - path: "~/.local/share/voice-cc/models/ggml-small.en.bin"
      provides: "Whisper small.en model (~190MB, Q5_0)"
    - path: "~/.config/voice-cc/vocab.txt"
      provides: "User-editable vocabulary for whisper --prompt"
    - path: ".gitignore"
      provides: "Excludes models/, /tmp artefacts, OS noise"
    - path: "README.md"
      provides: "Stub documenting hotkey choice (cmd+option+space) and one-time setup pointer"
  key_links:
    - from: "setup.sh"
      to: "Homebrew"
      via: "brew install --cask hammerspoon / brew install sox / brew install whisper-cpp"
      pattern: "brew install"
    - from: "setup.sh"
      to: "HuggingFace ggerganov mirror"
      via: "curl -C - -L for resumable model download"
      pattern: "huggingface.co/ggerganov/whisper.cpp"
    - from: "setup.sh"
      to: "vocab.txt.default"
      via: "cp only if ~/.config/voice-cc/vocab.txt absent (no clobber)"
      pattern: "vocab.txt.default"
---

<objective>
Install Phase 1 runtime dependencies (Hammerspoon, sox, whisper.cpp), download the `ggml-small.en.bin` Whisper model with checksum verification, create XDG directory layout from day one, seed `vocab.txt` with AI/dev terminology, and prove the raw pipeline works by manually invoking `sox -d ... ; whisper-cli ...` end-to-end.

Purpose: Establish a verified, idempotent foundation. The bash glue (Plan 02) cannot be written until sox + whisper-cli are confirmed working on Oliver's actual machine with the right model in the right place. This plan also baselines the locked decisions from CONTEXT.md (XDG paths, model URL, vocab seed) into shipped files so Phase 3's `install.sh` is a refinement, not a rewrite.

Output: A `setup.sh` script that is safe to re-run, the model present at the XDG path, vocab seeded, and a recorded manual-validation result that proves the sox→whisper-cli pipeline produces correct transcripts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-spike/01-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- Locked decisions (D-01..D-08) baked into this plan. -->
<!-- Reference these directly in setup.sh; do NOT rederive. -->

XDG paths (D-03 — used verbatim):
  ~/.config/voice-cc/                              (config root)
  ~/.config/voice-cc/vocab.txt                     (user-editable vocab)
  ~/.local/share/voice-cc/models/                  (model storage)
  ~/.local/share/voice-cc/models/ggml-small.en.bin (the Whisper model)
  ~/.cache/voice-cc/                               (last.txt, error.log dest)
  ~/.local/bin/voice-cc-record                     (bash glue, symlinked in Plan 02)
  ~/.hammerspoon/voice-cc/                         (Lua module dir, symlinked in Plan 03)
  /tmp/voice-cc/                                   (single working WAV)

Model source (D-05, D-06):
  URL: https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.en.bin
  Download: curl -C - -L -o <path> <url>   (resumable)
  Verify: sha256sum -c (or `shasum -a 256 -c` on macOS) against hardcoded checksum
  Expected size: ~488 MB on disk (un-quantised small.en GGML — verify exact size during download; ~190 MB figure in CONTEXT.md refers to Q5_0 variant)

Brew packages (STACK.md Installation):
  brew install --cask hammerspoon       (D-01)
  brew install sox                       (provides sox + soxi)
  brew install whisper-cpp               (provides whisper-cli at /opt/homebrew/bin/whisper-cli)

Vocab seed (D-08 — exact list):
  Anthropic, Claude, Claude Code, Hammerspoon, MCP, Model Context Protocol,
  npm, TypeScript, GitHub, whisper.cpp, sox, Vercel, Supabase, JWT, OAuth,
  REPL, Tailwind, Next.js
  (Comma-separated single line; whisper --prompt has ~224-token cap.)

Hotkey decision (D-07 success criterion + Pitfall 5):
  cmd+option+space — push-and-hold. Document in README.md as the only Phase 1 hotkey.

Manual pipeline validation command (ARCHITECTURE.md "Build Order" STEP 1.1):
  /opt/homebrew/bin/sox -d -r 16000 -c 1 -b 16 /tmp/voicecc-validate.wav
    (Ctrl-C after speaking the test sentence)
  /opt/homebrew/bin/whisper-cli \
    -m ~/.local/share/voice-cc/models/ggml-small.en.bin \
    --language en \
    --prompt "$(cat ~/.config/voice-cc/vocab.txt)" \
    --no-timestamps \
    -f /tmp/voicecc-validate.wav
  Expected: stdout contains the spoken sentence with punctuation/capitalisation.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write idempotent setup.sh + vocab seed + .gitignore + README stub</name>
  <files>setup.sh, vocab.txt.default, .gitignore, README.md</files>
  <read_first>
    - .planning/phases/01-spike/01-CONTEXT.md (locked decisions D-01..D-08 — implement exactly)
    - .planning/research/STACK.md §"Installation" (brew commands + curl URL)
    - .planning/research/ARCHITECTURE.md §"install.sh idempotency" (lines ~378-424 — the idempotency template; this plan is the spike subset, not the full Phase 3 install.sh)
    - .planning/research/PITFALLS.md §"Pitfall 2" (absolute /opt/homebrew/bin/* paths from day one)
    - setup.sh (will not exist yet — confirm with `test -f setup.sh && echo exists` before Write)
  </read_first>
  <action>
    Create four files in the repo root.

    ===== setup.sh =====
    Use shebang `#!/usr/bin/env bash` and `set -euo pipefail` (per CONTEXT.md "Bash strict mode" + ARCHITECTURE.md anti-Pattern 1). Implement these steps in order, each idempotent:

    1. **Sanity-check macOS Apple Silicon prefix.** `if [ ! -d /opt/homebrew ]; then echo "voice-cc requires Apple Silicon Homebrew at /opt/homebrew (not detected). Aborting." >&2; exit 1; fi`

    2. **Homebrew dependencies (D-01, STACK.md).** For each package, check existence first:
       - Hammerspoon: `if [ ! -d /Applications/Hammerspoon.app ]; then brew install --cask hammerspoon; else echo "Hammerspoon.app already present, skipping."; fi`
       - sox: `brew list sox &>/dev/null || brew install sox`
       - whisper-cpp: `brew list whisper-cpp &>/dev/null || brew install whisper-cpp`

    3. **Verify binaries exist at expected absolute paths (Pitfall 2).** Loop over `/opt/homebrew/bin/sox`, `/opt/homebrew/bin/soxi`, `/opt/homebrew/bin/whisper-cli`. For each, `[ -x "$bin" ] || { echo "Missing: $bin" >&2; exit 1; }`.

    4. **Create XDG directory tree (D-03).** Single `mkdir -p` block:
       ```
       mkdir -p \
         "$HOME/.config/voice-cc" \
         "$HOME/.local/share/voice-cc/models" \
         "$HOME/.cache/voice-cc" \
         "$HOME/.local/bin" \
         "$HOME/.hammerspoon/voice-cc"
       ```

    5. **Download Whisper model with resume + checksum verify (D-05, D-06).**
       - `MODEL="$HOME/.local/share/voice-cc/models/ggml-small.en.bin"`
       - `MODEL_URL="https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-small.en.bin"`
       - `MODEL_SHA256="1be3a9b2063867b937e64e2ec7483364a79917e157fa98c5d94b5c1fffea987b"` (current published checksum for `ggml-small.en.bin` on HuggingFace ggerganov mirror as of 2025; if the download succeeds but verification fails, the executor MUST stop and re-fetch the published checksum from `https://huggingface.co/ggerganov/whisper.cpp/blob/main/ggml-small.en.bin` then update the constant — do not silently accept a mismatched file).
       - If file exists AND `shasum -a 256 "$MODEL" | awk '{print $1}'` matches `$MODEL_SHA256` → echo "Model present, checksum OK, skipping" and continue.
       - Else: `curl -C - -L --fail -o "$MODEL" "$MODEL_URL"` then re-verify. On verify failure: `rm -f "$MODEL"; echo "SHA256 mismatch — model corrupt or checksum stale" >&2; exit 1`.

    6. **Seed vocab.txt.default → ~/.config/voice-cc/vocab.txt (D-08, no-clobber).**
       - `VOCAB_DEST="$HOME/.config/voice-cc/vocab.txt"`
       - `if [ ! -f "$VOCAB_DEST" ]; then cp "$(dirname "$0")/vocab.txt.default" "$VOCAB_DEST"; echo "Seeded $VOCAB_DEST"; else echo "vocab.txt already exists, preserving user edits"; fi`

    7. **Print next-step reminders (do NOT auto-edit anything).** Echo a short block telling the user:
       - "Phase 1 setup complete."
       - "Hotkey will be cmd+option+space (push-and-hold) once Plan 03 wires Hammerspoon."
       - "Grant Hammerspoon Microphone + Accessibility on first launch (System Settings → Privacy & Security)."
       - "Disable macOS Dictation hotkey (System Settings → Keyboard → Dictation → Shortcut → Off) to avoid conflicts."

    ===== vocab.txt.default =====
    Single comma-separated line (D-08 exact list, in this order):
    `Anthropic, Claude, Claude Code, Hammerspoon, MCP, Model Context Protocol, npm, TypeScript, GitHub, whisper.cpp, sox, Vercel, Supabase, JWT, OAuth, REPL, Tailwind, Next.js`

    ===== .gitignore =====
    Lines:
    ```
    # macOS noise
    .DS_Store
    # Local working WAVs (Phase 1 uses /tmp; this guards future repo-local debugging)
    *.wav
    /tmp/voice-cc/
    # Model files (downloaded via setup.sh, never committed)
    *.bin
    *.mlmodelc/
    # Hammerspoon spoon caches
    .hammerspoon/
    # Editor cruft
    *.swp
    .idea/
    .vscode/
    ```

    ===== README.md =====
    Bare stub (full README is Phase 3). Include:
    - Title: `# voice-cc`
    - One-line core value (copy from STATE.md): "Speak → text appears in Claude Code, instantly and reliably, with no recurring cost or external dependency."
    - **Status:** "Phase 1 spike — proving the end-to-end loop on Oliver's machine. Not yet distributable."
    - **Hotkey:** `cmd+option+space` (push-and-hold). Locked decision; see `.planning/phases/01-spike/01-CONTEXT.md` D-01.
    - **Setup:** `bash setup.sh` (idempotent).
    - **Permissions to grant manually after first Hammerspoon launch:** Microphone + Accessibility for Hammerspoon.app (System Settings → Privacy & Security).
    - **Conflicting macOS feature to disable:** macOS Dictation hotkey (System Settings → Keyboard → Dictation → Shortcut → Off).

    Make setup.sh executable (`chmod +x setup.sh`) before declaring this task done.
  </action>
  <verify>
    <automated>bash -n setup.sh && grep -q "brew install --cask hammerspoon" setup.sh && grep -q "set -euo pipefail" setup.sh && grep -q "MODEL_SHA256" setup.sh && grep -q "shasum -a 256" setup.sh && grep -q "/opt/homebrew/bin/sox" setup.sh && grep -q "/opt/homebrew/bin/whisper-cli" setup.sh && grep -q "huggingface.co/ggerganov/whisper.cpp" setup.sh && grep -q "Anthropic" vocab.txt.default && grep -q "Hammerspoon" vocab.txt.default && grep -q "MCP" vocab.txt.default && grep -q "cmd+option+space" README.md && test -x setup.sh && grep -q "\*.bin" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n setup.sh` exits 0 (script is syntactically valid)
    - `setup.sh` contains `set -euo pipefail` (strict mode, CONTEXT.md "specifics")
    - `setup.sh` contains `brew install --cask hammerspoon` (D-01)
    - `setup.sh` contains `brew install sox` and `brew install whisper-cpp`
    - `setup.sh` contains `/opt/homebrew/bin/sox` AND `/opt/homebrew/bin/whisper-cli` as absolute path strings (Pitfall 2)
    - `setup.sh` contains `mkdir -p` for all five XDG dirs (.config, .local/share/.../models, .cache, .local/bin, .hammerspoon)
    - `setup.sh` contains `huggingface.co/ggerganov/whisper.cpp` (D-06)
    - `setup.sh` contains `curl -C -` (resumable download, D-05)
    - `setup.sh` contains `shasum -a 256` (checksum verification, D-05)
    - `setup.sh` contains a `MODEL_SHA256=` constant
    - `setup.sh` contains `if [ ! -f` or `[ -f` guard before model download (idempotent)
    - `setup.sh` contains `if [ ! -f` guard before vocab.txt copy (no-clobber, D-08)
    - `setup.sh` is executable (`test -x setup.sh` succeeds)
    - `vocab.txt.default` contains `Anthropic`, `Hammerspoon`, `MCP`, `Claude Code`, `whisper.cpp`, `Next.js` (D-08 exact list)
    - `vocab.txt.default` is a single line (commas, not newlines, between terms)
    - `README.md` contains the string `cmd+option+space`
    - `README.md` contains the words `Microphone` and `Accessibility` (permissions reminder)
    - `.gitignore` contains `*.bin` and `*.wav` and `.DS_Store`
  </acceptance_criteria>
  <done>
    setup.sh, vocab.txt.default, .gitignore, README.md exist in repo root with all acceptance criteria satisfied. Files are committed-ready (no syntax errors, no placeholder strings).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run setup.sh — install deps, download model, seed config</name>
  <files>(no repo files modified; mutates ~/.config/voice-cc/, ~/.local/share/voice-cc/, ~/.cache/voice-cc/, ~/.local/bin/, ~/.hammerspoon/voice-cc/, /Applications/Hammerspoon.app)</files>
  <read_first>
    - setup.sh (the script created in Task 1 — read it before running so a failure mid-execution can be diagnosed against the expected steps)
    - .planning/research/STACK.md §"Performance Notes" (sets the latency expectation against which manual validation is judged)
  </read_first>
  <action>
    Execute `bash setup.sh` from the repo root. This is a real, side-effecting run on Oliver's machine — not a dry-run.

    Expected runtime: ~2-5 minutes (Homebrew installs + ~488 MB model download). The model download is the long pole; on a slow connection, allow up to 10 minutes.

    On any non-zero exit, do NOT proceed. Read the error from stderr, fix the underlying issue (most likely: wrong `MODEL_SHA256` constant — re-fetch from `https://huggingface.co/ggerganov/whisper.cpp/blob/main/ggml-small.en.bin` and update Task 1's setup.sh, then re-run), and re-execute setup.sh. Setup.sh is idempotent so re-runs are safe.

    After successful exit, verify the post-conditions by running these commands and confirming output:

    ```
    test -d /Applications/Hammerspoon.app && echo "OK: Hammerspoon.app"
    test -x /opt/homebrew/bin/sox && echo "OK: sox"
    test -x /opt/homebrew/bin/soxi && echo "OK: soxi"
    test -x /opt/homebrew/bin/whisper-cli && echo "OK: whisper-cli"
    test -d ~/.config/voice-cc && echo "OK: ~/.config/voice-cc"
    test -d ~/.local/share/voice-cc/models && echo "OK: models dir"
    test -d ~/.cache/voice-cc && echo "OK: ~/.cache/voice-cc"
    test -d ~/.local/bin && echo "OK: ~/.local/bin"
    test -d ~/.hammerspoon/voice-cc && echo "OK: ~/.hammerspoon/voice-cc"
    test -f ~/.local/share/voice-cc/models/ggml-small.en.bin && echo "OK: model present"
    test -f ~/.config/voice-cc/vocab.txt && echo "OK: vocab seeded"
    grep -q "Anthropic" ~/.config/voice-cc/vocab.txt && echo "OK: vocab content"
    ls -lh ~/.local/share/voice-cc/models/ggml-small.en.bin   # expect ~488 MB
    ```

    All eleven `OK:` lines MUST print. If any do not, treat as a setup.sh bug, fix Task 1, re-run, re-verify.

    Then verify Silero VAD is bundled (open TODO from STATE.md — "validate Silero VAD is bundled"):
    `/opt/homebrew/bin/whisper-cli --help 2>&1 | grep -i vad` — expect at least one line mentioning `--vad`.

    If the brew bottle does NOT include VAD, this is a Phase 2 problem (Phase 2 needs the flag); for Phase 1 we just record the finding in the SUMMARY.md so Phase 2 planning knows to source-build whisper.cpp if needed.
  </action>
  <verify>
    <automated>test -d /Applications/Hammerspoon.app && test -x /opt/homebrew/bin/sox && test -x /opt/homebrew/bin/soxi && test -x /opt/homebrew/bin/whisper-cli && test -f ~/.local/share/voice-cc/models/ggml-small.en.bin && test -f ~/.config/voice-cc/vocab.txt && grep -q "Anthropic" ~/.config/voice-cc/vocab.txt && test -d ~/.config/voice-cc && test -d ~/.local/share/voice-cc/models && test -d ~/.cache/voice-cc && test -d ~/.local/bin && test -d ~/.hammerspoon/voice-cc</automated>
  </verify>
  <acceptance_criteria>
    - `bash setup.sh` exits 0 on first run from a clean state
    - `bash setup.sh` exits 0 on a second back-to-back run with no side effects beyond "skipping" log messages (idempotency proof)
    - `/Applications/Hammerspoon.app` exists
    - `/opt/homebrew/bin/sox`, `/opt/homebrew/bin/soxi`, `/opt/homebrew/bin/whisper-cli` all exist and are executable
    - `~/.local/share/voice-cc/models/ggml-small.en.bin` exists with file size > 400 MB and < 600 MB
    - `shasum -a 256 ~/.local/share/voice-cc/models/ggml-small.en.bin` matches the `MODEL_SHA256` constant in setup.sh
    - `~/.config/voice-cc/vocab.txt` exists and contains `Anthropic`
    - All five XDG directories exist
    - `whisper-cli --help` has been executed and its VAD support (or lack thereof) is noted for the Plan SUMMARY
  </acceptance_criteria>
  <done>
    All host-side post-conditions verified. The machine now has the full Phase 1 dependency surface installed and validated. `setup.sh` proven idempotent by a second run. VAD availability noted for Phase 2 reference.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manually validate the raw sox + whisper-cli pipeline (ARCHITECTURE.md STEP 1.1)</name>
  <what-built>
    Phase 1 dependencies installed (Hammerspoon, sox, whisper-cli, ggml-small.en.bin model). Vocab seeded. XDG directories exist. The raw pipeline is *available* but not yet *exercised* — this checkpoint exercises it manually before Plan 02 builds the bash glue around it.

    This is the "manual pipeline → bash glue → Hammerspoon" build order's STEP 1.1 (ARCHITECTURE.md §"Build Order"). Skipping it means Plan 02's bash glue could be debugging two unknowns at once: (a) "does the pipeline work?" and (b) "does my glue work?".
  </what-built>
  <how-to-verify>
    Execute these commands manually in a Terminal (NOT via Hammerspoon — that's Plan 03's responsibility). Hammerspoon does not need to be granted Microphone yet for this test; Terminal will use its own TCC grant (which macOS will prompt for on first sox run if not previously granted).

    **Step A — Record a 5-second test clip:**
    ```
    /opt/homebrew/bin/sox -d -r 16000 -c 1 -b 16 /tmp/voicecc-validate.wav
    ```
    Speak this exact sentence into the default mic: **"Refactor the auth middleware to use JWTs instead of session cookies."** (CONTEXT.md "Reference utterance for success criterion 1".) Press Ctrl-C when done. If macOS prompts for Microphone access for Terminal, grant it.

    Confirm the WAV exists and has non-trivial duration:
    ```
    /opt/homebrew/bin/soxi -D /tmp/voicecc-validate.wav   # expect 3-8 (seconds)
    ls -lh /tmp/voicecc-validate.wav                       # expect 100-300 KB
    ```

    **Step B — Transcribe with vocab biasing:**
    ```
    /opt/homebrew/bin/whisper-cli \
      -m ~/.local/share/voice-cc/models/ggml-small.en.bin \
      --language en \
      --no-timestamps \
      --prompt "$(cat ~/.config/voice-cc/vocab.txt)" \
      -f /tmp/voicecc-validate.wav
    ```
    Wait ~1-2 seconds. Output is printed to stdout interleaved with stderr progress.

    **Step C — Confirm three things:**
    1. **Transcript correctness (ROADMAP success criterion 1 baseline):** the printed text contains "Refactor", "auth middleware", "JWT" (or "JWTs"), "session cookies". Punctuation/capitalisation present (success criterion 3).
    2. **Vocab biasing (ROADMAP success criterion 4 baseline):** "JWT" / "JWTs" is spelt as letters (not "jay double yew tee" or phonetic), and not transcribed as "jewts" or similar — confirming `--prompt` biased toward the technical term.
    3. **Latency:** the wall-clock from invoking whisper-cli to printed transcript is under ~2 seconds (subjective; precise measurement is Phase 3's hyperfine territory).

    **Step D — Bonus vocab-bias A/B (optional but informative for the SUMMARY):**
    Re-record a sentence containing "Anthropic", "Hammerspoon", "MCP". Run whisper-cli twice — once with `--prompt "$(cat ~/.config/voice-cc/vocab.txt)"`, once with `--prompt ""`. Confirm the vocab-prompted run gets at least one of those terms correct that the empty-prompt run gets wrong (or that both get them right, which is also fine — the model may already know them).

    **If the pipeline does NOT produce a correct transcript:**
    Report what failed. Most likely causes:
    - Model file corrupt → re-run `setup.sh` (it will re-verify checksum and refuse to proceed if mismatched).
    - Microphone not granted to Terminal → grant in System Settings → Privacy & Security → Microphone.
    - Audio device not the expected mic (e.g., AirPods auto-switched) → set `SwitchAudioSource -s "MacBook Pro Microphone"` or unplug AirPods.

    Type your finding in the resume signal.
  </how-to-verify>
  <resume-signal>
    Type "approved" if the manual pipeline produced a correct transcript with native punctuation/capitalisation in under ~2 seconds. Otherwise describe what failed (transcript text, latency observed, error messages) so the issue can be diagnosed before Plan 02 builds glue around a broken pipeline.
  </resume-signal>
</task>

</tasks>

<verification>
After all tasks complete, walk these checks (these are the Phase 1 ROADMAP success criteria #2, #3, #4, #5 *baselined* — not the full end-to-end test, which requires Plan 03):

- [ ] `bash setup.sh` is idempotent (proven by second run producing only "skipping" output, no errors)
- [ ] All XDG paths exist exactly as defined in CONTEXT.md D-03
- [ ] Model file present, executable binaries verified at absolute paths (Pitfall 2 prevention starts here)
- [ ] Manual pipeline (sox → whisper-cli) produces correct transcript with punctuation (success criteria #2 partial, #3)
- [ ] Vocab biasing demonstrably works (success criterion #4 baseline)
- [ ] Absolute paths used everywhere (success criterion #5)
- [ ] Hotkey choice (cmd+option+space) documented in README — implementation comes in Plan 03
- [ ] Phase 2 scope items (VAD flag, denylist, duration gate, clipboard preserve, indicator, audio cues, trap cleanup, TCC detection) are NOT in setup.sh
</verification>

<success_criteria>
**Plan complete when:**
1. `setup.sh`, `vocab.txt.default`, `.gitignore`, `README.md` exist in repo root with all acceptance criteria from Task 1 satisfied.
2. `setup.sh` runs to exit 0 on a clean machine and is proven idempotent on second run.
3. The manual sox → whisper-cli pipeline produces a correct, punctuated transcript of the reference utterance in under ~2 seconds (Task 3 returns "approved").
4. All Phase 1 dependencies are installed at absolute paths matching `/opt/homebrew/bin/*`.
5. Whisper model verified by SHA256 at `~/.local/share/voice-cc/models/ggml-small.en.bin`.
6. Vocab seed copied to `~/.config/voice-cc/vocab.txt` only if absent (no-clobber).

**Plan does NOT include** (Phase 2/3 scope — do not add):
- bash glue script `voice-cc-record` (Plan 02)
- Hammerspoon Lua module (Plan 03)
- VAD flag, denylist, duration gate (Phase 2)
- Clipboard preserve/restore (Phase 2)
- Menu-bar indicator, audio cues (Phase 2)
- TCC silent-deny detection (Phase 2)
- Failure notifications (Phase 2)
- Full README walkthrough, install.sh refinement, hyperfine (Phase 3)
</success_criteria>

<output>
After completion, create `.planning/phases/01-spike/01-01-SUMMARY.md` recording:
- Files created (setup.sh, vocab.txt.default, .gitignore, README.md) with line counts
- The exact `MODEL_SHA256` value used (so future planners can verify it didn't drift)
- Result of the manual pipeline test: actual transcript text vs spoken sentence, observed wall-clock latency
- Result of `whisper-cli --help | grep vad` — does the brew bottle ship VAD? (Open TODO from STATE.md — needed for Phase 2 planning)
- Any deviations from the plan (e.g., if the SHA256 had to be updated, record the new value and source)
- Phase 1 build-order status: "STEP 1.1 complete; ready for Plan 02 (bash glue, STEP 1.2)"
</output>
